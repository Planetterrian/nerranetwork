{#- Shared chrome for the legal / trust pages (AI disclosure, privacy
    policy, terms of service). Included from each page's ``head_extra``
    block so the three pages share one look — and that look is the
    site's (DM Sans / Source Serif 4, the ``--nn-*`` tokens), not the
    hand-written Inter stylesheet the pages carried until Sep 2026. -#}
<style>
    .legal-hero {
        padding: calc(var(--nav-height) + var(--sp-12)) var(--sp-6) var(--sp-8);
        text-align: center;
        position: relative;
        overflow: hidden;
    }
    .legal-hero::before {
        content: '';
        position: absolute;
        inset: 0;
        background: radial-gradient(ellipse at 50% 0%, rgba(107, 71, 255, 0.15), transparent 70%);
        pointer-events: none;
    }
    .legal-hero h1 {
        font-family: var(--font-heading);
        font-size: clamp(2rem, 4.5vw, 2.8rem);
        font-weight: 700;
        color: var(--nn-white);
        margin: 0 0 var(--sp-3);
        line-height: 1.15;
    }
    .legal-hero .legal-meta {
        font-family: var(--font-ui);
        font-size: 0.85rem;
        color: var(--nn-text-muted);
        text-transform: uppercase;
        letter-spacing: 0.08em;
    }
    .legal-hero p.lede {
        font-family: var(--font-body);
        font-size: clamp(1rem, 1.8vw, 1.15rem);
        color: var(--nn-text);
        line-height: 1.6;
        max-width: 680px;
        margin: var(--sp-4) auto 0;
    }
    .legal-body {
        max-width: 820px;
        margin: 0 auto;
        padding: 0 var(--sp-6) var(--sp-12);
    }
    .legal-body h2 {
        font-family: var(--font-heading);
        font-size: clamp(1.25rem, 2.4vw, 1.6rem);
        font-weight: 700;
        color: var(--nn-white);
        margin: var(--sp-10) 0 var(--sp-3);
        padding-top: var(--sp-4);
        border-top: 1px solid var(--nn-border);
    }
    .legal-body h3 {
        font-family: var(--font-heading);
        font-size: 1.05rem;
        font-weight: 600;
        color: var(--nn-cyan);
        margin: var(--sp-6) 0 var(--sp-2);
    }
    .legal-body p,
    .legal-body li {
        font-family: var(--font-body);
        font-size: 1rem;
        color: var(--nn-text);
        line-height: 1.7;
    }
    .legal-body p { margin: 0 0 var(--sp-3); }
    .legal-body ul, .legal-body ol {
        padding-left: var(--sp-6);
        margin: 0 0 var(--sp-4);
    }
    .legal-body li { margin-bottom: var(--sp-2); }
    .legal-body strong { color: var(--nn-white); }
    .legal-body a { color: var(--nn-cyan); }
    .legal-body a:hover { color: var(--nn-white); }
    .legal-callout {
        background: linear-gradient(135deg, rgba(107, 71, 255, 0.15) 0%, rgba(0, 212, 255, 0.08) 100%);
        border-left: 4px solid var(--nn-purple);
        border-radius: 6px;
        padding: var(--sp-5);
        margin: var(--sp-6) 0;
    }
    .legal-callout p:last-child { margin-bottom: 0; }
    .legal-callout p { color: var(--nn-white); font-size: 1.05rem; }
    .legal-box {
        background: var(--nn-card);
        border: 1px solid var(--nn-border);
        border-radius: 12px;
        padding: var(--sp-5);
        margin: var(--sp-5) 0;
    }
    .legal-box h3 { margin-top: 0; }
    .legal-box p:last-child, .legal-box ul:last-child { margin-bottom: 0; }
    .legal-warning {
        background: rgba(255, 171, 64, 0.08);
        border-left: 4px solid #FFAB40;
        border-radius: 6px;
        padding: var(--sp-4) var(--sp-5);
        margin: var(--sp-5) 0;
    }
    .legal-warning p { margin: 0; }
    .legal-grid {
        display: grid;
        grid-template-columns: repeat(auto-fit, minmax(240px, 1fr));
        gap: var(--sp-3);
        margin: var(--sp-5) 0;
    }
    .legal-grid .legal-box { margin: 0; }
    .legal-grid h4 {
        font-family: var(--font-heading);
        font-size: 0.95rem;
        font-weight: 700;
        color: var(--nn-purple);
        margin: 0 0 var(--sp-2);
    }
    .legal-grid p { font-size: 0.92rem; margin: 0; }
    .legal-table {
        width: 100%;
        border-collapse: collapse;
        margin: var(--sp-4) 0 var(--sp-5);
        font-family: var(--font-ui);
        font-size: 0.92rem;
    }
    .legal-table th, .legal-table td {
        text-align: left;
        padding: var(--sp-2) var(--sp-3);
        border-bottom: 1px solid var(--nn-border);
        vertical-align: top;
        color: var(--nn-text);
    }
    .legal-table th {
        color: var(--nn-white);
        font-weight: 600;
        background: var(--nn-surface);
    }
    .legal-table-wrap { overflow-x: auto; }
    .legal-checklist { list-style: none; padding-left: 0; }
    .legal-checklist li { padding-left: 1.6rem; position: relative; }
    .legal-checklist li::before {
        content: '✓';
        position: absolute;
        left: 0;
        color: var(--nn-cyan);
        font-weight: 700;
    }
    .legal-footer-note {
        margin-top: var(--sp-10);
        padding-top: var(--sp-5);
        border-top: 1px solid var(--nn-border);
        text-align: center;
        font-family: var(--font-ui);
        font-size: 0.85rem;
        color: var(--nn-text-muted);
    }
    .legal-footer-note .nn-highlight { color: var(--nn-purple); font-weight: 500; }
    .legal-related {
        display: flex;
        flex-wrap: wrap;
        gap: var(--sp-2);
        justify-content: center;
        margin-top: var(--sp-4);
    }
    .legal-related a {
        font-family: var(--font-ui);
        font-size: 0.82rem;
        padding: 0.35rem 0.9rem;
        border: 1px solid var(--nn-border);
        border-radius: 999px;
        color: var(--nn-text);
        text-decoration: none;
    }
    .legal-related a:hover { border-color: var(--nn-purple); color: var(--nn-white); }
    @media (max-width: 640px) {
        .legal-body { padding: 0 var(--sp-4) var(--sp-10); }
    }
</style>
