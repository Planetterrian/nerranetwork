# spacex — quality review (2026-08-05)

_Generated by the Grok-4.3 Show Review Agent (`scripts/run_show_review.py`). Proposed prompt/audio changes are listed in the PR body under "A/B-listen required" and are NOT applied — the operator applies and listens before merging (landmine #17)._

## Evidence

Snapshot (`scripts/review_snapshot.py spacex`) on Ep50–59 `_tts.txt` + Whisper transcripts + `chapters_ep050.json`–`chapters_ep059.json` + `shows/spacex.yaml` (llm/chapters/deep_dive/youtube) + `shows/prompts/spacex_podcast.txt` / `spacex_digest.txt` + ledger through 2026-07-30.

- **Length:** 7/10 scripts below `min_podcast_words: 1300` (Ep50 1155, Ep51 1176, Ep52 1057, Ep54 1201, Ep55 1288, Ep56 1299, Ep57 1188). Only Ep53 1307, Ep58 1359, Ep59 1778 (Q2 earnings special) clear the floor. Special target is `deep_dive.min_podcast_words: 2400` — Ep59 1778 is ~26% under its own special floor.
- **Boilerplate (transcript n-grams):** `from an engineering standpoint` **10/10**; `on the ai front` 8/10 (required AI chapter anchor); `worth keeping an eye on` 7/10 (required Counterpoint anchor); network AI-voice outro 10/10 (expected); `this is spacex daily episode` 8/10.
- **Chapters:** snapshot says 10/10 “clean,” but Ep58’s JSON is not — six mid-body chapters are raw story headlines prefixed `Title:` (e.g. `Title: SpaceX knocks out 50th launch on Space Coast this…`). Ep50–51 lack any Introduction chapter (first marker is Counterpoint at ~222s / ~257s). Ep59 special shape (Intro → Counterpoint → Engineering → Closing) is acceptable for the deep-dive template.
- **Fetch-filter leakage:** 0 hits on `exclude_title_patterns` vs last 10 digests (junk-title filter holding).
- **SPCX price-twice:** still present on every daily with a market line — e.g. Ep50 transcript: market note “SPC X closed at $112.20, down 0.8%” **and** closing “SPC X is trading at $112.20, down 0.8%”; same doubled pattern Ep51–58.
- **Entity bleed:** Ep53 transcript: “SpaceX AI added another data center building in Memphis” / “SpaceX delayed removal of XAI's unpermitted temporary turbines” — xAI Colossus attributed as SpaceX (July-02 network finding still live).
- **Audience:** OP3 downloads_7d 534, downloads_30d 1179, weekly trend [100, 191, 334, 534] — strong upward trajectory.
- **Cost:** ~$0.44/episode avg over last 10.
- **Already-on levers:** `digest_expand_below_target: true`, `min_digest_words: 1200`, `podcast_expand_below_target: false` (July-29 network ban). Digest + podcast prompts already state Engineering Deep Dive ~280–360w. No podcast-side length lever is legitimate.

## Scoring prior predictions

| Metric | Verdict | Evidence |
|--------|---------|----------|
| exact 'from an engineering standpoint' occurrences in next 10 transcripts (2026-07-30) | miss | Still **10/10** in Ep50–59 transcripts; shape de-seed was proposed only, never applied |
| laundered SEO / sports-fixture / video-ID junk titles in digests (2026-07-02 network) | hit | Snapshot fetch-filter leakage: **0 hits** on last 10 digests |
| episodes >=1300w / teaser-frame (2026-07-15/17, already scored miss in 07-30) | miss (reconfirmed) | 3/10 ≥1300w this window; do not re-file the same length prediction |

## P0 — listener-facing bugs shipping today

### P0-1 — Ep58 malformed chapter titles (`Title:` story headlines)
`digests/spacex/chapters_ep058.json` ships six chapters whose titles are truncated digest/headline strings with a literal `Title:` prefix, e.g. `Title: SpaceX knocks out 50th launch on Space Coast this…`, `Title: US Space Force Awards SpaceX $1.6 Billion for…`, `Title: Heat Is an Orbital Data Center’s Greatest Foe.…`. These are not section anchors from `shows/spacex.yaml` `section_markers` (Introduction / Counterpoint / AI & Compute / Engineering / Market Watch / Teaser / Closing). Listener-facing chapter UI and any downstream YouTube/podcast chapter surface get spam titles.

**Proposal:** code-only sanitizer + drift-guard — drop or rewrite any chapter title matching `^Title:\s*` / obvious headline truncation; assert in `tests/test_spacex_show.py` (or network chapter validity) that no committed `chapters_ep*.json` title starts with `Title:` or ends with a bare ellipsis artifact. No audio change.

## P1 — quality ceiling

### P1-1 — `from an engineering standpoint` is a 10/10 verbatim tic (chapter-anchor collision)
Transcripts Ep50–59 all open the deep-dive with the exact phrase. It is **also** a required chapter key: `shows/spacex.yaml` pattern `engineering deep dive|engineering angle|from an engineering standpoint|the engineering reality` and `spacex_podcast.txt` REQUIRED line allowing only `from an engineering standpoint` **or** `the engineering angle`. Prior pass proposed a de-seed that never shipped; phrase frequency did not move (10/10).

**Proposal (prompt, A/B-listen):** keep the allowed anchor **set** for chapters, force rotation across the set, ban reusing the prior episode’s exact opener (shape + MEMORY, no new quotable full-sentence example). Prefer that “the engineering angle” / “the engineering reality” actually appear so the 10/10 monoculture breaks without breaking chapter detection.

### P1-2 — Chronic under-length — ESCALATE (do not re-file)
7/10 dailies under 1300w; same digest-ceiling diagnosis as June–July reviews; podcast-side expand already off network-wide; digest expand + deep-dive word guidance already on. Length predictions conditioned on “if deep-dive lever ships” have missed repeatedly. Per playbook meta-rule: **stop re-proposing the same lever**.

**Operator decision needed:** (a) accept grok-4.3 plateau at ~1100–1300 for thin news days, (b) raise `min_digest_words` / strengthen full-text fetch substrate, or (c) accept more thin-day deep-dive-led episodes like Ep52 (engineering-first when news is empty) as the product shape. No new podcast word-floor or expand-retry proposal this pass.

### P1-3 — SPCX price spoken twice per daily — ESCALATE (do not re-file)
Deferred since 2026-06-13; chapter blocker cleared 2026-06-18 (Closing before Market Watch); still doubled on Ep50–58 (market segment + `{closing_block}` price sentence). Identical “make Market Watch qualitative / single quote in closing” proposal has been carried without shipping across ≥5 reviews.

**Operator decision needed:** ship the coupled prompt+closing_block change (Market Watch qualitative; one precise price only in closing) as an explicit A/B-listen, or permanently accept double price as brand. Not re-proposed as a silent fix this pass.

### P1-4 — xAI / SpaceX entity conflation still on-air
Ep53: “SpaceX AI added another data center building in Memphis”; turbines/Colossus framed as SpaceX. July-02 network pass already flagged Memphis/Colossus as xAI’s and banned `SpaceXAI`. Still live.

**Proposal (prompt, A/B-listen):** hard entity line in podcast (+ digest reinforcement): Colossus/Memphis turbines/xAI compute are xAI unless the digest explicitly states a SpaceX-owned facility; never “SpaceXAI”; attribute cross-company deals as deals.

### P1-5 — Q2 earnings special under its own floor
Ep59 (deep-dive path) 1778w vs `deep_dive.min_podcast_words: 2400` and podcast prompt target 2400–3000. Special brief prompt asks 2000–2800w; thin brief → thin special. Soft-skip floor (0.6×2400) still passes 1778, so it published short.

**Proposal:** digest-side only — raise pressure on `spacex_deep_dive.txt` segment floors already listed (Segments 2–7 sentence counts) with an explicit brief-word check in the special path docs/operator runbook; do **not** re-enable podcast expand. Optional code: surface `deep_dive` length miss in metrics when `episode_kind=deep_dive` and words < min.

### P1-6 — Missing Introduction chapters on some dailies
Ep50–51 `chapters_ep*.json` start at Counterpoint (no Introduction). Identity line “This is SpaceX Daily…” is 8/10 in the n-gram table — two episodes omitted or de-anchored it after cold-open. Pattern `Space ?X Daily|welcome to Space ?X` is correct post-pronunciation; compliance is the gap.

**Proposal:** drift-guard test that dailies (non-special) have an Introduction chapter when a Closing exists; prompt already requires exact `{intro_line}` — no prompt rewrite unless a further episode drops it post-fix.

## P2 — growth / discoverability

- OP3 trend is the healthiest signal on this show (100→534 weekly). No metadata emergency.
- Tomorrow-teaser “Watch for the [first/next] \<test\>” monoculture has loosened (Ep50–58 mostly “Before we go, \<concrete catalyst\>”); deprioritize teaser rotation vs engineering-anchor rotation.
- Shorts motion A/B ended 2026-08-01; `shorts_first_is_hook: true` restored — outside this audio pass.
- RU funnel / video podcast pilot — no new defect in this window’s scripts.

## Deferred

- **Operator decision: length plateau** — accept vs digest-substrate escalation only (no podcast-side levers).
- **Operator decision: SPCX price-once** — coupled Market Watch qualitative + single closing quote; A/B-listen; stop silent carry-forward.
- Engineering Deep Dive ~280–360w as a *hope* metric without a new substrate change (already in prompts; scoring it again without a shipped lever is conditional → banned).
- Teaser opener rotation (P2; frame no longer 8/10 dead).
- June-28 missed Sunday recap scheduler forensics (operator; weekly_summary_segment now true — verify Sunday path still fires).
- NASA b-roll pool follow-through after shorts_ab end (render-side; not this audio pass).

## What would ship vs recommend

This agent cannot edit the tree. Proposals only:
1. Code: sanitize `Title:` chapter titles + drift-guard (P0).
2. Prompt: rotate engineering chapter-anchors by shape + prior-episode MEMORY ban (P1, A/B).
3. Prompt: xAI vs SpaceX entity discipline (P1, A/B).
4. Metrics/runbook: deep-dive special length visibility (P1, no audio).
5. Escalations only: length plateau + price-once.
