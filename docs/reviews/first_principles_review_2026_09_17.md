# first_principles — quality review (2026-09-17)

_Generated by the Grok-4.3 Show Review Agent (`scripts/run_show_review.py`). Proposed prompt/audio changes are listed in the PR body under "A/B-listen required" and are NOT applied — the operator applies and listens before merging (landmine #17)._

## Evidence

Snapshot (`scripts/review_snapshot.py first_principles`): last-10 `_tts.txt` word counts are **all** under the live `llm.min_podcast_words: 1400` floor (ep94–103: 1200–1388w). Chapters clean 10/10 with Closing present (June-23 marker-reorder still holds — `chapters_ep094.json`–`chapters_ep103.json`). Digest-heading leaks: 0. Cost ~$0.31/ep.

Audience: OP3 7d=18 · 30d=176 · WoW −74.1%; first-week downloads median **0** over 3 episodes. Newsletter + YouTube are already ON in `shows/first_principles.yaml` (launch-era `do_not_retry` on enabling distribution is stale as a blocker; growth is still flat).

Density audit (last 10): digest-verbatim 70–86%, coverage 89–100%, filler ~0–2%, chapters shaped from real section markers. Scripts are dense and on-thesis; the ceiling problem is substrate/plateau, not empty airtime.

Whisper transcripts ep94–103:
- Expected network outro/AI-disclosure boilerplate dominates the cross-episode repeated-phrase list (not actionable tics).
- **`on the order of`**: absent from the 10-transcript window (prior baseline 6/10) — the podcast-prompt verbatim ban landed.
- **Successor hedging tic**: Ep097 speaks *"A rough magic wand estimate for the finished slug begins with the commodity price of the type metal itself"* — the older formula returned.
- Root seed still live in `shows/prompts/first_principles_system.txt`: the accuracy block literally lists `"on the order of"` and `"a rough magic-wand estimate"` as preferred hedging examples, so the podcast ban is fighting the system prompt every episode.
- Closing line Whisper-garble *"share it with someone who depreciated"* recurs on the standard closer (ep94/96/98/99/100/101); ep102–103 use a different closer (*"That's a wrap on today's first principles breakdown…"*). Treat as TTS/Whisper read of the stock closer, not a new script bug — no phonetic fix (landmine #17).

Config already matches network length policy: `podcast_expand_below_target: false` (July 29 disable), `digest_expand_below_target: true` + `min_digest_words: 1200` (Aug 20 retune after operator listen found write-past-the-floor repetitive). Grok-4.6 pin withdrawn Aug 27 (latency/outage). `source_integrity.enforce: true` / `on_failure: block` is correctly set for this narrative show.

## Scoring prior predictions

| metric | verdict | evidence |
|--------|---------|----------|
| episodes below 1500-word floor in last 10 `_tts.txt` | miss | Floor is now 1400; **10/10** last scripts are 1200–1388w (worse than July 29’s 6/10 below 1500) |
| occurrences of `on the order of` across 10 transcripts | hit | Phrase absent from ep94–103 Whisper window (baseline 6/10); ban in `first_principles_podcast.txt` held |

## P0

None. Chapters include Closing on every sampled episode; no spoken segment-label leaks in transcripts; no committed offensive content.

## P1

1. **Chronic under-floor scripts (10/10)** — ep94–103 all below `min_podcast_words: 1400` (snapshot). Length predictions have now missed/partial’d across July 9 → 10 → 16 → 29 → this pass with the same digest-retry family of proposals. Per July 2026 category rules: **no podcast-side length lever**; **do not re-file** the same digest-tuning proposal a third time. Operator already lowered brief pressure on Aug 20 after a listen verdict of repetitive expansion, disabled podcast expand, and withdrew 4.6. This is an **operator-decision item**, not another prompt shove (see Deferred).

2. **System prompt re-seeds banned hedging tics** — `shows/prompts/first_principles_system.txt` accuracy block still exemplifies hedging with the exact strings `"on the order of"` and `"a rough magic-wand estimate"`. Podcast prompt correctly bans `on the order of` (hit), but Ep097 revived the magic-wand formula because the system prompt keeps teaching it. De-seed **by shape** (no replacement quotable example) + extend the podcast verbatim ban to the magic-wand estimate family; record a successor-tic watch.

3. **High digest-verbatim (70–86%)** — density audit. Acceptable for a brief→narration narrative show when coverage stays high and filler is ~0, but it means podcast-stage “walk the arithmetic” instructions are mostly pass-through. No model bump available (4.6 withdrawn). Do **not** add podcast expand or word-floor prompt pressure. Monitor only unless verbatim climbs with filler.

## P2

- Discoverability/growth: 30d downloads 176 but first-week median 0 on recent eps; YT retention fields empty in snapshot (0 videos scored). Titles from `chapters_ep*.json` are strong hook sentences — not the bottleneck. No metadata bug found in sampled chapter JSON.
- Cross-promo tails fire often (Models & Agents, SpaceX Daily, Omni View, etc.); fine, not a tic class unto itself.

## Deferred

- **Operator decision — length floor vs plateau (ESCALATE):** Same under-length metric has missed repeatedly (July 16 partial → July 29 partial/miss → today 10/10 under 1400). Identical digest-retry proposals are exhausted; podcast-side levers and prompt escalation are `do_not_retry`. Decision needed: (a) accept grok-4.3’s ~1200–1400 narrative plateau and lower `min_podcast_words` to a true thin-pass tripwire (~1100) so the dashboard stops false-alarming, or (b) invest in non-prompt substrate (richer topic-queue briefs / licensed numbers) knowing Aug 20 already rejected “write past the floor” as repetitive on listen. Do not ship another length prompt.
- Garbage auto-segment chapter titles (shared network LLM-title class) — not observed in ep94–103; keep on backlog only.
- Further length escalation via prompt / podcast expand — `do_not_retry`.
- Phonetic respellings for closer garble (“depreciated”) — landmine #17; reword template only if operator confirms the written closer is the cause.
