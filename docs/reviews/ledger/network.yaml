# Review ledger for the cross-cutting `network` target.
# Schema: docs/reviews/ledger/README.md. Feeds the meta-review (verdict
# rates by finding category) per the README.
reviews:
  - date: 2026-07-02
    pr: null
    doc: docs/reviews/network_review_2026_07_02.md
    reviewer: claude-code (network editorial pass)
    summary: >
      Full network editorial review — every transcript in the
      2026-06-18..07-02 window (~150 episodes, all 13 shows) scored for
      fit / interest / content / positioning. Four cross-cutting classes:
      (1) number/name normalization garbles in flagship positions
      (Tesla/SpaceX comma-number hooks — fixed); (2) guard/retry mechanisms
      degrading the audio they protect (MAB double-spoken closing;
      M&A/MAB/UC expansion-retry paraphrase padding — both fixed);
      (3) third-generation seeded-template convergence (OV/EI/MAB/FP/MIT/
      SpaceX — all prompt de-seeds PROPOSED under A/B, not applied);
      (4) same-day sibling overlap / beat encroachment (PT astronomy filter
      shipped — the review's highest-leverage fix; FF lookback + ephemeris;
      Tesla/SpaceX beat rules proposed). Plus scheduler anomalies: three
      same-day double-publishes + one silently missed SpaceX recap
      (operator forensics).
    shipped:
      - comma-number normalization (assets/pronunciation.py; all five
        shipped garble forms regression-tested)
      - closing-guard fuzzy signature match (engine/pipeline.py)
      - expansion-retry near-duplicate sentence stripping with
        fallback-to-original
      - Source-line scrub in the script-save path (spoken scaffold class)
      - EI Closing/Teaser-before-body chapter reorder; PT astronomy + FF
        ephemeris/lookback-7 + SpaceX junk-title fetch filters; M&A
        duplicate-feed removal + network no-duplicate-feeds guard
      - MIT voided-trade status + 37-trade migration + review-once guard
      - PR vocab no-reteach 8 / WOTD never-repeat / theme-gap enforcement
      - UC + FPD topic-queue hygiene (interleave, dedup, alternation)
      - review_snapshot.py Unicode-aware tic detector +
        missing-final-Closing chapter check (two tooling blind spots that
        let shipped defects pass three snapshots)
    deferred:
      - "all seeded-template de-seeds (the 12-show A/B list in the review
        doc) — PROPOSED, NOT applied (landmine #17; no GROK_API_KEY here to
        render before/after)"
      - "playbook meta-lesson (recorded, not yet applied to
        .claude/commands/review-show.md): never put a quotable example
        phrase in a rotation menu — three shows elected the first menu item
        as their next tic; de-seed by shape"
      - "operator: double-publish forensics (Tesla 6/23, PR 6/22, OV 6/19);
        SpaceX 6/28 missed recap; FP cadence alignment; M&A x_enabled
        mismatch vs CLAUDE.md"
    predictions:
      - metric: guard/retry-inflicted audio defects network-wide
          (double-spoken closings, verbatim-doubled sentences)
        baseline: "MAB 5/15 double closings; M&A Ep087 doubled sentences"
        expected: "0 across the next 2 weeks of episodes"
        verdict: partial
        evidence: "2026-07-18: the two fixed classes did not recur, but
          sibling classes shipped — SpaceX Ep034 whole-deep-dive spoken
          twice (final-script path, fixed 07-16, Ep035/036 clean) and MAB
          5/14 MISSING closings from two new causes (fixed 07-16; all four
          defective episodes re-parse clean with the current config)." 
      - metric: snapshot visibility of Cyrillic tics + missing-final-Closing
          chapter shapes
        baseline: "snapshot cleared FP while a template ran 7/8, and cleared
          EI while 3/5 shipped orphaned Closings"
        expected: "the next FP/EI snapshots surface these classes
          mechanically (no hand-reading required)"
        verdict: partial
        evidence: "2026-07-18: both checks landed and caught real defects
          (FP Cyrillic tics surfaced; EI Ep49-51 orphan Closings caught by
          the 07-13 review), but the Closing check was English-only until
          07-16 and false-flagged every FP episode in the interim." 
      - metric: same-day sibling story duplication (PT vs FF lead stories)
        baseline: "6 consecutive days of verbatim astronomy duplication"
        expected: "0 after the PT fetch filter"
        verdict: partial
        evidence: "2026-07-18: verbatim same-day duplication ended, but the
          astronomy class kept leaking via the unfiltered web-search route
          until the 07-16 fix (PT Ep116/119/120); PT clean since. FF's own
          ephemeris class remains open (3/10 recent episodes — reopened as
          a 07-18 finding)." 
    agent_cost_usd: null

  - date: 2026-07-18
    pr: null
    doc: docs/reviews/network_review_2026_07_18.md
    reviewer: claude-code (goals & differentiation audit + meta-review)
    summary: >
      Operator-requested whole-network audit: are goals met, is the product
      differentiated. Verdict: factory world-class (100% success, ~$35/mo,
      90 eps/wk, 5 languages), funnel nearly absent (3,070 downloads/30d,
      3 newsletter subs); differentiation is topical authority (Tesla+M&A+
      SpaceX=53% of downloads), the two most differentiated bets (DP Pod,
      Age of AI) are dark. Shipped: closing-is-final chapter invariant
      (Tesla Ep544 / MIT Ep104 promo-tail theft), DP Pod network-pick
      rotation memory, dashboard voice-baseline fix (stale ElevenLabs RU
      baseline = false-positive drift warnings), snapshot fetch-filter
      leakage counter, playbook category rules + ledger-discipline rule
      from the full 15-ledger meta-review. Verified: 07-16 denoise chain
      ENGAGED in shipped audio (re-denoise removes ~0 dB). P0 operator
      item: FP/PR publish off-schedule via the daily-audit retry path
      (three configs disagree on cadence).
    shipped:
      - "engine/chapters.py: where:end chapter is final — post-closing
        promo-tail marker theft dropped engine-wide (verified re-parse of
        Tesla Ep544 + MIT Ep104)"
      - "shows/hooks/dp_pod.py: _recent_network_picks rotation memory
        (FF was the pick 5/10 with a fixed host+frame)"
      - "scripts/generate_dashboard.py: RU voice baseline 0b875ae2 + ara
        sanctioned (kills every-build false drift warnings)"
      - "scripts/review_snapshot.py: fetch-filter leakage section
        (immediately scored 3 long-pending filter predictions)"
      - ".claude/commands/review-show.md: category rules (digest-only
        length levers, de-seed-by-shape+memory, no conditional
        predictions, two-miss escalation, rejection-signal bar for
        do_not_retry) + ledger-entry-required rule"
    deferred:
      - "P1-3 tic de-seeds (FP x2 100% saturation, MIT seeded transition,
        PT takeaway, PR secret opener) — A/B-gated proposals in the doc"
      - "P1-2 FF ephemeris digest-scope bullet — A/B-gated (title filters
        cannot hold the line; digest normalizes titles)"
      - "chronic under-length: OV 07-18 slot-depth realignment is the
        class-wide read; podcast-side levers now banned via do_not_retry"
      - "operator: RU cadence decision (P0-1), dp_pod feed submission +
        first real dispatch, FPD distribution gate, FPD/PR product
        decision, Age of AI launch, EI all-BC drift watch"
    predictions:
      - metric: chapters starting after the Closing chapter, network-wide
        baseline: "2 in the last 10 days (Tesla Ep544, MIT Ep104)"
        expected: "0 in the next 2 weeks (engine invariant)"
        verdict: hit
        evidence: "2026-07-31: 0 post-Closing chapters in any episode
          dated >= 07-18 (all shows' chapters_ep*.json scanned by date)"
      - metric: dp_pod Network pick concentration (single show share of
          last 10 picks)
        baseline: "5/10 Fascinating Frontiers incl. consecutive days"
        expected: "no show >3/10, no consecutive-day repeats"
        verdict: miss
        evidence: "2026-07-31: Planetterrian Daily was the pick 6/10 incl.
          SIX consecutive days (Ep019-024) with the rotation-memory ban
          text present in every prompt. Re-attacked data-side: recently
          picked shows are now excluded from the fresh-episode candidate
          list (shows/hooks/dp_pod.py, guarded)."
      - metric: dashboard voice-drift warnings on FP/PR/age_of_ai
        baseline: "3 false-positive warns on every build"
        expected: "0 (only real drift warns)"
        verdict: hit
        evidence: "2026-07-31: claude_md_drift_detected false, 0 show rows
          flagged in api/dashboard.json (built 07-30)"
      - metric: FF ephemeris/almanac items in shipped digests (snapshot
          leakage counter)
        baseline: "3 in last 10 episodes"
        expected: "0 within 2 weeks of the (A/B-gated) digest-scope bullet
          shipping; n/a-lever-not-shipped if it stays unapplied"
        verdict: miss
        evidence: "2026-07-31: still leaking (Ep147 Venus almanac hook came
          through a re-headlined column). Re-attacked at the deterministic
          fetch filter: 3 new title patterns in shows/
          fascinating_frontiers.yaml (zero real-news false drops verified)."

  - date: 2026-07-31
    pr: "https://github.com/Planetterrian/nerranetwork/pull/927"
    doc: docs/reviews/network_review_2026_07_31.md
    reviewer: claude-code (operator-directed prompt + LLM pass; 4 parallel cluster audits)
    summary: >
      Full network prompt + LLM review at operator request ("best shows
      possible"; optimize as Grok versions improve). P0: the July-30
      identity line was deleted network-wide by the junk-title-header
      stripper (fixed + guarded); DP Pod spoke "Source: Google News."
      and FP aired the Russian em-dash source label 13x (both scrub
      shapes added). Sweep de-seeded 11 confirmed tics by shape across
      10 shows, fixed cold-open integration contradictions, root-caused
      PR's re-teach cycle to vocab_tracker window bugs, gave EI the
      digest-side length lever, wired clean_digest (operator-approved),
      added llm.podcast_model for per-stage grok-4.5 trials (digests
      stay on grok-4.3 per its confident-hallucination profile), and
      upgraded the scheduled reviewer to grok-4.5.
    shipped:
      - identity-line exemption in the title-header stripper (P0, network-wide)
      - Source-scaffold scrub: outlet-name + Russian em-dash shapes
      - clean_digest wired into the podcast prompt (operator-approved; A/B)
      - llm.podcast_model per-stage override + guards; REVIEW_MODEL -> grok-4.5
      - 11 tic de-seeds by shape across 10 shows (A/B-listen list in PR #927)
      - FF ephemeris fetch-filter title patterns (deterministic)
      - vocab_tracker: no-reteach list uncapped + themes window = no-reteach window
      - env_intel digest-side length lever (min_digest_words 950), podcast retry off
      - dp_pod pick rotation enforced data-side (exclude recently picked shows)
      - UC/FPD queue pruning (3 fabrication-flag + 5 category-error briefs)
      - Age of AI: thesis name-dup, NARA garble path, honest auto-approve sign-off
    deferred:
      - FP digest-side length lever (verify true pre-format digest length first)
      - dp_pod Ep023-class closing containment (turn deleted + label swapped)
      - identity-line reinsertion guard (LLM-omission case)
      - MIT alpha-significance caveat: THIRD miss -> operator accept-or-abandon
      - dp_pod dispatches.json still empty (operator)
      - dead _shared includes (accuracy_rules, ai_transparency_note) — wire or delete
      - narrative-shaped outline prompt + dialogue-aware reviewer prompt
      - MIT fetch collapse (10-24 articles on 3 of last 4 days) — fetch-side pass
    predictions:
      - metric: spoken identity line present (show name in first 60s of transcript)
        baseline: "0% of episodes since 2026-07-30 (stripper deleted it network-wide)"
        expected: "100% of episodes over the next 2 weeks"
        verdict: pending
        evidence: null
      - metric: spoken source-attribution leaks in FP/DP transcripts
          (outlet-name + Russian em-dash shapes)
        baseline: "13 RU-dash airings across 6 of FP's last 10; DP Ep022 outlet form x2"
        expected: "0 in the next 10 episodes of each"
        verdict: pending
        evidence: null
      - metric: dp_pod Network pick concentration (single show share of last 10)
        baseline: "6/10 Planetterrian incl. 6 consecutive (data-side exclusion now live)"
        expected: "no show >3/10, no consecutive-day repeats"
        verdict: pending
        evidence: null
      - metric: PR words re-taught from within the 8-episode no-reteach window
        baseline: "5-7 words/ep re-taught from 7-8 episodes back (ep58/59/60/63)"
        expected: "0 re-teaches within the window over the next 6 episodes"
        verdict: pending
        evidence: null
      - metric: env_intel scripts below min_podcast_words 900
        baseline: "5 of last 10 below (digests 563-891w were the ceiling)"
        expected: "<=2 of the next 10 below, via the digest lever"
        verdict: pending
        evidence: null
    agent_cost_usd: null

  - date: '2026-08-16'
    pr: null
    doc: null
    generated_by: claude-code-session
    summary: 'Operator-directed build: story-recurrence memory (engine/story_recurrence.py) — inline update-don''t-retell notes on fetched articles matching the ContentTracker''s dated headline window, enabled on the 9 daily news shows; narrative show_memory expanded to env_intel (regulatory arcs) and omni_view (world-news arcs), the last two news-driven shows without it. Built after the Fort Bend story ran on 11 distinct days of Tesla''s current 15-episode window past three existing dedup layers.'
    scored_prior_predictions: []
    shipped:
    - file: engine/story_recurrence.py
      kind: code
      ab_listen_required: false
      rationale: 'Data-side, deterministic, no LLM calls, no new storage (reads the tracker''s window). Salient-token matching with an automatic common-token filter catches re-headlined variants the 0.72 title-similarity dedup missed; verified against the real Tesla tracker (all 5 shipped Fort Bend variants match, unrelated stories do not). Inline annotation in the article listing — the ContentTracker''s separate DO-NOT-REPEAT block demonstrably failed; the annotation instructs update-don''t-retell instead of banning (a real new development deserves coverage as an update).'
    - file: run_show.py
      kind: code
      ab_listen_required: false
      rationale: 'Annotation gated on the new story_recurrence YAML flag (9 news shows opted in; lesson/narrative shows deliberately off). Outcome metric story_recurrence_in_digest recorded per episode against a window that excludes today (FF Ep128 self-match class). Best-effort: any failure renders the legacy listing.'
    - file: engine/show_memory.py
      kind: code
      ab_listen_required: true
      rationale: 'env_intel + omni_view MemoryConfig entries seeded conservatively (arc descriptions + open questions, no speculative status claims). Memory-section injection changes prompt context for both shows -> A/B-listen per landmine #17 (same note as the July 24 MIT/DP Pod expansion).'
    deferred:
    - dp_pod + offshore_north have no ContentTracker section patterns, so story_recurrence would be a silent no-op there — give them tracker patterns first if their repetition ever bites.
    - The recurrence annotation is digest-side only; if a recurring story still gets RE-TOLD (not updated) in scripts after 2 weeks, the next lever is podcast-prompt-side framing (A/B) — not a data change.
    predictions:
    - metric: 'tesla: same-story full re-telling across >=3 episodes of a 10-episode review window (Fort Bend class)'
      baseline: Fort Bend ran 5 of 10 episodes (Ep565-573) re-told from scratch each time, and on 11 distinct days of the tracker window
      expected: 0 stories re-told from scratch across >=3 episodes in the next tesla review window; legitimate developments framed as updates are a PASS, verbatim re-telling is the fail condition
      verdict: pending
      evidence: null
    - metric: mean story_recurrence_annotated per episode on tesla+spacex (metrics_ep*.json)
      baseline: 'unmeasured (new metric); real-window probe: ~1-3 articles/episode expected to match'
      expected: metric present and nonzero within 3 post-merge episodes on both shows (proves the wiring fired); if 0 across 5 episodes, the flag or tracker wiring silently broke
      verdict: pending
      evidence: null
    - metric: env_intel + omni_view narrative trackers exist and auto-freshness advances
      baseline: no tracker files exist for either show
      expected: digests/<dir>/<slug>_narrative_tracker.json committed within 3 episodes of merge, last_mentioned_episode advancing; narrative pages render
      verdict: pending
      evidence: null
    agent_cost_usd: null
  - date: '2026-08-18'
    pr: null
    doc: docs/reviews/llm_usage_review_2026_08_18.md
    generated_by: claude-code-session
    summary: 'Operator-directed network LLM usage review against the August 2026 xAI product state (grok-4.6, Grok Voice July-6 update, Imagine video 1.5, the May-15 retirement, TTS GA pricing, per-call search billing). Verdict: no model upgrades warranted today — the dp_pod 4.5 / MIT 4.6 script A/Bs are the right probes and read out within two weeks. Found and fixed three silent-number/silent-drift failures: search accounting multiplied a $0.025 per-source rate by a sources field xAI stopped reporting (782 August calls untracked), the episode reviewer sat on a slug retired 05-15 (served by grok-4.3 at 4.3 billing, costed at the retired model''s 6x-cheaper rates), and the translation stage rode the floating grok-latest alias (vendor releases silently changed shipped dub audio; cost proxied at 4.3 rates). Dashboard cost rollup gained images+search categories (41 percent of tracked spend, previously invisible).'
    scored_prior_predictions: []
    shipped:
    - file: engine/tracking.py
      kind: code
      ab_listen_required: false
      rationale: 'Search billed per CALL ($0.005, env XAI_SEARCH_COST_PER_CALL) per 2026 xAI Agent Tools pricing; per-source term kept at zero-effect for any future source-count reporting. Retired grok-4-1-fast pricing rows annotated as historical-only.'
    - file: review_episodes.py
      kind: code
      ab_listen_required: false
      rationale: 'Reviewer pinned to grok-4.3 with explicit reasoning_effort none — exact parity with the retired-slug redirect that has served it since 05-15; nothing changes but the honesty of the model name and the cost.'
    - file: engine/translate.py
      kind: code
      ab_listen_required: true
      rationale: 'Translation pinned to grok-4.6 (what grok-latest resolves to today) with env override NERRA_TRANSLATION_MODEL; intended as behavior-freezing, but if the alias in fact still resolved to 4.5 this is a small translation change — listen-check the first post-merge dub track per landmine #17. engine/multilingual.py now prices the actual pinned model.'
    - file: scripts/generate_dashboard.py
      kind: code
      ab_listen_required: false
      rationale: 'cost_rollup breaks out images + search (additive keys); the two categories were 41 percent of tracked 30d spend and invisible.'
    deferred:
    - Digest/fetch stay on grok-4.3 — 4.6 factuality unproven for the confident-hallucination failure mode and 2.4x output cost; revisit only with the MIT readout plus a digest-side factuality probe.
    - Multilingual flagship-voice A/B on dub tracks — sequenced AFTER the language cull (FR decision 08-25, per-language OP3); do not invest in voice quality for languages that may be switched off.
    - Imagine image-quality tier — only if gallery-retention data shows image fidelity binds retention; would 2.5x the image category on no evidence today.
    predictions:
    - metric: search_api estimated_cost_usd in new credit files (search-fetching shows)
      baseline: 'token-only; 100 percent of 136 files since 07-29 had sources:0 and $0 tool fees'
      expected: nonzero per-call fee (calls x $0.005) present within 3 post-merge episodes on tesla/MIT/omni_view; network 30d search category on the dashboard rises to roughly $6-8/mo
      verdict: pending
      evidence: null
    - metric: reviewer + translation model ids recorded in configs/credit files
      baseline: retired slug grok-4-1-fast-non-reasoning in 3 config sites; grok-latest in translate.py
      expected: no retired slug or floating alias reachable from live config (tests/test_llm_usage_pass.py stays green); dub-track brand-garble rate does NOT rise after the 4.6 pin (translate restore_brand_names holds)
      verdict: pending
      evidence: null
    - metric: script-stage model A/B outcomes vs this review's no-upgrade verdict
      baseline: dp_pod 4.5 and MIT 4.6 reading; digests on 4.3
      expected: at least one of the two A/Bs resolves at its readout date with an explicit keep/revert in experiments.yaml, and NO digest/fetch stage moves off grok-4.3 before a measured factuality probe exists
      verdict: pending
      evidence: null
    agent_cost_usd: null

do_not_retry:
  - idea: podcast-side length levers (word floors, expand-retries, prompt
      pressure on the podcast stage) for ANY show
    evidence: "ledger aggregate 2026-07-18: ~10 misses vs 1 hit across
      Tesla/EI/OV/FF/PT/FP(D)/MAB/SpaceX; every miss re-diagnosed to the
      digest ceiling. Digest-substrate levers only (digest_expand,
      licensed-knowledge floors, full-text fetch)."
