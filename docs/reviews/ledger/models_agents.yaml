# Review ledger for models_agents. Schema: docs/reviews/ledger/README.md
reviews:
  - date: 2026-06-14
    pr: null
    doc: docs/reviews/models_agents_review_2026_06_14.md
    summary: >
      First dedicated Models & Agents pass (after the June 10 four-show
      pass). Two listener-facing metadata bugs, both no-A/B: phonetic
      garbles ("An-thropic"/"Lah-mah"/"Hah-sah-biss") shipping to TTS and
      chapter titles, and the Under the Hood chapter marker missing the
      "pop the hood" phrase the deep-dive actually uses (only 1/5 episodes
      got the chapter, firing auto-segment raw-sentence titles).
    shipped:
      - fix_phonetic_garbles += Anthropic/Llama/Hassabis (engine/utils.py, global)
      - Under the Hood chapter marker += "pop the hood" (shows/models_agents.yaml)
    deferred:
      - digest-driven / position-aware mid-section chapter titles (medium effort, shared; Ep078 residual)
      - expand Under the Hood section for length, after the four-show length A/B settles
      - >-
        "let's pop the hood on" opener tic — not changed (signature phrase,
        audio-change risk); revisit only with A/B evidence
    predictions:
      - metric: '"An-thropic" occurrences in last-10 _tts.txt (sum)'
        baseline: "94 (snapshot 2026-06-14; 6 in Ep080)"
        expected: "0 in episodes generated post-merge"
        verdict: pending
        evidence: null
      - metric: 'episodes with an "Under the Hood" chapter, last 5'
        baseline: "1/5 (only Ep078)"
        expected: ">= 4/5 post-merge (deep-dive present 9-10/10)"
        verdict: pending
        evidence: null
      - metric: 'episodes whose chapters include a raw mid-sentence title (ends "…" or >40 chars), last 5'
        baseline: "2/5 (Ep078, Ep080)"
        expected: "<= 1/5 post-merge (Ep078-class residual until digest-driven titles)"
        verdict: pending
        evidence: null
      - metric: median _tts.txt words, last 10 eps
        baseline: "~1290 (range 1085-1476; all below 1500 floor)"
        expected: "no regression; chronic under-length stays deferred to digest-expansion lever"
        verdict: hit
        evidence: "Median ~1300 (range 1067-1582); no regression. Digest-ceiling root cause unchanged."
    agent_cost_usd: null

  - date: 2026-06-21
    pr: null
    doc: docs/reviews/models_agents_review_2026_06_21.md
    summary: >
      Third M&A pass. All four June-14 predictions scored HIT. One recurring
      reader-facing text bug fixed: the shared pronunciation map respells
      CUDA -> "koo-dah" for TTS, and that respelling leaks into the published
      blog/RSS transcript (built from _tts.txt) — shipped in 12 episodes since
      Ep040 (23 occurrences). Same class as the original "nassa" leak that
      created fix_phonetic_garbles; restored the same way. Sharpened the
      deferred chapter diagnosis: chapter quality is inversely correlated with
      prompt compliance (full chapters only when the host announces banned
      section labels aloud), confirming digest-driven titles as the durable
      lever. Found a broader network-wide latent class (LoRA->"Laura",
      RAG->"rag", JAXA->"jacksa" etc. all leak to transcripts) — deferred.
    shipped:
      - 'fix_phonetic_garbles += "koo-dah" -> CUDA (engine/utils.py, global restore layer)'
    deferred:
      - digest-driven / position-aware chapter titles (carried; sharpened diagnosis — chapters only clean when host disobeys the no-spoken-labels prompt rule)
      - 'network-wide: pronunciation-map word-guides leak to published transcripts (LoRA/RAG/JAXA/CRISPR/PSYCHE/LOFAR/DART); collision-unsafe ones cannot use the restore dict — durable fix is sourcing the blog/RSS transcript from pre-pronunciation text or the Whisper transcript'
      - expand Under the Hood section for length, after the four-show length A/B settles (carried)
      - chronic under-length = digest ceiling, deferred behind four-show length A/B (carried)
      - '"keep an eye on" Tomorrow-Teaser opener tic (10/10) — optional A/B rotation menu, not changed'
    predictions:
      - metric: '"koo-dah" occurrences in published blog transcripts of post-merge CUDA episodes'
        baseline: "shipped in 12 episodes since Ep040 (23 total occurrences); live in Ep086 (2), Ep088 (2)"
        expected: "0 in any episode generated post-merge that mentions CUDA (CUDA appears instead)"
        verdict: hit
        evidence: "2026-07-02 network pass: 'koo-dah' absent from all post-merge
          transcripts; CUDA appears instead."
      - metric: 'custom voice still reads CUDA cleanly (Whisper of next post-merge CUDA episode)'
        baseline: 'current "koo-dah" audio reads as ~CUDA (Whisper Ep088: "a CUDA kernel")'
        expected: 'Whisper transcript shows "CUDA"; no "C U D A" letter-split (matches NASA/Anthropic precedent)'
        verdict: pending
        evidence: null
      - metric: median _tts.txt words, last 10 eps
        baseline: "~1300 (range 1067-1582; 9/10 below 1500 floor)"
        expected: "no regression; chronic under-length stays deferred to the four-show length A/B"
        verdict: hit
        evidence: "2026-07-02 network pass: median steady ~1300, no regression.
          Under-length stays deferred (digest ceiling)."
    agent_cost_usd: null

  - date: 2026-07-02
    pr: null
    doc: docs/reviews/network_review_2026_07_02.md
    reviewer: claude-code (network editorial pass)
    summary: >
      Network editorial pass. Scored June-21 (koo-dah HIT; length
      no-regression HIT). Findings: arXiv cs.CL was subscribed TWICE — the
      preprint flood crowded out the fortnight's two biggest builder stories
      (Codex Record Replay + Gemini Computer Use aired on the BEGINNER show,
      never here); the expand-below-target retry pads by
      paraphrase-duplication (Ep087 shipped verbatim doubled sentences);
      continuity-callback tic ("...tracked since episode eighty six" — the
      banned phrase reborn).
    shipped:
      - duplicate arXiv cs.CL feed removed + network-wide no-duplicate-feeds
        guard
      - expansion-retry near-duplicate sentence stripping with
        fallback-to-original (network-wide, engine-side)
    deferred:
      - "A/B proposals (NOT applied): selection rebalance (lab
        product/feature announcements outrank preprints; arXiv items <= 40%);
        continuity-callback cap (<= 1 spoken callback/episode, never episode
        numbers); recap must synthesize, not splice (Ep095 reused dailies'
        sentences verbatim; Ep088 proves the model can)"
      - digest-driven / position-aware chapter titles (carried)
      - chronic under-length = digest ceiling (carried; four-show length A/B)
    predictions:
      - metric: verbatim-doubled sentence pairs in shipped _tts.txt scripts
        baseline: "Ep087 shipped verbatim doubled sentences (retry padding)"
        expected: "0 post-merge (near-duplicate stripping in the retry)"
        verdict: pending
        evidence: null
    agent_cost_usd: null

do_not_retry: []
