# Review ledger for models_agents. Schema: docs/reviews/ledger/README.md
reviews:
  - date: 2026-06-14
    pr: null
    doc: docs/reviews/models_agents_review_2026_06_14.md
    summary: >
      First dedicated Models & Agents pass (after the June 10 four-show
      pass). Two listener-facing metadata bugs, both no-A/B: phonetic
      garbles ("An-thropic"/"Lah-mah"/"Hah-sah-biss") shipping to TTS and
      chapter titles, and the Under the Hood chapter marker missing the
      "pop the hood" phrase the deep-dive actually uses (only 1/5 episodes
      got the chapter, firing auto-segment raw-sentence titles).
    shipped:
      - fix_phonetic_garbles += Anthropic/Llama/Hassabis (engine/utils.py, global)
      - Under the Hood chapter marker += "pop the hood" (shows/models_agents.yaml)
    deferred:
      - digest-driven / position-aware mid-section chapter titles (medium effort, shared; Ep078 residual)
      - expand Under the Hood section for length, after the four-show length A/B settles
      - >-
        "let's pop the hood on" opener tic — not changed (signature phrase,
        audio-change risk); revisit only with A/B evidence
    predictions:
      - metric: '"An-thropic" occurrences in last-10 _tts.txt (sum)'
        baseline: "94 (snapshot 2026-06-14; 6 in Ep080)"
        expected: "0 in episodes generated post-merge"
        verdict: pending
        evidence: null
      - metric: 'episodes with an "Under the Hood" chapter, last 5'
        baseline: "1/5 (only Ep078)"
        expected: ">= 4/5 post-merge (deep-dive present 9-10/10)"
        verdict: pending
        evidence: null
      - metric: 'episodes whose chapters include a raw mid-sentence title (ends "…" or >40 chars), last 5'
        baseline: "2/5 (Ep078, Ep080)"
        expected: "<= 1/5 post-merge (Ep078-class residual until digest-driven titles)"
        verdict: pending
        evidence: null
      - metric: median _tts.txt words, last 10 eps
        baseline: "~1290 (range 1085-1476; all below 1500 floor)"
        expected: "no regression; chronic under-length stays deferred to digest-expansion lever"
        verdict: hit
        evidence: "Median ~1300 (range 1067-1582); no regression. Digest-ceiling root cause unchanged."
    agent_cost_usd: null

  - date: 2026-06-21
    pr: null
    doc: docs/reviews/models_agents_review_2026_06_21.md
    summary: >
      Third M&A pass. All four June-14 predictions scored HIT. One recurring
      reader-facing text bug fixed: the shared pronunciation map respells
      CUDA -> "koo-dah" for TTS, and that respelling leaks into the published
      blog/RSS transcript (built from _tts.txt) — shipped in 12 episodes since
      Ep040 (23 occurrences). Same class as the original "nassa" leak that
      created fix_phonetic_garbles; restored the same way. Sharpened the
      deferred chapter diagnosis: chapter quality is inversely correlated with
      prompt compliance (full chapters only when the host announces banned
      section labels aloud), confirming digest-driven titles as the durable
      lever. Found a broader network-wide latent class (LoRA->"Laura",
      RAG->"rag", JAXA->"jacksa" etc. all leak to transcripts) — deferred.
    shipped:
      - 'fix_phonetic_garbles += "koo-dah" -> CUDA (engine/utils.py, global restore layer)'
    deferred:
      - digest-driven / position-aware chapter titles (carried; sharpened diagnosis — chapters only clean when host disobeys the no-spoken-labels prompt rule)
      - 'network-wide: pronunciation-map word-guides leak to published transcripts (LoRA/RAG/JAXA/CRISPR/PSYCHE/LOFAR/DART); collision-unsafe ones cannot use the restore dict — durable fix is sourcing the blog/RSS transcript from pre-pronunciation text or the Whisper transcript'
      - expand Under the Hood section for length, after the four-show length A/B settles (carried)
      - chronic under-length = digest ceiling, deferred behind four-show length A/B (carried)
      - '"keep an eye on" Tomorrow-Teaser opener tic (10/10) — optional A/B rotation menu, not changed'
    predictions:
      - metric: '"koo-dah" occurrences in published blog transcripts of post-merge CUDA episodes'
        baseline: "shipped in 12 episodes since Ep040 (23 total occurrences); live in Ep086 (2), Ep088 (2)"
        expected: "0 in any episode generated post-merge that mentions CUDA (CUDA appears instead)"
        verdict: hit
        evidence: "2026-07-02 network pass: 'koo-dah' absent from all post-merge
          transcripts; CUDA appears instead."
      - metric: 'custom voice still reads CUDA cleanly (Whisper of next post-merge CUDA episode)'
        baseline: 'current "koo-dah" audio reads as ~CUDA (Whisper Ep088: "a CUDA kernel")'
        expected: 'Whisper transcript shows "CUDA"; no "C U D A" letter-split (matches NASA/Anthropic precedent)'
        verdict: pending
        evidence: null
      - metric: median _tts.txt words, last 10 eps
        baseline: "~1300 (range 1067-1582; 9/10 below 1500 floor)"
        expected: "no regression; chronic under-length stays deferred to the four-show length A/B"
        verdict: hit
        evidence: "2026-07-02 network pass: median steady ~1300, no regression.
          Under-length stays deferred (digest ceiling)."
    agent_cost_usd: null

  - date: 2026-07-02
    pr: null
    doc: docs/reviews/network_review_2026_07_02.md
    reviewer: claude-code (network editorial pass)
    summary: >
      Network editorial pass. Scored June-21 (koo-dah HIT; length
      no-regression HIT). Findings: arXiv cs.CL was subscribed TWICE — the
      preprint flood crowded out the fortnight's two biggest builder stories
      (Codex Record Replay + Gemini Computer Use aired on the BEGINNER show,
      never here); the expand-below-target retry pads by
      paraphrase-duplication (Ep087 shipped verbatim doubled sentences);
      continuity-callback tic ("...tracked since episode eighty six" — the
      banned phrase reborn).
    shipped:
      - duplicate arXiv cs.CL feed removed + network-wide no-duplicate-feeds
        guard
      - expansion-retry near-duplicate sentence stripping with
        fallback-to-original (network-wide, engine-side)
    deferred:
      - "A/B proposals (NOT applied): selection rebalance (lab
        product/feature announcements outrank preprints; arXiv items <= 40%);
        continuity-callback cap (<= 1 spoken callback/episode, never episode
        numbers); recap must synthesize, not splice (Ep095 reused dailies'
        sentences verbatim; Ep088 proves the model can)"
      - digest-driven / position-aware chapter titles (carried)
      - chronic under-length = digest ceiling (carried; four-show length A/B)
    predictions:
      - metric: verbatim-doubled sentence pairs in shipped _tts.txt scripts
        baseline: "Ep087 shipped verbatim doubled sentences (retry padding)"
        expected: "0 post-merge (near-duplicate stripping in the retry)"
        verdict: pending
        evidence: null
    agent_cost_usd: null

  - date: '2026-07-19'
    pr: null
    doc: docs/reviews/models_agents_review_2026_07_19.md
    generated_by: grok-4.3
    summary: This pass finds persistent under-length (9/10 recent episodes below 1500w floor) and recurring boilerplate tics ("keep an eye on", "Okay let's pop the hood on", network outro blocks) across transcripts, with all prior length predictions scoring hit and chapter/garble fixes remaining stable.
    scored_prior_predictions:
    - metric: '"An-thropic" occurrences in last-10 _tts.txt (sum)'
      verdict: hit
      evidence: 0 in ep107–ep116 transcripts
    - metric: episodes with an "Under the Hood" chapter, last 5
      verdict: hit
      evidence: 4/5 (ep108/110/112/114)
    - metric: episodes whose chapters include a raw mid-sentence title (ends "…" or >40 chars), last 5
      verdict: hit
      evidence: 0/5 in latest batch
    - metric: median _tts.txt words, last 10 eps
      verdict: hit
      evidence: ~1300 steady; no regression
    - metric: '"koo-dah" occurrences in published blog transcripts of post-merge CUDA episodes'
      verdict: hit
      evidence: 0 post-merge (ep107+)
    - metric: custom voice still reads CUDA cleanly (Whisper of next post-merge CUDA episode)
      verdict: hit
      evidence: Whisper shows "CUDA" (no letter-split)
    - metric: verbatim-doubled sentence pairs in shipped _tts.txt scripts
      verdict: partial
      evidence: ep115/116 clean; prior Ep087 fixed
    shipped: []
    deferred:
    - Digest-driven / position-aware mid-section chapter titles (carried)
    - Expand Under the Hood section for length after four-show A/B settles (carried)
    - Chronic under-length = digest ceiling (carried)
    predictions:
    - metric: episodes below min_podcast_words (1500) in last 10
      baseline: 9/10
      expected: <=3/10 after digest min_words raise
      verdict: pending
      evidence: null
    - metric: '"keep an eye on" occurrences as Tomorrow Teaser opener, last 10'
      baseline: 10/10
      expected: <=2/10 after shape-based de-seed
      verdict: pending
      evidence: null
    proposed_changes_in_pr:
    - file: shows/models_agents.yaml
      kind: config
      ab_listen_required: false
      rationale: Direct lever per length meta-rule; targets digest ceiling without podcast-side changes
    - file: shows/prompts/models_agents_digest.txt
      kind: prompt
      ab_listen_required: true
      rationale: Shape-based de-seed of 10/10 tic; avoids quotable example per meta-rule
    agent_cost_usd: 0.0591
  - date: '2026-08-02'
    pr: null
    doc: docs/reviews/models_agents_review_2026_08_02.md
    generated_by: grok-4.5
    summary: Prior length and teaser-tic predictions both missed (8/10 scripts still under 1500w; “keep an eye on” still ~8/10); chapters are not actually clean (sparse/auto-titles, ep127 first marker at 4min, ep128 missing Under the Hood when the host skipped “pop the hood”), and continuity still speaks episode numbers—propose digest floor raise, shape-ban teaser de-seed, and episode-number ban only.
    scored_prior_predictions:
    - metric: episodes below min_podcast_words (1500) in last 10
      verdict: miss
      evidence: 8/10 ep120–129 still under 1500w (only ep126=1592, ep129=1501); min_digest_words=1300 + digest_expand already live
    - metric: '"keep an eye on" occurrences as Tomorrow Teaser opener, last 10'
      verdict: miss
      evidence: Still ~8/10 teasers (ep120–127); July-19 shape de-seed never applied (shipped:[])
    - metric: verbatim-doubled sentence pairs in shipped _tts.txt scripts
      verdict: hit
      evidence: No paraphrase-doubled sentence pairs in ep120–129 transcripts after expansion-strip
    - metric: custom voice still reads CUDA cleanly (Whisper of post-merge CUDA episode)
      verdict: hit
      evidence: No koo-dah/letter-split in ep120–129; prior July-19 hit stands
    shipped: []
    deferred:
    - Digest-driven / position-aware mid-section chapter titles (carried; ep120/121 ellipsis auto-titles, ep127 first chapter at 240s, ep128 lost Under the Hood when host skipped pop-the-hood — markers remain brittle)
    - Under the Hood licensed-knowledge sentence/fact floor in digest prompt — operator option B if min_digest_words=1600 still leaves >=7/10 podcast scripts under 1500
    - Accept ~1200–1400w natural length and lower min_podcast_words (operator option C) so health dashboards stop false-alarming
    - Signature opener "okay let's pop the hood on" (8/10) — intentional chapter signal; revisit only with A/B evidence
    - 'Network-wide: blog/RSS transcript sourced from pre-pronunciation text or Whisper (LoRA/RAG/JAXA class leaks) — collision-unsafe for lone-token restore'
    - July-2 selection rebalance A/B (lab product/feature announcements outrank preprints; arXiv items <= 40%) — still not applied
    - Recap must synthesize, not splice dailies' sentences (Sunday weekly_summary_segment) — carried
    - OP3 7d drop 173→88 — watch one more cycle before product changes; no metadata root cause found this pass
    predictions:
    - metric: episodes below min_podcast_words (1500) in last 10
      baseline: 8/10 (ep120–129)
      expected: <=4/10 in episodes generated after min_digest_words=1600 merge (digest lever only; if still >=7/10, escalate to UTH floor or accept-lower-floor — do not re-raise podcast expand)
      verdict: pending
      evidence: null
    - metric: '"keep an eye on" / "Tomorrow keep an eye on" / "Before we go, keep an eye on" as teaser opener, last 10'
      baseline: ~8/10
      expected: <=2/10 post de-seed merge
      verdict: pending
      evidence: null
    - metric: 'successor teaser-opener tic: any single new opener phrase in >=6/10 teasers (convergence watch)'
      baseline: n/a (watch)
      expected: 0 phrases at >=6/10; if a replacement converges, ban by shape next pass
      verdict: pending
      evidence: null
    - metric: spoken episode-number continuity callbacks ("episode N" / "EpN") in last 10 Whisper/_tts.txt
      baseline: '>=1 (ep120 "episode 119"; pattern live)'
      expected: 0 in post-merge episodes
      verdict: pending
      evidence: null
    proposed_changes_in_pr:
    - file: shows/models_agents.yaml
      kind: config
      ab_listen_required: false
      rationale: July-19 length prediction missed with 1300+expand still yielding 8/10 podcast scripts under 1500. Meta-review allows digest-substrate levers only; this is the next step before an Under-the-Hood floor or accepting a lower podcast floor. Do not touch podcast_expand_below_target.
    - file: shows/prompts/models_agents_podcast.txt
      kind: prompt
      ab_listen_required: true
      rationale: De-seed the 8/10 teaser tic by shape + verbatim ban with no quotable replacement (meta-rule). Same edit hard-bans spoken episode-number callbacks seen in ep120. A/B-listen required.
    - file: tests/test_models_agents_quality_pass.py
      kind: code
      ab_listen_required: false
      rationale: Drift-guard the config floor and the two prompt bans so a later edit cannot silently drop them (pattern from test_models_agents_quality_pass.py / network quality passes).
    agent_cost_usd: 0.1151
  - date: '2026-08-04'
    pr: null
    doc: docs/reviews/models_agents_review_2026_08_04.md
    generated_by: grok-4.5
    summary: Prior length and teaser-tic predictions missed again because Aug-2 levers never shipped (7/10 scripts still under 1500w; “keep an eye on” still 6/10); chapters remain brittle (ep127 first marker at 4min, ep128 lost Under the Hood); new UTH body tic “Everyone talks about…” is 8/10 — escalate length to an operator decision and re-propose only the never-applied teaser/UTH de-seeds plus a no-A/B chapter-marker catch.
    scored_prior_predictions:
    - metric: episodes below min_podcast_words (1500) in last 10
      verdict: miss
      evidence: 7/10 ep122–131 still under 1500 (worst ep127=1099, ep124=1135); min_digest_words still 1300 — Aug-2 1600 lever never applied
    - metric: '"keep an eye on" / "Tomorrow keep an eye on" / "Before we go, keep an eye on" as teaser opener, last 10'
      verdict: miss
      evidence: 6/10 still (ep122–127 verbatim); ep128–131 drifted to Before-we-go without the phrase; Aug-2 shape de-seed never applied
    - metric: 'successor teaser-opener tic: any single new opener phrase in >=6/10 teasers (convergence watch)'
      verdict: hit
      evidence: '"Before we go" at 5/10 (ep126,128–131) — under the 6/10 threshold; watch next pass'
    - metric: spoken episode-number continuity callbacks ("episode N" / "EpN") in last 10 Whisper/_tts.txt
      verdict: hit
      evidence: 0 episode-number callbacks in ep122–131; framing is yesterday/prior-episode/tracked-arc instead
    shipped: []
    deferred:
    - 'OPERATOR DECISION (escalated after two missed length predictions — July-19 digest-min raise miss + Aug-2 min_digest_words=1600 miss; do not re-file 1600 a third time without an explicit choice): (A) raise min_digest_words to 1600, (B) Under-the-Hood licensed-knowledge sentence/fact floor in digest prompt, or (C) accept ~1200–1400w natural length and lower min_podcast_words. Never re-enable podcast_expand_below_target.'
    - Digest-driven / position-aware mid-section chapter titles (carried; ep125/130 ellipsis auto-titles, ep127 first chapter at 240s, ep128 lost UTH before marker patch — markers remain brittle)
    - Under the Hood licensed-knowledge sentence/fact floor in digest prompt — operator option B
    - Accept ~1200–1400w natural length and lower min_podcast_words — operator option C
    - Signature opener "okay let's pop the hood on" (8/10) — intentional chapter signal; revisit only with A/B evidence
    - 'Network-wide: blog/RSS transcript sourced from pre-pronunciation text or Whisper (LoRA/RAG/JAXA class leaks) — collision-unsafe for lone-token restore'
    - July-2 selection rebalance A/B (lab product/feature announcements outrank preprints; arXiv items <= 40%) — still not applied
    - Recap must synthesize, not splice dailies' sentences (Sunday weekly_summary_segment) — carried
    - OP3 7d 105 vs prior ~170 plateau — recovering from Aug-2 88 trough; watch one more cycle before product changes
    predictions:
    - metric: '"keep an eye on" as Tomorrow Teaser / before-we-go opener, last 10 post-merge'
      baseline: 6/10 (ep122–127)
      expected: <=2/10 in episodes generated after teaser shape-ban merges
      verdict: pending
      evidence: null
    - metric: '"Everyone talks about" as Under-the-Hood / deep-dive opener, last 10 post-merge'
      baseline: 8/10 (ep124–131)
      expected: <=2/10 after UTH shape-ban merges
      verdict: pending
      evidence: null
    - metric: 'successor UTH-opener tic: any single new contrast-cliché opener in >=6/10 deep-dives (convergence watch)'
      baseline: n/a (watch)
      expected: 0 phrases at >=6/10; if a replacement converges (e.g. "Most teams still…", "The common story is…"), ban by shape next pass
      verdict: pending
      evidence: null
    - metric: 'successor teaser-opener tic: "Before we go" (with or without keep-an-eye) in >=6/10 teasers'
      baseline: 5/10 (ep126,128–131)
      expected: <=5/10 sustained; if it hits >=6/10, shape-ban next pass
      verdict: pending
      evidence: null
    - metric: episodes with an Under the Hood chapter when a deep-dive is present, last 10
      baseline: 8/10 (ep128 missing UTH chapter despite deep-dive; ep127 sparse/no UTH marker)
      expected: '>=9/10 after section_markers += everyone talks about (ep128-class caught)'
      verdict: pending
      evidence: null
    - metric: spoken episode-number continuity callbacks in last 10
      baseline: 0 (ep122–131)
      expected: 0 sustained after explicit podcast-side ban
      verdict: pending
      evidence: null
    proposed_changes_in_pr:
    - file: shows/prompts/models_agents_podcast.txt
      kind: prompt
      ab_listen_required: true
      rationale: '"keep an eye on" still opens 6/10 teasers (ep122–127); July-19 and Aug-2 shape de-seeds never applied. Meta-rule: de-seed by shape + verbatim ban, no quotable replacement menu; successor "Before we go" already at 5/10 is on the watch list.'
    - file: shows/prompts/models_agents_podcast.txt
      kind: prompt
      ab_listen_required: true
      rationale: 'New third-generation tic: "Everyone talks about…" is the UTH body opener in 8/10 recent episodes and is also why ep127/128 lost the pop-the-hood chapter signal. Shape-ban with no replacement menu; keep signature pop-the-hood alone.'
    - file: shows/prompts/models_agents_podcast.txt
      kind: prompt
      ab_listen_required: true
      rationale: ep122–131 already hit 0 episode-number callbacks, but the Aug-2 podcast-side ban never landed. Belt-and-suspenders so the ep120 regression cannot return; matches digest continuity budget.
    - file: shows/models_agents.yaml
      kind: config
      ab_listen_required: false
      rationale: ep128 deep-dive opened "Everyone talks about MCP…" with no pop-the-hood → zero UTH chapter. ep127 same shape. Adding the live formula phrase to section_markers restores the chapter without changing audio. Does not replace deferred digest-driven titles.
    - file: tests/test_models_agents_quality_pass.py
      kind: code
      ab_listen_required: false
      rationale: Drift-guard the two prompt bans, the UTH marker alternate, and the no-podcast-expand / digest-floor floor so a later edit cannot silently drop them (pattern from existing quality-pass tests).
    agent_cost_usd: 0.1256
do_not_retry: []
