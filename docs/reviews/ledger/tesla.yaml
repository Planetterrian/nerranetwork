# Review ledger for tesla — see docs/reviews/ledger/README.md for schema.
# Seeded from the June 2026 manual passes so the agent's first Tesla review
# has predictions to score.
reviews:
  - date: 2026-06-10
    pr: 576
    doc: docs/tesla_review_2026_06_10.md
    summary: Second June pass — chapters, closing rotation, expansion retry
      with full digest, unified length target, live performance loop.
    shipped:
      - chapter positional constraints + once-per-title matching
      - closing rotates 4 variants, omits price sentence when quote invalid
      - expansion retry carries the full digest
      - single unified length target (2200-2400 words, min 2000)
      - performance loop live (OP3 -> strong_topics_last_30d)
      - theme mining filters narrative echo; word-boundary program detection
      - content-tracker junk/slur title filter + one-time scrub
    deferred:
      - digest-driven chapter titles (fuzzy-match Top-12 story headlines)
    predictions:
      - metric: episodes with trailing duplicate "Introduction" chapter
        baseline: "10 of 10 (Ep496-505)"
        expected: "0 of 10 on next review"
        verdict: hit
        evidence: "chapters_ep507-516.json — no duplicate/trailing
          Introduction on any of the last 10 episodes."
      - metric: median _tts.txt words, last 10 eps
        baseline: "~15-35% under the old 1600 target"
        expected: ">= 2000 words consistently"
        verdict: miss
        evidence: "Ep507-516 run 1254-1676w (median ~1440); every episode
          below the 2000 floor. Re-diagnosed: the digest itself is the
          ceiling (1234-1800w, script tracks ~1:1), Top-12 items ship at 3
          thin sentences from Google-News snippets. Same root cause as
          FF/UC/PT — deferred behind the four-show length A/B with the
          digest-expansion / First-Principles-expansion levers named."

  - date: 2026-06-20
    pr: null
    doc: docs/reviews/tesla_review_2026_06_20.md
    summary: Second agent pass — scored June 10 (chapters hit, length miss),
      fixed the Teslarati spoken garble, the "Daily" brand normalizer, and
      13F filing-spam at fetch time; re-diagnosed under-length as the digest
      ceiling and documented the chapter body-nav regression.
    shipped:
      - "fix_phonetic_garbles: Tesla-rah-tee -> Teslarati (25+ eps, voiced garble)"
      - "brand normalizer drops stray 'Daily' (spoken intro said it 100% of eps)"
      - "exclude_title_patterns: drop 13F institutional-filing spam at fetch (no-A/B)"
    deferred:
      - "chronic under-length: digest ceiling (Top-12 3-sentence snippet items);
        levers = digest-expansion retry OR First-Principles essay expansion;
        held behind the four-show length A/B (FF/UC/PT same root cause)"
      - "chapter body navigation: fragment auto-segment titles (8/10) AND the
        no-body-chapter regression (2/10, Ep515/516 — chapters.py:208 trigger
        doesn't match its docstring's 'head spans most of script' condition).
        Fix = digest-driven chapter titles; shared engine/chapters.py, route
        through the network review."
      - "digest-driven chapter titles (carried from June 10 — still the lever
        for the fragment-title class)"
    predictions:
      - metric: '"Tesla-rah-tee" occurrences in last-10 _tts.txt scripts'
        baseline: "5 of last 10 episodes (Ep500/505/512/516 + more)"
        expected: "0 of 10 — every instance restored to 'Teslarati'"
        verdict: hit
        evidence: "2026-07-02 network pass: zero 'Tesla-rah-tee' in any
          window _tts.txt (2026-06-18..07-02)."
      - metric: episodes whose spoken intro says "Tesla Shorts Time Daily"
        baseline: "10 of 10 (Ep506-516)"
        expected: "0 of 10 — normalizer drops the stray 'Daily'"
        verdict: miss
        evidence: "Still spoken in 14/14 window episodes. Root-caused
          2026-07-02: a SECOND normalizer in run_show.py
          (_normalize_tst_opening) kept rewriting TOWARD the old brand,
          undoing the June-20 generator fix. Fixed in the network pass."
      - metric: 13F institutional-filing items in the digest
        baseline: "up to 3/episode on thin days (Ep516)"
        expected: "0 — filtered at fetch via exclude_title_patterns"
        verdict: hit
        evidence: "2026-07-02 network pass: no 13F institutional-filing items
          in any window digest; fetch filter holding."
    agent_cost_usd: null

  - date: 2026-07-02
    pr: null
    doc: docs/reviews/network_review_2026_07_02.md
    reviewer: claude-code (network editorial pass)
    summary: >
      Network editorial pass (all 13 shows). Scored June-20 (Teslarati HIT,
      13F HIT, brand-"Daily" MISS — root-caused to a SECOND normalizer).
      Findings: comma-blind number formatting shipped hook garbles
      ("fifty-nine dollars,990", "1,zeroth") in 4 episodes, twice in the
      HOOK; spoken brand still "Tesla Shorts Time Daily" 14/14; SpaceX
      corporate-finance drift (Ep518's first two minutes were "a worse
      SpaceX Daily"); weak recaps (Ep517 covered the Dojo patent 3x, read
      raw headlines + "EtherType 0x9AC6" hex on air); June-23 double-publish
      Ep519+Ep520 (operator item).
    shipped:
      - "comma-number normalization fix (assets/pronunciation.py) — kills the
        'dollars,990' / '1,zeroth' hook-garble class; regression tests for
        all five shipped forms"
      - "second brand normalizer in run_show.py (_normalize_tst_opening)
        fixed — it kept rewriting TOWARD 'Tesla Shorts Time Daily', undoing
        the June-20 generator fix; completes the approved June-10 decision
        (quick A/B-listen advised)"
    deferred:
      - "chronic under-length: digest ceiling (carried; four-show length A/B)"
      - digest-driven chapter titles (carried, shared network lever)
      - "A/B proposals (NOT applied): recap hardening (one story once, no
        Title-Case headlines or patent/hex minutiae, transition variety);
        ban standalone dateline-filler sentences; require named entities;
        counterpoint-section enforcement (5/14 misses); SpaceX/xAI corporate
        items are SpaceX Daily's beat — never Tesla's hook/lead"
      - "operator: June-23 double-publish forensics; narrative-tracker
        curated status texts lag the show's own reporting by months
        (scripts/update_tesla_narrative.py)"
    predictions:
      - metric: episodes whose spoken intro says "Tesla Shorts Time Daily"
        baseline: "14 of 14 (window 2026-06-18..07-02)"
        expected: "0 of next 10 — the second normalizer no longer re-adds it"
        verdict: pending
        evidence: null
      - metric: comma-number hook garbles ("dollars,990"-class) in new _tts.txt
        baseline: "4 episodes in the window, twice in the HOOK"
        expected: "0 post-merge (deterministic formatter fix)"
        verdict: pending
        evidence: null
    agent_cost_usd: null

  - date: '2026-08-01'
    pr: null
    doc: docs/reviews/tesla_review_2026_08_01.md
    generated_by: grok-4.5
    summary: July-2 brand/comma fixes held, but 6/10 episodes still speak the malformed closer “TSLA closed at down…”, every recent script remains under the 2000-word floor despite digest-side expand, and the tomorrow-teaser/CTA pools have collapsed into single tics.
    scored_prior_predictions:
    - metric: episodes whose spoken intro says "Tesla Shorts Time Daily"
      verdict: hit
      evidence: Ep550-559 snapshot phrase list has identity as "i'm patrick in vancouver"; no "Daily" / "Tesla Shorts Time Daily" recurrence in last 10.
    - metric: comma-number hook garbles ("dollars,990"-class) in new _tts.txt
      verdict: hit
      evidence: No dollars,NNN / zeroth / comma-number class in last-10 snapshot phrases; formatter fix holding.
    shipped: []
    deferred:
    - 'Operator length decision (ESCALATE — third miss): (a) full-text fetch for primary Tesla feeds, (b) accept grok-4.3 plateau and lower min_podcast_words + RSS ''15 focused minutes'' to match ~11-12 min reality, or (c) hard First Principles word floor on the digest. Do not re-enable podcast_expand_below_target.'
    - Digest-driven chapter titles + engine/chapters.py long-Introduction auto-split when editorial markers alone hit min_chapters (network-scoped; June-10/20/July-2 carry).
    - Narrative-tracker curated status lag — scripts/update_tesla_narrative.py operator curation.
    - 'July-2 A/B not applied: recap hardening (one story once; no Title-Case headline readbacks / patent-hex minutiae); dateline-filler ban; counterpoint enforcement; SpaceX/xAI corporate never as Tesla hook/lead.'
    - June-23 double-publish forensics (operator).
    - RSS listing minute-count copy — only after operator length decision so store text is not edited twice.
    predictions:
    - metric: episodes whose spoken closer matches /closed at (up|down)/i in last-10 Whisper transcripts or _tts.txt
      baseline: 6/10 (snapshot phrase "tsla closed at down")
      expected: 0/10 after _pick_closing harden merges
      verdict: pending
      evidence: null
    - metric: episodes containing verbatim teaser opener "keep an eye on" in last-10 transcripts
      baseline: 8/10
      expected: ≤2/10; successor tic (if any) named in next ledger score
      verdict: pending
      evidence: null
    - metric: single CTA bigram (e.g. "rating or review") frequency in last-10 closers
      baseline: 6/10 "a rating or review"
      expected: ≤3/10 for any one CTA bigram after closer-tail rotation
      verdict: pending
      evidence: null
    proposed_changes_in_pr:
    - file: shows/hooks/tesla.py
      kind: code
      ab_listen_required: true
      rationale: 'Snapshot: ''tsla closed at down'' in 6/10 shipped transcripts — listener-facing garble in the final seconds of the show. Same function also explains the 6/10 ''a rating or review'' CTA collapse.'
    - file: shows/prompts/tesla_podcast.txt
      kind: prompt
      ab_listen_required: true
      rationale: 8/10 episodes open the teaser with the same 'keep an eye on' tic — dead rotation. De-seed by shape + verbatim ban per playbook (no quotable exemplar that becomes the next tic).
    - file: tests/test_tesla_quality_pass.py
      kind: code
      ab_listen_required: false
      rationale: Drift-guard pattern from prior Tesla quality passes; locks P0 closer fix and CTA rotation without touching audio settings.
    agent_cost_usd: 0.0902
  - date: '2026-08-15'
    pr: null
    doc: docs/reviews/tesla_review_2026_08_15.md
    generated_by: claude-code-session
    summary: Operator-directed flagship pass. The "closed at up" closer tic of the last two reviews was a snapshot-tokenizer artifact (digit-blind) — closer verified well-formed; tokenizer fixed. Shipped the two verified P0s (13F/analyst fetch-filter gaps incl. the X-post bypass that cold-opened Ep566 on fund spam; Ep565's 417-second First Principles chapter mislabel) plus the empty tesla-narrative.html title and RU channel-description translation leaks.
    scored_prior_predictions:
    - metric: episodes whose spoken closer matches /closed at (up|down)/i in last-10 Whisper transcripts or _tts.txt
      verdict: n/a-invalid-baseline
      evidence: 'Snapshot tokenizer dropped digits, so "closed at $328.58, up $9.05" tokenized to "closed at up" — the closer was never broken (verified in _tts.txt + Whisper both windows). Tokenizer fixed (numbers -> 0 placeholder) so the phantom cannot be re-filed.'
    - metric: episodes containing verbatim teaser opener "keep an eye on" in last-10 transcripts
      verdict: hit
      evidence: 0/10 Ep564-573 — but the de-seed mandated "Before we go" verbatim, manufacturing a 10/10 successor with a "should/could clarify" sub-template in 6/10.
    - metric: single CTA bigram (e.g. "rating or review") frequency in last-10 closers
      verdict: n/a-lever-not-shipped
      evidence: closer-tail rotation never merged (hooks/tesla.py unchanged); "rating or review" 4/10.
    shipped:
    - file: shows/tesla.yaml
      kind: config
      ab_listen_required: false
      rationale: 'Fetch-filter widen: noun-first 13F spam ("Boosts Tesla Stake", "Buys 4,143 Shares of Tesla") + aggregator "consensus recommendation" now excluded; named-analyst calls with anchors still pass. Chapter hygiene: bare First Principles marker removed (1/26 fire rate, the one fire was Ep565''s 417s news-body mislabel); banned "keep an eye on" dropped from teaser marker.'
    - file: run_show.py
      kind: code
      ab_listen_required: false
      rationale: X posts merged after both filter call sites bypassed exclude_title_patterns entirely — both TSLAming (X) leaks (Ep566 cold-open, Ep573) came through that channel. Filter now applied at the X-merge site.
    - file: generate_html.py
      kind: code
      ab_listen_required: false
      rationale: tesla-narrative.html shipped <title></title> — base template renders a page_title VARIABLE the bespoke Tesla generator never passed ({% block title %} is dead markup). Page regenerated.
    - file: scripts/review_snapshot.py
      kind: code
      ab_listen_required: false
      rationale: Digit-blind tokenizer produced the phantom closer tic two reviews chased; numbers now tokenize as a placeholder so only wording-invariant phrases register as tics.
    - file: digests/tesla_shorts_time/channel_i18n.json
      kind: data
      ab_listen_required: false
      rationale: RU channel description carried untranslated "buzzing"/"bearish"/"recap"; fixed in the rebuild cache + live podcast.ru.rss.
    deferred:
    - 'OPERATOR DECISION (length, 4th consecutive 10/10-below window; digest floor itself missed 3/10): (a) raise digest substrate via full-text fetch, or (b) accept ~1250-1700w, lower min_podcast_words, and fix the "15 focused minutes" RSS copy (real durations 8:56-12:17). No podcast-side lever (do_not_retry).'
    - Teaser successor de-seed (ban the "should/could clarify" reveal shape + different-story-than-yesterday rule; keep the "Before we go" chapter anchor) — A/B-listen.
    - Named-entity recurrence memory for story repetition (Fort Bend story ran 5/10 episodes and 4x inside Ep573; lookback_days 2 cannot see multi-day recurrence) — data-side, DP Pod lever-memory pattern.
    - Implication-filler enforcement (89 "The move/This change…" openers per 10 eps vs an unenforced prompt ban) — A/B.
    - Narrative tracker curation ~85 days stale (newest last_major_update Ep482; 6 programs never curated; status text contradicts on-air facts) — operator task via scripts/update_tesla_narrative.py.
    - Tesla RU funnel (19,353 RU Short views resolving to the English tesla.html with no capture path) — add funnel block + RU lander after the RU SpaceX pilot click-through reads out.
    - Weekend closer "Friday''s close" qualifier — small A/B.
    predictions:
    - metric: excluded-class 13F/fund/consensus items in last-10 tesla digests (incl. X-sourced)
      baseline: 4 in Ep564-573 (Ep566 hook, Ep569 x2, Ep573)
      expected: 0 in the next 10 episodes after the filter widen + X-merge pass
      verdict: pending
      evidence: null
    - metric: chapters files with a First Principles chapter mislabeling news-body content
      baseline: 'Ep565: 417s chapter spanning the whole news body'
      expected: 0 in the next 10 episodes (marker removed; body titled by digest headlines)
      verdict: pending
      evidence: null
    - metric: 'successor teaser tic watch: "should clarify|could clarify" reveal in last-10 teasers'
      baseline: 6/10 (Ep564-573)
      expected: scored next pass; if >=6/10 persists, ship the shape de-seed (A/B) rather than re-observing
      verdict: pending
      evidence: null
    agent_cost_usd: null
do_not_retry:
  - idea: phonetic respellings or programmatic speech-tag injection
    evidence: 100% regression rate on custom voice kdif6sqjcyiq (May 2026,
      landmine 17); only revisit with 3-context A/B listen evidence.
