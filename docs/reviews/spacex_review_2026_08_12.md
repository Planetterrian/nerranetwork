# spacex — quality review (2026-08-12)

_Generated by the Grok-4.3 Show Review Agent (`scripts/run_show_review.py`). Proposed prompt/audio changes are listed in the PR body under "A/B-listen required" and are NOT applied — the operator applies and listens before merging (landmine #17)._

## Evidence

Snapshot (`scripts/review_snapshot.py spacex`) on Ep58–67 `_tts.txt` + Whisper transcripts + `chapters_ep058.json`–`chapters_ep067.json` + `shows/spacex.yaml` (llm/chapters/deep_dive) + `shows/prompts/spacex_podcast.txt` / `spacex_digest.txt` + ledger through 2026-08-07.

- **Length:** 5/10 scripts below `min_podcast_words: 1300` (Ep60 1199, Ep61 **863**, Ep62 1194, Ep65 1189, Ep66 1263). Clearers: Ep58 1359, Ep59 1778 (Q2 special — still under `deep_dive.min_podcast_words: 2400`), Ep63 1436, Ep64 1405, Ep67 1377. Ep61 sits only ~83w above the 0.6× soft-skip floor (780w).
- **Boilerplate (transcript n-grams):** `from an engineering standpoint` **10/10**; `on the ai front` 9/10 (required AI chapter anchor); network AI-voice outro 10/10 (expected); `this is spacex daily episode` 10/10 (expected); `i'm patrick in vancouver` 6/10 (Whisper in/and on the stock sign-off — not a P0; do not phonetic-fix, landmine #17).
- **Chapters:** Snapshot claims 10/10 clean, but committed JSON is not:
  - **Ep58:** six mid-body chapters are raw headlines with literal `Title:` prefix (e.g. `Title: SpaceX knocks out 50th launch on Space Coast this…`).
  - **Ep60:** same class plus bare truncated headline — `Title: Danuri captures…`, `Title: Space finally faced…`, `Title: SpaceX claims Starlink Mobile…`, and bare `Confirmation of a Falcon 9 stage striking the moon tests…`.
  - **Ep61–67:** no new `Title:` spam (bug did not spread further; sanitizer still never shipped so Ep58/60 remain polluted in-tree).
  - **Market Watch gaps / placement:** Ep62–64 have price only in the sign-off → no Market Watch chapter (Closing-before-Market-Watch ordering working as designed). Ep65–66 fire Market Watch on a real segment. **Ep67** fires Market Watch mid-body (`startTime` 252, before Counterpoint) because the script spoke `SPC X closed at $133.29` once in the news body and again as “A quick look at the tape shows SPCX at $133.29” — double price + early marker.
- **Fetch-filter leakage:** 0 hits on `exclude_title_patterns` vs last 10 digests (July-02 junk-title filter still holding).
- **SPCX price-twice:** still on dailies with a market line — Ep58 market “closed at $125.33” **and** closing “trading at $125.33”; same doubled pattern Ep60, Ep65, Ep66; Ep67 doubles mid-body + tape line.
- **Entity bleed:** no new clear Memphis/Colossus-as-SpaceX mis-attribution in Ep58–67 Whisper (Ep61–67 attribute xAI/Colossus/Cursor correctly). Ep53 archive line still live; entity prompt never shipped so regression risk remains.
- **Already-on levers:** `digest_expand_below_target: true`, `min_digest_words: 1200`, `podcast_expand_below_target: false` (network ban). Digest + podcast prompts already state Engineering Deep Dive ~280–360w. No podcast-side length lever is legitimate.
- **Audience:** OP3 downloads_7d 623, downloads_30d 1760, weekly trend [217, 348, 517, 623] — strong upward trajectory.
- **Cost:** ~$0.43/episode avg over last 10.
- **Product notes (not new bugs):** Ep65 leads with the Engineering Deep Dive immediately after identity (thin-news engineering-led shape). `shorts_ab_enabled: false` (operator ended 2026-08-01); `shorts_first_is_hook: true` now on.

## Scoring prior predictions

| Metric | Verdict | Evidence |
|--------|---------|----------|
| chapters_ep*.json titles matching /^Title:\s*/ or bare truncated headline chapters outside known section set (2026-08-07) | miss | Sanitizer never applied; Ep58 still has six `Title:` chapters; Ep60 still has three `Title:` + one bare headline; Ep61–67 clean but historical pollution remains |
| exact phrase 'from an engineering standpoint' in next 10 daily (non-special) transcripts (2026-08-07) | miss | Still **10/10** Ep58–67 Whisper (incl. special Ep59); Aug-05/07 anchor-rotation prompt never applied |
| Market Watch chapter present when script contains SPCX + trading/closed price line (2026-08-07) | miss | Pattern widen never shipped; YAML still requires `S ?P ?C ?X (closed\|is at\|is trading\|opened)` — bare `trading at` / “quick look at the tape” forms unlisted; Ep67 early mid-body price also shows marker fragility |
| on-air xAI facility attributed as SpaceX (Memphis/Colossus/'SpaceX AI' datacenter operator) (2026-08-07) | partial | 0 clear mis-attrs in Ep58–67 window; entity-discipline prompt never shipped (Ep53 archive still wrong) |
| laundered SEO / video-ID junk titles (2026-07-02; already hit) | hit (hold) | Snapshot fetch-filter leakage still **0 hits** |
| episodes >=1300w / conditional deep-dive length (prior; escalated 08-05/07) | miss (reconfirmed) | 5/10 ≥1300w this window; do not re-file podcast-side or “if lever ships” length predictions |

## P0 — listener-facing bugs shipping today

### P0-1 — `Title:` / headline chapters still committed (Ep58 + Ep60)
`digests/spacex/chapters_ep058.json` and `chapters_ep060.json` still ship mid-body chapters whose titles are truncated story headlines, often with a literal `Title:` prefix (digest list artifact). These are not `shows/spacex.yaml` `section_markers` anchors (Introduction / Counterpoint / AI & Compute / Engineering / Market Watch / Teaser / Closing). Listener-facing chapter UI and YouTube/podcast chapter surfaces get spam titles. Proposed Aug-05 and Aug-07; never applied; Ep61–67 did not regress further.

**Proposal:** code-only sanitizer in chapter post-process — drop or rewrite any chapter title matching `^Title:\s*`, and drop titles that are not in the show’s known section-title set when they look like truncated headlines (trailing `…` / ellipsis). One-time cleanup of committed Ep58/Ep60 JSON. Drift-guard: no committed `chapters_ep*.json` title starts with `Title:` or is a bare headline ellipsis. No audio change.

### P0-2 — Market Watch chapter pattern still too narrow
`shows/spacex.yaml` Market Watch pattern is `on the market side|market watch|S ?P ?C ?X (closed|is at|is trading|opened)` and still misses bare `trading at` / tape-phrasing variants the model actually speaks (Aug-07 Ep56 class; Ep67 “A quick look at the tape shows SPCX at…”). Closing-before-Market-Watch `where: end` ordering must stay so sign-off-only price lines remain Closing (Ep62–64 correctly have no Market Watch).

**Proposal:** widen Market Watch pattern to also accept `trading at` / `trades at` / low-key tape openers (`quick market note|quick look at the tape`) while keeping Closing listed first with `where: end`. Metadata-only. Drift-guard in `tests/test_spacex_show.py`.

## P1 — quality ceiling

### P1-1 — `from an engineering standpoint` is a 10/10 verbatim tic (chapter-anchor collision)
Every Ep58–67 transcript opens the deep-dive with the exact phrase. It is also a required chapter key: YAML `engineering deep dive|engineering angle|from an engineering standpoint|the engineering reality` and podcast REQUIRED line allowing only the first two forms in practice. Aug-05/07 shape de-seed never shipped; frequency unchanged (10/10).

**Proposal (prompt, A/B-listen):** keep the allowed anchor **set** for chapters; force rotation across the set; ban reusing the prior episode’s exact opener via shape description + verbatim ban + MEMORY (recent openers do-not-reuse) — **no new quotable full-sentence example**. Prefer that “the engineering angle” / “the engineering reality” actually appear so monoculture breaks without breaking chapter detection. Log successor-tic risk: convergence on “the engineering angle” alone.

### P1-2 — Chronic under-length — ESCALATE (do not re-file)
5/10 under 1300w including Ep61 at 863w; same digest-ceiling diagnosis as June–August reviews; podcast expand off network-wide; digest expand + deep-dive word guidance already on. Conditional “7+/10 if deep-dive lever ships” predictions have missed repeatedly. Per playbook: **stop re-proposing the same lever**.

**Operator decision needed:** (a) accept grok-4.3 plateau at ~1100–1300 for thin news days, (b) raise `min_digest_words` / strengthen full-text fetch substrate only, or (c) accept more thin-day engineering-led shorts (Ep65 shape) as product shape. No podcast word-floor, expand-retry, or conditional length prediction this pass.

### P1-3 — SPCX price spoken twice per daily — ESCALATE (do not re-file)
Deferred since 2026-06-13; chapter blocker cleared 2026-06-18; still doubled on Ep58, Ep60, Ep65, Ep66 (market segment + `{closing_block}` price sentence); Ep67 doubles mid-body + tape. Identical “Market Watch qualitative / single quote in closing” proposal carried across ≥7 reviews without shipping.

**Operator decision needed:** ship the coupled prompt + `closing_block` change as an explicit A/B-listen, or permanently accept double price as brand. Not re-proposed as a silent fix this pass.

### P1-4 — xAI / SpaceX entity discipline (regression guard)
Window Ep58–67 is clean of the Ep53 “SpaceX AI … Memphis” class, but the entity-discipline prompt never shipped and Colossus/xAI/Cursor remain high-traffic topics (Ep62–67). Regression risk only.

**Proposal (prompt, A/B-listen):** hard entity line in podcast (+ digest reinforcement): Colossus/Memphis turbines/xAI compute are xAI unless the digest explicitly states a SpaceX-owned facility; never “SpaceXAI”; attribute cross-company deals as deals. Low urgency vs P0/P1-1.

### P1-5 — Q2/special path under its own floor (carry)
Ep59 (deep-dive path) 1778w vs `deep_dive.min_podcast_words: 2400`. Soft-skip floor (0.6×2400) still passes 1778. Digest/brief substrate only — do not re-enable podcast expand. Carry: richer `spacex_deep_dive.txt` segment pressure / operator runbook when next special is forced.

## P2 — growth / discoverability

- OP3 trajectory is strong (623/7d, accelerating weekly). No P2 emergency.
- Tomorrow-teaser “Watch for the first/next <test>” frame is no longer dominant in Ep58–67 (prior P2 can stay deferred).
- NASA public-domain b-roll pool vs retired `shorts_ab` generated motion remains a render-side follow-up (operator ended motion A/B 2026-08-01) — not audio.
- Scheduler forensics for any missed Sunday `weekly_summary_segment` run (June-28 class) — operator item, carry.

## Deferred

- OPERATOR DECISION (escalation after repeated misses): accept grok-4.3 daily length plateau vs further digest-substrate only (`min_digest_words` / full-text fetch) — do not re-propose podcast expand, word floors in podcast prompt, or “7+/10 if deep-dive ships” predictions.
- OPERATOR DECISION (escalation since 2026-06-13): SPCX price-once — Market Watch qualitative + single precise quote only in `closing_block`; coupled A/B-listen; stop carrying the same unshipped fix as a silent proposal.
- Q2/special path: enforce richer `spacex_deep_dive.txt` briefs so specials clear `deep_dive.min_podcast_words` 2400 — digest/brief substrate only.
- Tomorrow-teaser rotation (P2; frame no longer dominant in Ep58–67).
- Scheduler forensics for any missed Sunday weekly_summary_segment run (June-28 class) — operator.
- NASA public-domain b-roll pool vs retired shorts_ab generated motion — render-side follow-up, not audio.
