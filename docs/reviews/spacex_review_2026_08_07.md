# spacex — quality review (2026-08-07)

_Generated by the Grok-4.3 Show Review Agent (`scripts/run_show_review.py`). Proposed prompt/audio changes are listed in the PR body under "A/B-listen required" and are NOT applied — the operator applies and listens before merging (landmine #17)._

## Evidence

Snapshot (`scripts/review_snapshot.py spacex`) on Ep53–62 `_tts.txt` + Whisper transcripts + `chapters_ep053.json`–`chapters_ep062.json` + `shows/spacex.yaml` (llm/chapters/deep_dive) + `shows/prompts/spacex_podcast.txt` / `spacex_digest.txt` + ledger through 2026-08-05.

- **Length:** 7/10 scripts below `min_podcast_words: 1300` (Ep54 1201, Ep55 1288, Ep56 1299, Ep57 1188, Ep60 1199, Ep61 **863**, Ep62 1194). Clearers: Ep53 1307, Ep58 1359, Ep59 1778 (Q2 special — still ~26% under `deep_dive.min_podcast_words: 2400`). Ep61 is only ~83w above the 0.6× soft-skip floor (780w).
- **Boilerplate (transcript n-grams):** `from an engineering standpoint` **10/10** (Ep53–62); `on the ai front` 9/10; `worth keeping an eye on` 7/10; network AI-voice outro 10/10 (expected); `this is spacex daily episode` 10/10.
- **Chapters:** Snapshot claims 10/10 clean, but committed JSON is not:
  - **Ep58:** six mid-body chapters are raw headlines with literal `Title:` prefix (e.g. `Title: SpaceX knocks out 50th launch on Space Coast this…`).
  - **Ep60:** same class plus bare truncated headlines — `Title: Danuri captures before-and-after images of Falcon 9…`, `Title: Space finally faced the people living next to its…`, `Title: SpaceX claims Starlink Mobile will be better than…`, and `Confirmation of a Falcon 9 stage striking the moon tests…`.
  - **Market Watch misses:** Ep56 transcript speaks “SPC X trading at $114.53” but has **no** Market Watch chapter — YAML pattern requires `is trading` (`shows/spacex.yaml` Market Watch marker), so bare `trading at` never matches. Ep53/57/62 also lack Market Watch when the segment is thin or phrasing drifts.
- **Fetch-filter leakage:** 0 hits on `exclude_title_patterns` vs last 10 digests (July-02 junk-title filter still holding).
- **SPCX price-twice:** still on most dailies with a market line — e.g. Ep54 “SPCX closed at $108.37, down 3.9%” **and** closing “SPCX is trading at $108.37, down 3.9%”; same doubled pattern Ep55–58, Ep60.
- **Entity bleed:** Ep53 still on-air: “SpaceX AI added another data center building in Memphis” / “SpaceX delayed removal of XAI's unpermitted temporary turbines” (xAI Colossus framed as SpaceX). Ep54–62 mostly attribute Colossus/xAI correctly; fix never shipped so regression risk remains.
- **Already-on levers:** `digest_expand_below_target: true`, `min_digest_words: 1200`, `podcast_expand_below_target: false` (network ban). Digest + podcast prompts already state Engineering Deep Dive ~280–360w. No podcast-side length lever is legitimate.
- **Audience:** OP3 downloads_7d 517, downloads_30d 1221, weekly trend [118, 217, 348, 517] — strong upward trajectory.
- **Cost:** ~$0.42/episode avg over last 10.
- **ASR note (not filed as P0):** snapshot 6/10 “i'm patrick and vancouver” is likely Whisper in/and confusion on the stock “I'm Patrick in Vancouver” sign-off; do not ship a phonetic workaround (landmine #17).

## Scoring prior predictions

| Metric | Verdict | Evidence |
|--------|---------|----------|
| chapters_ep*.json titles matching /^Title:\s*/ in next 10 (2026-08-05) | miss | Sanitizer never applied; Ep58 still has six `Title:` chapters; **Ep60 added** three more `Title:` + one bare headline chapter |
| exact phrase 'from an engineering standpoint' in next 10 daily transcripts (2026-08-05) | miss | Still **10/10** Ep53–62 Whisper; anchor-rotation prompt never applied |
| on-air xAI facility attributed as SpaceX (Memphis/Colossus/'SpaceX AI') in next 10 (2026-08-05) | partial | Ep53 still ships the Memphis “SpaceX AI” mis-attr; Ep60–62 clean on that class (n=3) but entity-discipline prompt never shipped |
| laundered SEO / video-ID junk titles (2026-07-02; already hit 08-05) | hit (hold) | Snapshot fetch-filter leakage still **0 hits** |
| episodes >=1300w / conditional deep-dive length (prior; escalated 08-05) | miss (reconfirmed) | 3/10 ≥1300w this window; do not re-file podcast-side or “if lever ships” length predictions |

## P0 — listener-facing bugs shipping today

### P0-1 — `Title:` / headline chapters spreading (Ep58 + Ep60)
`digests/spacex/chapters_ep058.json` and `chapters_ep060.json` ship mid-body chapters whose titles are truncated story headlines, often with a literal `Title:` prefix (digest list artifact). These are not `shows/spacex.yaml` `section_markers` anchors (Introduction / Counterpoint / AI & Compute / Engineering / Market Watch / Teaser / Closing). Listener-facing chapter UI and YouTube/podcast chapter surfaces get spam titles. Aug-05 proposed sanitizer never applied; bug **worsened** (1 → 2 episodes in window).

**Proposal:** code-only sanitizer in chapter post-process — drop or rewrite any chapter title matching `^Title:\s*`, and drop titles that are not in the show’s known section-title set when they look like truncated headlines (trailing `…` / ellipsis). Drift-guard: no committed `chapters_ep*.json` title starts with `Title:` or is a bare headline ellipsis. No audio change.

### P0-2 — Market Watch chapter pattern misses bare “trading at”
Ep56 transcript: “A quick look at the market shows SPCX trading at $114.53” — no Market Watch chapter. YAML pattern (`shows/spacex.yaml` chapters.section_markers Market Watch) is `S ?P ?C ?X (closed|is at|is trading|opened)` and does not match bare `trading at` after pronunciation spacing.

**Proposal:** extend Market Watch pattern to also accept `trading at` / `trades at` (still post-pronunciation tolerant). Metadata-only; verify Closing-before-Market-Watch ordering still wins the sign-off price line on weekly-recap / no-segment days. Drift-guard in `tests/test_spacex_show.py`.

## P1 — quality ceiling

### P1-1 — `from an engineering standpoint` is a 10/10 verbatim tic (chapter-anchor collision)
Every Ep53–62 transcript opens the deep-dive with the exact phrase. It is also a required chapter key: YAML `engineering deep dive|engineering angle|from an engineering standpoint|the engineering reality` and podcast REQUIRED line allowing only the first two forms in practice. Aug-05 shape de-seed never shipped; frequency unchanged (10/10).

**Proposal (prompt, A/B-listen):** keep the allowed anchor **set** for chapters; force rotation across the set; ban reusing the prior episode’s exact opener via shape description + verbatim ban + MEMORY (recent openers do-not-reuse) — **no new quotable full-sentence example**. Prefer that “the engineering angle” / “the engineering reality” actually appear so monoculture breaks without breaking chapter detection. Log successor-tic risk: convergence on “the engineering angle” alone.

### P1-2 — Chronic under-length — ESCALATE (do not re-file)
7/10 under 1300w including Ep61 at 863w; same digest-ceiling diagnosis as June–August reviews; podcast expand off network-wide; digest expand + deep-dive word guidance already on. Conditional “7+/10 if deep-dive lever ships” predictions have missed repeatedly. Per playbook: **stop re-proposing the same lever**.

**Operator decision needed:** (a) accept grok-4.3 plateau at ~1100–1300 for thin news days, (b) raise `min_digest_words` / strengthen full-text fetch substrate only, or (c) accept more thin-day engineering-led shorts as product shape. No podcast word-floor, expand-retry, or conditional length prediction this pass.

### P1-3 — SPCX price spoken twice per daily — ESCALATE (do not re-file)
Deferred since 2026-06-13; chapter blocker cleared 2026-06-18; still doubled on Ep54–58, Ep60 (market segment + `{closing_block}` price sentence). Identical “Market Watch qualitative / single quote in closing” proposal carried across ≥6 reviews without shipping.

**Operator decision needed:** ship the coupled prompt + `closing_block` change as an explicit A/B-listen, or permanently accept double price as brand. Not re-proposed as a silent fix.

### P1-4 — xAI / SpaceX entity conflation still in catalog (Ep53) + prompt unshipped
Ep53: “SpaceX AI added another data center building in Memphis”. July-02 network + Aug-05 review already flagged Memphis/Colossus as xAI’s. Later episodes improved opportunistically; hard rule never landed.

**Proposal (prompt, A/B-listen):** hard entity line in podcast (+ digest reinforcement): Colossus/Memphis turbines/xAI compute are xAI unless the digest explicitly states a SpaceX-owned facility; never “SpaceXAI” / “SpaceX AI” as a datacenter operator name; attribute cross-company deals as deals. (Colossus/Anthropic material remains real — do not relitigate operator-confirmed facts.)

### P1-5 — Deep-dive special under its own floor (Ep59)
Ep59 1778w vs `deep_dive.min_podcast_words: 2400` and special podcast target 2400–3000. Soft-skip (0.6×2400) still passes 1778. Substrate issue: thin brief → thin special.

**Proposal:** digest/brief-side only — pressure `spacex_deep_dive.txt` segment floors already listed; optional metrics flag when `episode_kind=deep_dive` and words < min. Do **not** re-enable podcast expand.

### P1-6 — Ep61 critically short body / thin Top News coverage
863w with a very short Counterpoint chapter (~11s) and compressed news body. Symptom of digest ceiling on a thin day, not a separate podcast bug. Fold into operator length decision (P1-2); no new podcast lever.

## P2 — growth / discoverability

- OP3 trend is healthy (517 d7 / 1221 d30, accelerating weekly). No emergency growth fix.
- Tomorrow-teaser “Watch for the first/next …” frame is no longer dominant in this window (Aug-05 observation holds) — keep rotation deferred/low priority.
- Shorts motion A/B already ended 2026-08-01; NASA b-roll path is render-side follow-up, not this audio pass.

## Deferred

- OPERATOR DECISION (escalation after repeated misses): accept grok-4.3 daily length plateau vs further digest-substrate only (`min_digest_words` / full-text fetch) — do not re-propose podcast expand, podcast word floors, or “7+/10 if deep-dive ships” predictions.
- OPERATOR DECISION (escalation since 2026-06-13): SPCX price-once — Market Watch qualitative + single precise quote only in `closing_block`; coupled A/B-listen; stop carrying the same unshipped fix as a silent proposal.
- Q2/special path: richer `spacex_deep_dive.txt` briefs so specials clear `deep_dive.min_podcast_words` 2400 (Ep59 shipped 1778) — brief substrate only.
- Tomorrow-teaser rotation (P2; frame no longer dominant Ep53–62).
- Scheduler forensics for any missed Sunday `weekly_summary_segment` run (June-28 class) — operator.
- NASA public-domain b-roll pool vs retired shorts_ab generated motion — render-side, not audio.
