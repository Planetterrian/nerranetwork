# models_agents — quality review (2026-08-04)

_Generated by the Grok-4.3 Show Review Agent (`scripts/run_show_review.py`). Proposed prompt/audio changes are listed in the PR body under "A/B-listen required" and are NOT applied — the operator applies and listens before merging (landmine #17)._

## Evidence

Snapshot (`scripts/review_snapshot.py models_agents`): last-10 `_tts.txt` lengths ep122–131 = 1466, 1174, 1135, 1484, 1592, 1099, 1305, 1501, 1305, 1521 → **7/10 below** `llm.min_podcast_words: 1500` (only ep126=1592, ep129=1501, ep131=1521 at-or-over). Config still has the July substrate only: `min_digest_words: 1300`, `digest_expand_below_target: true`, `podcast_expand_below_target: false` (`shows/models_agents.yaml`). The Aug-2 proposal to raise `min_digest_words` → 1600 is **not in the tree**.

Repeated-phrase detector (10 transcripts): expected network outro/disclosure at 10/10; boilerplate tics — “okay let's pop the hood on” **8/10**, “keep an eye on” **6/10** (ep122–127), full closing block ~6/10. Avg cost ~$0.350/ep. OP3: downloads_7d **105**, 30d 642, weekly trend [170, 165, 176, **105**] — partial recovery from the Aug-2 88 trough, still below the prior ~170 plateau.

Chapters: snapshot says “10/10 clean”, but raw `chapters_ep*.json` again contradict that heuristic (same inverse-correlation June-21 diagnosed):
- **ep125 / ep130**: truncated auto-titles ending in “…” (e.g. ep125: “Builders running production agents should test the new…”, “MCP’s largest update removes session stickiness so agents…”).
- **ep123 / ep124 / ep126 / ep128 / ep131**: sparse (often Intro + one mid + UTH/Closing only).
- **ep127**: first chapter starts at **240.4s** — no markers for the opening ~4 minutes (cold-open never hits `welcome.*Models and Agents|Models and Agents.*episode`).
- **ep128**: **no Under the Hood chapter** — deep-dive opened “Everyone talks about MCP…” without “pop the hood” / “under the hood” (marker still depends on that phrase — `shows/models_agents.yaml` `chapters.section_markers`).
- **ep122 / ep129 / ep130 / ep131**: UTH present when host says “pop the hood”.

Whisper spot-check (ep122–131): no `An-thropic` / `koo-dah` regressions. Continuity **no longer** speaks bare episode numbers (uses “yesterday” / “prior episode”) — Aug-2 baseline from ep120 has cleared. New formula tic in the deep-dive body: **“Everyone talks about …”** opens or immediately follows UTH in **ep124–131** (8/10; ep122–123 are the only clean UTH openers without it). Host ASR confusions (“Clawed”, “Andthropic”, “Narra”) treated as Whisper, not `_tts.txt` respellings.

July-19 and Aug-2 reviews both had `shipped: []`; their lever-gated predictions must be scored against today’s tree and the unapplied proposals.

## Scoring prior predictions

| Metric | Verdict | Evidence |
|--------|---------|----------|
| episodes below min_podcast_words (1500) in last 10 | **miss** | 7/10 still under; `min_digest_words` still 1300 — Aug-2 1600 lever never merged |
| "keep an eye on" / teaser-opener variants, last 10 | **miss** | 6/10 (ep122–127); ep128–131 partial drift to “Before we go” without the phrase; Aug-2 shape de-seed never applied |
| successor teaser-opener tic ≥6/10 | **hit** | “Before we go” at 5/10 (ep126,128–131) — under threshold; watch next pass |
| spoken episode-number continuity callbacks, last 10 | **hit** | 0 “episode N” / EpN in ep122–131 Whisper; yesterday/prior-episode framing instead |

## P0 — listener-facing bugs shipping today

None verified. No spoken failure-mode text, no `koo-dah`/`An-thropic` transcript leaks, no enclosure/RSS path issues. (ep125 Whisper speaks a github path — “github.com slash JCE Kent…” — one-off prompt compliance slip, not a systemic P0.)

## P1 — quality ceiling

### 1. Chronic under-length — ESCALATE (do not re-file the same lever a third time)
7/10 scripts <1500w (worst: ep127 1099, ep124 1135, ep123 1174). Scripts still track the digest ~1:1. Podcast-side expand correctly **off**. Substrate still `min_digest_words: 1300` + digest expand.

**Named misses (playbook escalate rule):**
- July-19 predicted ≤3/10 under after a digest min raise → **miss** (lever never applied; 8–9/10 stayed under).
- Aug-2 predicted ≤4/10 under after `min_digest_words: 1600` → **miss** (lever still not in tree; 7/10 under).

**Operator decision required (pick one; do not re-propose 1600 as a silent third filing):**
- **(A)** Raise `min_digest_words` hard to **1600** (next digest-only step; still the sanctioned substrate class).
- **(B)** Add an Under-the-Hood **sentence/fact floor** in the digest prompt (licensed-knowledge section — deferred “expand UTH”, unblocked now that four-show length A/B and podcast-expand are settled).
- **(C)** Accept ~1200–1400w as this show’s natural length and lower `min_podcast_words` so dashboards stop false-alarming.

Do **not** re-enable `podcast_expand_below_target`. No length prediction this pass until a lever is chosen.

### 2. “keep an eye on” Tomorrow-Teaser opener tic (prompt-seeded; never de-seeded)
Transcripts: ep122 “Tomorrow, keep an eye on early benchmarks…”, ep123 “Tomorrow, keep an eye on further rough rule expansions…”, ep124–125/127 same shape, ep126 “Before we go, keep an eye on…”. Snapshot **6/10**. July-19 and Aug-2 both proposed a shape de-seed that **never shipped**. Re-propose on the **podcast** prompt (spoken surface), by shape + verbatim ban + **no quotable replacement menu** (meta-rule). Predict successor convergence on “Before we go, …” which is already 5/10.

### 3. NEW — “Everyone talks about …” Under-the-Hood body tic (8/10)
Deep-dive bodies now open with the same formula after (or instead of) “pop the hood”:
- ep124: “Everyone talks about uniform adapters as the obvious default…”
- ep125: “Everyone talks about reinforcement learning post-training…”
- ep126: “Everyone talks about instruction hierarchies…”
- ep127/128: **skip** “pop the hood” entirely and lead with “Everyone talks about agentic rag / MCP…”
- ep129–131: same “Everyone talks about …” immediately after pop-the-hood.

This is third-generation convergence (signature opener → teaser tic → UTH contrast template). De-seed by **shape** (contrast-cliché opener that pits “everyone/most teams” against the real engineering), verbatim ban on the exact phrase, **no example replacement menu**, and add a successor-tic prediction.

### 4. Chapter quality still inverse to prompt compliance (carried + small no-A/B patch)
Full keyword chapters appear when the host announces section-ish labels; natural delivery → sparse/auto-segment titles (ep125/130 ellipsis titles; ep127 gap to 240s; ep128 lost UTH when “pop the hood” was skipped in favor of “Everyone talks about”). **Durable fix remains digest-driven / position-aware titles** (deferred network-wide).

**Small verified patch this pass:** extend the Under the Hood `section_markers` pattern with `everyone talks about` so ep127/128-class deep-dives still get a chapter when the host uses the formula opener without “pop the hood”. Config-only; does not change audio. Does **not** replace digest-driven titles.

### 5. Continuity episode numbers — cleared, keep a belt-and-suspenders ban
0 spoken “episode N” in ep122–131 (hit). Digest already says prefer date/“yesterday” and never “EpN”. Still add an explicit podcast-side ban so the ep120 regression cannot return silently (Aug-2 item, never applied).

### 6. “Okay let's pop the hood on” (8/10) — leave alone
Signature deep-dive opener **and** the primary chapter signal. Prior passes deliberately kept it. Not proposed. (ep131’s “pop the hood on this one” is weak but still fires the marker.)

## P2 — growth/discoverability

- **OP3:** 7d downloads 105 vs Aug-2’s 88 and the prior ~170 plateau — recovering, not crashing. No feed/metadata bug found. Continue watch; no product change this pass.
- Network outro / YouTube CTA blocks are repetitive by design (cross-promo rotation); not filed as defects.

## What this pass proposes (nothing “shipped” — non-interactive)

1. `shows/prompts/models_agents_podcast.txt` — shape-ban “keep an eye on” teaser openers (verbatim ban, no example menu). **A/B-listen.**
2. `shows/prompts/models_agents_podcast.txt` — shape-ban “Everyone talks about …” as UTH/deep-dive opener (verbatim ban, no example menu). **A/B-listen.**
3. `shows/prompts/models_agents_podcast.txt` — continuity: never speak episode numbers; ≤1 callback. **A/B-listen.**
4. `shows/models_agents.yaml` — UTH chapter marker += `everyone talks about` (catches ep127/128-class). **No A/B** (markers only).
5. Drift-guard tests (operator to add): teaser/UTH bans present in podcast prompt; UTH marker includes the new alternate; no `min_digest_words` silent drop.

**Not proposed (escalate):** another `min_digest_words` bump — operator must choose A/B/C above.

## Deferred

- **Operator length decision A/B/C** (escalated after two missed digest-floor predictions; blocking further length filings until chosen).
- Digest-driven / position-aware mid-section chapter titles (carried; ep078-class residual; ep127/128 prove markers still brittle even after the everyone-talks-about catch).
- Under the Hood licensed-knowledge **floor** as option B if operator wants length without accepting a lower podcast floor.
- Optional accept-lower-`min_podcast_words` (option C).
- “let’s pop the hood on” signature opener — do not rotate without A/B evidence.
- Network-wide: pronunciation-map word-guides that collide with English (LoRA→Laura, RAG→rag) leaking to blog/RSS if sourced from post-pronunciation `_tts.txt`.
- July-2 A/B still open: selection rebalance (lab product/feature > preprints; arXiv ≤40%); recap must synthesize not splice.
- OP3 plateau watch (105 vs ~170) — one more cycle before product changes.

## do_not_retry

None new (no closed-unmerged review PR or revert evidence in this bundle; `do_not_retry: []` unchanged). Length re-raise without operator decision is now process-blocked by the escalate rule, not a do_not_retry entry.
