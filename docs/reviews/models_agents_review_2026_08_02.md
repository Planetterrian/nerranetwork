# models_agents — quality review (2026-08-02)

_Generated by the Grok-4.3 Show Review Agent (`scripts/run_show_review.py`). Proposed prompt/audio changes are listed in the PR body under "A/B-listen required" and are NOT applied — the operator applies and listens before merging (landmine #17)._

## Evidence

Snapshot (`scripts/review_snapshot.py models_agents`): last-10 `_tts.txt` lengths ep120–129 = 1296, 1420, 1466, 1174, 1135, 1484, 1592, 1099, 1305, 1501 → **8/10 below** `llm.min_podcast_words: 1500` (only ep126/ep129 at-or-over). Config already has the sanctioned substrate levers live: `min_digest_words: 1300`, `digest_expand_below_target: true`, and `podcast_expand_below_target: false` (disabled July 29 with the network-wide ban on podcast-side length retries — `shows/models_agents.yaml`).

Repeated-phrase detector (10 transcripts): expected network outro/disclosure blocks at 10/10; **boilerplate tics** — “okay let's pop the hood on” 8/10, “keep an eye on” 7–8/10, “tomorrow keep an eye on” 7/10, full closing block ~6/10. Avg cost ~$0.342/ep (up vs June ~$0.10 as digest-expand runs). OP3: downloads_7d **88**, 30d 618, weekly trend [165, 168, 173, **88**] — sharp week-over-week drop.

Chapters: snapshot says “10/10 clean”, but raw `chapters_ep*.json` contradict that heuristic (same inverse-correlation June-21 diagnosed):
- ep120/121/125 ship **truncated auto-titles** ending in “…” (e.g. ep120: “Anthropic’s new Claude Security plugin turns your terminal…”).
- ep123/124/126/128/129 are **sparse** (often Intro + one mid + Under the Hood/Closing only).
- **ep127** first chapter starts at **240.4s** — no markers for the opening ~4 minutes.
- **ep128** has **no Under the Hood chapter** because the deep-dive opened “Everyone talks about MCP…” without “pop the hood” / “under the hood” (marker depends on that phrase — `shows/models_agents.yaml` chapters.section_markers).

Whisper spot-check (ep120–129): no `An-thropic` / `koo-dah` regressions. Continuity still speaks **episode numbers** (ep120: “tracked agents and tool use arc from episode 119”) — the July-2 banned shape. Host sometimes says “Clawed”/“Andthropic” in ASR; treat as Whisper confusions unless `_tts.txt` shows respellings (not evidenced here).

July-19 review shipped **nothing** (`shipped: []`); its two predictions were lever-gated and must be scored against today’s tree.

## Scoring prior predictions

| Metric | Verdict | Evidence |
|--------|---------|----------|
| episodes below min_podcast_words (1500) in last 10 | **miss** | 8/10 still under; `min_digest_words: 1300` + digest expand already on; ceiling unchanged |
| "keep an eye on" as Tomorrow Teaser opener, last 10 | **miss** | Still ~8/10 (ep120–127 nearly every teaser; ep128–129 only partial drift); July-19 shape de-seed never applied |
| verbatim-doubled sentence pairs (July-2, carried) | **hit** | No doubled-sentence padding in ep120–129 transcripts post expansion-strip |
| garble/chapter fixes (An-thropic, koo-dah, Under-the-Hood marker when phrase present) | **hit** (stable) | 0 garbles in last-10; Under the Hood chapter present when host says “pop the hood” (ep120–126,129) |

## P0 — listener-facing bugs shipping today

None verified. No spoken failure-mode text, no `koo-dah`/`An-thropic` transcript leaks, no enclosure/RSS path issues.

## P1 — quality ceiling

### 1. Chronic under-length — escalate (do not re-file the same lever blindly)
8/10 scripts <1500w (worst: ep124 1135, ep127 1099, ep123 1174). Scripts still track the digest ~1:1. Podcast-side expand is correctly **off** (meta-review: ~10 length misses vs 1 hit). Substrate already at `min_digest_words: 1300` + `digest_expand_below_target: true` and the July-19 “raise digest min → ≤3/10 under” prediction **missed**.

**Operator decision (name the misses):** (A) raise `min_digest_words` hard to **1600** (proposed this pass as the next digest-only step), (B) add an Under-the-Hood **sentence/fact floor** in the digest prompt (licensed-knowledge section — the deferred “expand UTH” item, unblocked now that four-show length A/B and podcast-expand are settled), or (C) accept ~1200–1400w as this show’s natural length and lower `min_podcast_words` so dashboards stop false-alarming. Do not re-enable `podcast_expand_below_target`.

### 2. “keep an eye on” Tomorrow-Teaser opener tic (prompt-seeded rotation failure)
Transcripts: ep120 “Tomorrow keep an eye on further pool side…”, ep121 “Tomorrow keep an eye on Android support…”, ep122–125/127 same shape, ep126 “Before we go, keep an eye on…”. Snapshot 7–8/10. July-19 proposed a shape de-seed that **never shipped**. Re-propose on the **podcast** prompt (spoken surface), by shape + verbatim ban + no quotable replacement menu (meta-rule: de-seed by shape; predict successor tic).

### 3. Continuity callback speaks episode numbers
ep120 Whisper: “continues the tracked agents and tool use arc from **episode 119**”; similar “tracked since / arc from episode N” pattern the July-2 network pass already flagged. Digest prompt says prefer date/“yesterday” and never “EpN”, but the podcast script still emits spoken episode numbers. Tighten podcast rules: ≤1 continuity callback/episode, **never** episode numbers.

### 4. Chapter quality still inverse to prompt compliance (carried diagnosis)
Full keyword chapters appear when the host announces banned section labels (ep122). Natural delivery → sparse/auto-segment titles (ep120/121/125 ellipsis titles; ep127 gap to 240s; ep128 lost Under the Hood when “pop the hood” was skipped). **Not fixable by more markers.** Durable fix remains digest-driven / position-aware titles (deferred network-wide).

### 5. “Okay let's pop the hood on” (8/10) — leave alone
Signature deep-dive opener and the chapter signal. Prior passes deliberately kept it. Not proposed.

## P2 — growth/discoverability

- **OP3 step-down:** 7d downloads 88 vs prior three weeks 165–173. Single-week noise possible (snapshot timing); watch next review before product changes. No feed/metadata bug found tied to the drop.
- Network outro / YouTube CTA blocks are repetitive by design (cross-promo rotation); not filed as defects.

## What this pass proposes (nothing “shipped” — non-interactive)

1. `shows/models_agents.yaml` — `min_digest_words: 1300` → `1600` (digest-substrate length lever only).
2. `shows/prompts/models_agents_podcast.txt` — shape-ban “keep an eye on” teaser openers (verbatim ban, no example menu).
3. `shows/prompts/models_agents_podcast.txt` — continuity: never speak episode numbers; ≤1 callback.
4. Drift-guard tests (operator to add): teaser-ban string absent from prompt’s allowed examples; `min_digest_words >= 1600`; podcast prompt forbids “episode {N}” continuity.

All prompt/audio-affecting items need **A/B-listen (landmine #17)**.

## Deferred

- Digest-driven / position-aware mid-section chapter titles (carried; ep078-class residual; ep127/128 prove markers still brittle).
- Under the Hood licensed-knowledge **floor** as alternate/complement length lever if 1600 digest min is not enough (carried forward as operator option B).
- Optional accept-lower-`min_podcast_words` if operator chooses reality-match over longer digests.
- “let’s pop the hood on” signature opener — do not rotate without A/B evidence.
- Network-wide: pronunciation-map word-guides that collide with English (LoRA→Laura, RAG→rag) leaking to blog/RSS if sourced from post-pronunciation `_tts.txt` (durable fix = pre-pronunciation or Whisper transcript for reader text).
- July-2 A/B still open: selection rebalance (lab product/feature > preprints; arXiv ≤40%); recap must synthesize not splice.

## do_not_retry

None new (no closed-unmerged review PR or revert evidence in this bundle; `do_not_retry: []` unchanged).
