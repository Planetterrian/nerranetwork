# models_agents — quality review (2026-08-11)

_Generated by the Grok-4.3 Show Review Agent (`scripts/run_show_review.py`). Proposed prompt/audio changes are listed in the PR body under "A/B-listen required" and are NOT applied — the operator applies and listens before merging (landmine #17)._

## Evidence

Snapshot (`scripts/review_snapshot.py models_agents`): last-10 `_tts.txt` lengths ep129–138 = 1501, 1305, 1521, 1159, 1201, 1381, 1442, 1480, 1285, 1336 → **8/10 below** `llm.min_podcast_words: 1500` (only ep129=1501, ep131=1521 at-or-over). Config unchanged since July: `min_digest_words: 1300`, `digest_expand_below_target: true`, `podcast_expand_below_target: false` (`shows/models_agents.yaml`). Aug-2/Aug-4 `min_digest_words: 1600` lever was **never merged** and is under an explicit operator-decision escalate — not re-filed here.

Repeated-phrase detector (10 transcripts): expected network outro/disclosure at 10/10; signature “let's pop the hood on” / “okay let's pop the hood on” still ~8/10 (intentional chapter signal). **Boilerplate tics that matter this pass:**
- **“Before we go”** opens the Tomorrow Teaser in **10/10** (ep129–138) — the Aug-4 successor watch that was 5/10 has fully converged.
- **“keep an eye on”** as teaser opener: **~0/10** in ep129–138 (cleared vs Aug-4 6/10 baseline) even though the shape-ban never landed.
- **UTH contrast-cliché:** “Everyone talks about…” in ep129–133,135,137; “Everyone treats…” in ep134,136,138 → **10/10** deep-dives open with the same everyone-vs-reality throat-clear (exact “Everyone talks about” still 7/10).

Chapters: snapshot says “10/10 clean”; raw `chapters_ep*.json` again contradict that heuristic:
- **ep132 / ep133: no Under the Hood chapter** — host skipped “pop the hood” and led with “Everyone talks about…” (ep132 TTS/local-voice deep-dive; ep133 model-collapse). `section_markers` still only `under the hood|deep dive|technical deep dive|pop the hood` (`shows/models_agents.yaml`) — Aug-4 `everyone talks about` alternate **never applied**.
- Truncated auto-titles ending “…”: ep130 (“Qwen just moved its largest model yet from preview to…”, “It notes that current systems still need human escalation…”), ep132, ep133, ep135, ep136, ep137.
- Sparse keyword sets when host stays natural (ep129/131/134/138 often Intro + Practical + UTH + Teaser + Closing only).
- ep135 first marker at **59.8s** (better than historical ep127 240s gap).

Whisper spot-check (ep129–138): no `An-thropic` / `koo-dah` regressions. Continuity uses yesterday/prior-arc framing — **0** spoken “episode N” callbacks beyond the structural identity line “This is Models & Agents, Episode N”. Identity line is expected, not a continuity tic.

Cost ~$0.365/ep avg. OP3: downloads_7d **152**, 30d 657, weekly [162, 175, 96, 152] — recovered from the Aug-2 88 trough toward the prior ~170 plateau; no product change warranted.

Aug-2 and Aug-4 both had `shipped: []`; score their pending predictions against today’s tree and unapplied proposals.

## Scoring prior predictions

| Metric | Verdict | Evidence |
|--------|---------|----------|
| "keep an eye on" as Tomorrow Teaser / before-we-go opener, last 10 post-merge | **hit** | ~0/10 in ep129–138 teasers (drifted fully off the phrase) |
| "Everyone talks about" as UTH / deep-dive opener, last 10 post-merge | **miss** | 7/10 exact + 3/10 "Everyone treats" = 10/10 contrast-cliché; Aug-4 shape-ban never applied |
| successor UTH-opener tic ≥6/10 (new phrase) | **hit** | "Everyone treats" alone is 3/10; no independent successor ≥6/10 — original formula still dominates |
| successor teaser-opener "Before we go" ≥6/10 | **miss** | **10/10** ep129–138 open teaser with "Before we go" |
| episodes with Under the Hood chapter when deep-dive present, last 10 | **miss** | 8/10 (ep132/133 missing UTH); marker alternate never shipped |
| spoken episode-number continuity callbacks, last 10 | **hit** | 0 continuity "episode N" / EpN in ep129–138 (identity line only) |

## P0 — listener-facing bugs shipping today

None verified. No spoken failure-mode text, no `koo-dah`/`An-thropic` transcript leaks, no enclosure/RSS path issues.

## P1 — quality ceiling

### 1. Chronic under-length — still OPERATOR DECISION (do not re-file 1600)
8/10 scripts <1500w (worst: ep132=1159, ep133=1201, ep137=1285). Scripts still track the digest ~1:1. Podcast-side expand correctly **off**.

**Named misses (playbook escalate rule — third identical filing forbidden):**
- July-19 predicted ≤3/10 under after a digest min raise → miss (lever never applied).
- Aug-2 predicted ≤4/10 under after `min_digest_words: 1600` → miss (lever still not in tree; 8/10 under today).

**Operator must pick one (carried, not re-proposed as a silent config PR):**
- **(A)** Raise `min_digest_words` hard to **1600** (sanctioned digest-substrate class).
- **(B)** Under-the-Hood **sentence/fact floor** in the digest prompt (licensed-knowledge section).
- **(C)** Accept ~1200–1400w natural length and lower `min_podcast_words` so dashboards stop false-alarming.

Do **not** re-enable `podcast_expand_below_target`. No length prediction this pass until a lever is chosen.

### 2. “Everyone talks about / treats …” UTH body tic — 10/10 (re-propose; never applied)
Deep-dive bodies still open with the same contrast-cliché after (or instead of) pop-the-hood:
- ep129: “Everyone talks about giving coding agents more context…”
- ep130: “Everyone talks about early exiting…”
- ep131: “Everyone talks about scaling laws…”
- ep132/133: **skip** pop-the-hood → lead with “Everyone talks about…”
- ep134/136/138: slight mutation “Everyone treats …” (same shape)
- ep135/137: “Everyone talks about diffusion / soft masking…”

Third-generation convergence is live. De-seed by **shape** (contrast-cliché that pits everyone/most-teams against “the real engineering”), verbatim ban on “Everyone talks about” / “Everyone treats”, **no replacement menu**, successor-tic prediction required.

### 3. “Before we go” Tomorrow-Teaser opener — 10/10 successor convergence
“keep an eye on” cleared (prediction hit), but the teaser locked onto **“Before we go, …”** in every ep129–138 transcript. Aug-4 watch threshold was ≥6/10 — now fully saturated. Shape-ban the default teaser throat-clear (including residual “keep an eye on” / “Tomorrow keep an eye on”) with **no quotable replacement menu**; lead with the substance of what to watch. Expand Tomorrow Teaser `section_markers` so chapters still fire when the host varies the lead-in (marker already includes `Before we go|before we wrap` — add substance-first alternates carefully).

### 4. Chapter quality still inverse to prompt compliance + small no-A/B marker catch
Full keyword chapters when host announces section-ish labels; natural delivery → sparse/ellipsis auto-titles; ep132/133 lost UTH when pop-the-hood was skipped for the Everyone-talks formula. **Durable fix remains digest-driven / position-aware titles** (deferred network-wide).

**Small verified config patch:** extend Under the Hood `section_markers` with `everyone talks about|everyone treats` so ep132/133-class deep-dives still get a chapter. Config-only; does not change audio. Does **not** replace digest-driven titles.

### 5. Continuity episode numbers — cleared; optional belt-and-suspenders
0 spoken continuity “episode N” in ep129–138 (hit, sustained from Aug-4). Digest already prefers date/“yesterday”. Re-propose the never-landed explicit podcast-side ban so the ep120 regression cannot return silently.

### 6. “Okay let's pop the hood on” (~8/10) — leave alone
Signature deep-dive opener **and** primary chapter signal. Prior passes deliberately kept it. Not proposed.

## P2 — growth/discoverability

- **OP3:** 7d 152 vs Aug-4’s 105 and Aug-2’s 88 — recovering toward the ~160–175 plateau. Weekly dip to 96 then back to 152 looks like noise, not a feed bug. No product change.
- Network outro / YouTube CTA blocks are repetitive by design; not filed as defects.

## What this pass proposes (nothing “shipped” — non-interactive)

1. `shows/prompts/models_agents_podcast.txt` — shape-ban UTH “Everyone talks/treats about…” contrast-cliché (verbatim + shape; no replacement menu). **A/B-listen.**
2. `shows/prompts/models_agents_podcast.txt` — shape-ban default teaser openers “Before we go” / “keep an eye on” / “Tomorrow keep an eye on”; substance-first teaser. **A/B-listen.**
3. `shows/prompts/models_agents_podcast.txt` — explicit ≤1 continuity callback; never speak episode numbers. **A/B-listen.**
4. `shows/models_agents.yaml` — UTH `section_markers` += `everyone talks about|everyone treats`; Tomorrow Teaser markers += a few substance-first alternates (`one thing to watch`, `worth watching next`, `watch whether`). Config-only / chapter metadata — no audio change from marker text itself.
5. `tests/test_models_agents_quality_pass.py` — drift-guard the prompt bans + marker alternates + `podcast_expand_below_target is false`.

## Deferred

- **OPERATOR DECISION (length — escalated after two misses; do not re-file 1600 without an explicit choice):** (A) `min_digest_words: 1600`, (B) UTH licensed-knowledge sentence/fact floor in digest prompt, or (C) accept ~1200–1400w and lower `min_podcast_words`. Never re-enable `podcast_expand_below_target`.
- Digest-driven / position-aware mid-section chapter titles (carried; ellipsis auto-titles + ep132/133 lost UTH prove markers remain brittle).
- Signature opener “okay let's pop the hood on” — intentional chapter signal; revisit only with A/B evidence.
- Network-wide: blog/RSS transcript sourced from pre-pronunciation text or Whisper (LoRA/RAG/JAXA class leaks).
- July-2 selection rebalance A/B (lab product/feature > preprints; arXiv ≤40%) — still not applied.
- Recap must synthesize, not splice dailies’ sentences (Sunday `weekly_summary_segment`) — carried.

## do_not_retry

None new (`do_not_retry: []` unchanged; no closed-unmerged rejection signal or revert evidence in this bundle).
