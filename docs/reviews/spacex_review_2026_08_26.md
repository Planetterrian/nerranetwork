# spacex — quality review (2026-08-26)

_Generated by the Grok-4.3 Show Review Agent (`scripts/run_show_review.py`). Proposed prompt/audio changes are listed in the PR body under "A/B-listen required" and are NOT applied — the operator applies and listens before merging (landmine #17)._

## Evidence

Snapshot on Ep072–081 `_tts.txt` + Whisper + `chapters_ep072.json`–`chapters_ep081.json` + live `shows/spacex.yaml` / hooks / prompts + ledger through 2026-08-15.

- **Length:** **0/10** scripts meet `min_podcast_words: 1300` (Ep072 1121 … Ep081 1066; peak Ep077 1239). Soft-skip floor 0.6×1300=780 still clears every episode. `podcast_expand_below_target: false`; digest expand + `min_digest_words: 1200` already on. No podcast-side length lever is legitimate (playbook + CLAUDE.md).
- **Boilerplate (10 transcripts):** expected network outro / identity 10/10; `on the ai front` 10/10 (required AI chapter anchor); `worth keeping an eye on` 7/10 (required Counterpoint opener — healthy); `before we go the` 6/10 (Tomorrow Teaser). **Engineering-anchor monoculture broken:** date-rotated `{engineering_anchor}` from `shows/hooks/spacex.py` produced mixed openers across the window (`the engineering angle` Ep072/076/080; `the engineering reality` Ep073/077/081; `from an engineering standpoint` Ep075/079; `in this engineering deep dive` Ep074/078) — prior 10/10 on one phrase is gone.
- **Chapters:** snapshot “10/10 clean” is wrong for Ep077. `chapters_ep077.json` inserts four mid-body **truncated headline** chapters outside the known section set (`SpaceX is hiring for natural gas…`, `Cape Canaveral studying…`, `NASA contracted SpaceX for $843 million…`, `Nvidia joins Starcloud’s…`) between Introduction and Counterpoint. Ep072–076, Ep078–081 use only known section titles. No `Title:`-prefixed titles in this window (Aug-15 scrub holding on the surface the snapshot now audits).
- **Digest heading integrity / fetch-filter:** 0 leaked `Title:` labels; 0 exclude_title_pattern hits — Aug-15 defense-in-depth + July-02 junk filter still holding.
- **Market Watch:** fires when a mid-body tape/price line exists (Ep072/074–077/079–081). Ep073 and Ep078 correctly omit Market Watch (sign-off-only / no mid-body tape). Closing-before-Market-Watch `where:end` ordering intact.
- **SPCX price-twice (structural):** still on most dailies that have any market line — Market Watch / body note **and** `closing_block` price ~30s apart (e.g. Ep072 “SPCX closed down 0.4%” then closing “SPCX is trading at $140 down 0.4%”; same class Ep074–081). Carried as operator decision since 2026-06-13 / escalated 08-05+; not re-proposed as a silent fix.
- **Entity bleed:** no new Memphis/Colossus-as-SpaceX operator mis-attribution in Ep072–081 Whisper (xAI/Cursor/orbital-node framing stays attributed). Ep053 archive still wrong; entity prompt never shipped (regression guard only).
- **Ep081 content shape:** Louisiana $100B / 10-pad story is restated across cold-open, Engineering Angle, Top-News body, and AI & Compute (Whisper shows the same $100B / 2027–2029 / NVIDIA-node facts multiple times). Continuity budget + zero-overlap rules did not stop same-episode multi-section echo on a single mega-story day.
- **Audience / cost:** OP3 downloads_7d **667**, downloads_30d **2500**, weekly trend [534, 573, 621, 667] — still climbing. Avg ~$0.42/ep.
- **Already-on levers (do not re-attack):** hook `engineering_anchor` rotation; digest FORMATTING de-seed + newsletter/chapters strippers; Market Watch pattern widen; `digest_expand_below_target` + deep-dive ~280–360w guidance; `story_recurrence: true`.

## Scoring prior predictions (2026-08-15)

| Metric | Verdict | Evidence |
|--------|---------|----------|
| digests with leaked `Title:` heading labels (snapshot Digest heading integrity) | **hit** | 0 leaked labels in last 10 digests (scrub deterministic) |
| non-special dailies with mid-body SPCX price/tape line and NO Market Watch chapter | **hit** | Ep072/074–077/079–081 fire Market Watch on widened forms; Ep073/078 correctly Closing-only |
| brand garbles (Grog\|Спейс-Экс\|Cloud Fable\|Global Star) in new youtube_videos.{ru,fr}.json titles | **partial** | No new garble class cited in this spacex window’s committed surfaces; network restore shipped 08-15 — hold and re-check on next dub publish batch |
| exact `from an engineering standpoint` 10/10 monoculture (escalated 08-15; not a formal pending prediction) | **hit (collateral)** | Date-rotated hook anchor broke monoculture; phrase no longer 10/10 |

## P0 — listener-facing bugs shipping today

### P0-1 — Ep077 mid-body headline chapters (known-section bypass)
`digests/spacex/chapters_ep077.json` lines for startTimes 75.0 / 112.9 / 183.1 / 225.1 are truncated story headlines, not `shows/spacex.yaml` `section_markers` titles (Introduction / Counterpoint / AI & Compute / Engineering / Market Watch / Teaser / Closing). Listener chapter UI and YT/podcast surfaces get spam titles. Distinct from the healed `Title:`-prefix class (Aug-15): no `Title:` token, but same listener-facing failure mode. Ep072–076 and Ep078–081 clean.

**Proposal:** strengthen chapter post-process to drop (or fold into Introduction) any chapter whose title is **not** in the show’s known section-title set when it looks like a truncated headline (ellipsis / em-dash tail / bare news clause). One-time cleanup of committed `chapters_ep077.json`. Drift-guard: no committed `chapters_ep*.json` title outside the known set. Code/metadata only — no audio.

## P1 — quality ceiling

### P1-1 — Chronic under-length — ESCALATE (do not re-file)
**0/10 ≥1300w** this window (worse than the 3–6/10 bands of July–early August). Same digest-ceiling diagnosis; podcast expand banned network-wide; digest expand + deep-dive word guidance already on. Conditional “7+/10 if deep-dive lever ships” predictions are playbook-forbidden. **Operator decision (third+ escalation):** accept the grok-4.3 daily length plateau for SpaceX, or pursue further **digest-substrate only** levers (`min_digest_words` raise, full-text article fetch, licensed-knowledge section floors). Do not re-propose podcast expand, podcast word floors, or “if lever ships” length predictions.

### P1-2 — SPCX price spoken twice — ESCALATE (do not re-file)
Still structural on dailies with a market line (Market Watch / body + `closing_block`). Escalated since 2026-06-13; multiple reviews carried the same unshipped coupled A/B. **Operator decision:** Market Watch qualitative (no repeated number) + single precise quote only in `closing_block` (coupled prompt + closing A/B-listen), **or** accept double price as brand and close the item. Stop silent re-proposal.

### P1-3 — Ep081 same-story multi-section echo
Whisper Ep081 restates the Louisiana $100B / 10-pad / 2027–2029 / orbital NVIDIA-node cluster in cold-open, Engineering Angle, body, and AI & Compute. Violates digest “each story in exactly ONE section” spirit and podcast continuity budget in practice on mega-story days. `story_recurrence` addresses cross-episode repeats, not intra-episode section echo.

**Proposal (prompt, A/B-listen):** shape-only reinforcement in digest + podcast — when one story dominates the day, full depth in **one** primary section; other sections get at most a one-clause pointer, never a second full retelling. **No quotable example sentence** (de-seed-by-shape rule). Verbatim ban on repeating the same dollar figure / pad count / year target outside the primary section.

### P1-4 — Entity-discipline regression guard (carried)
0 clear mis-attrs Ep072–081; Ep053 archive still wrong; prompt never shipped. Low urgency; keep as A/B regression guard only — do not treat as open P0.

## P2 — growth / discoverability

- OP3 trajectory remains the network growth story (667/7d, 2500/30d, steady weekly climb). RU Shorts funnel pilot stays the highest-leverage surface; no new audio change indicated.
- `api/shorts_ab.json` “collecting” after operator-ended 2026-08-14 experiment — report builder should mark ended (render/report only; carried).
- NASA public-domain b-roll pool vs retired generated-motion Shorts — render-side follow-up, not audio (carried).
- Specials runbook: when forcing `flight-14-catch-reaction` / earnings-class deep dives, do not stack on the daily’s publish day (Ep059 lesson); richer `spacex_deep_dive.txt` briefs so specials clear `deep_dive.min_podcast_words: 2400` — digest/brief substrate only (carried).

## Deferred

- OPERATOR DECISION: accept grok-4.3 daily length plateau vs further digest-substrate-only levers — do not re-propose podcast-side length.
- OPERATOR DECISION: SPCX price-once (qualitative Market Watch + single closing quote) vs accept double price as brand — coupled A/B or close.
- Q2/special path: richer deep-dive briefs + publish-day isolation for time-critical specials.
- Entity-discipline prompt (xAI/SpaceX) — regression guard, A/B, carried.
- `shorts_ab.json` status flip to ended; NASA b-roll pool — non-audio.
- Scheduler forensics for any missed Sunday `weekly_summary_segment` (June-28 class) — operator.

## What this pass would ship (proposals only — non-interactive)

Code/metadata: Ep077 chapter cleanup + known-section allowlist guard in chapter post-process + drift tests. Prompt (A/B): intra-episode mega-story single-section rule (digest + podcast), shape-only. No length or price-once prompt re-files.
