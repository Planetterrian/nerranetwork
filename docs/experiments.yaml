# Growth levers in flight — the dashboard's experiments registry.
#
# Every change that was shipped TO MOVE A NUMBER gets an entry here the
# day it ships, with the date the result becomes readable and (where the
# generator supports it) a named live metric so the dashboard can show
# the current value against the baseline. This makes "what are we testing
# and when do we decide" a rendered surface instead of tribal knowledge —
# the same discipline as the review ledgers, applied to growth work.
#
# Consumed by scripts/generate_dashboard.py::build_experiments_section.
# Schema per entry:
#   id:        unique slug
#   title:     short human name
#   area:      retention | reach | subscribers | funnel | quality
#   shipped:   YYYY-MM-DD the change reached production
#   readout:   YYYY-MM-DD the earliest honest read (or decision date)
#   status:    reading   — accruing data, do not stack another change on
#                          the same metric until readout
#              decide    — readout date is a DECISION with criteria below
#              done      — resolved; keep for the record, dashboard greys it
#   metric:    (optional) live-metric key the generator computes:
#                long_form_median_avp_en_14d   — EN long-form median
#                                                averageViewPercentage, 14d
#                fr_short_vpd_max              — best FR short_vpd (policy)
#                ru_short_vpd_max              — best RU short_vpd (policy)
#                channel_views_wow_ru          — RU channel views, last 7d
#                                                vs prior 7d (%)
#                channel_views_wow_en          — same for EN
#                dub_fragment_title_share_14d  — share of RU/FR window-
#                                                Short titles starting
#                                                mid-sentence, 14d
#              Unknown/absent metric renders as null — never 0.
#   baseline:  (optional) the number when the change shipped
#   target:    (optional) what success looks like
#   criteria:  (optional, decide entries) the decision rule
#   notes:     (optional) one line of context
#
# Prune done entries older than ~a quarter.

experiments:
  - id: cold-open-retention
    title: "Cold open — hook is the first thing spoken"
    area: retention
    shipped: 2026-07-30
    readout: 2026-08-15
    status: done
    criteria: >
      RESOLVED 2026-08-15 — POSITIVE. long_form_median_avp_en_14d 11.3 -> 13.4 (+2.1pp, ~19% relative). The cold open stays; no revert. Long-form retention remains far below Shorts (~72-77%), so the next retention lever is content-shape, not the intro.
    metric: long_form_median_avp_en_14d
    baseline: 11.3
    target: "long-form median AVP off the ~11% floor"
    notes: >
      Ten seconds of theme + a 35-word greeting used to push the first
      fact to ~second 28; Shorts (which skip the intro) hold ~42% on the
      same audio. Audio change — was A/B-listened per landmine #17.

  - id: staggered-shorts
    title: "Staggered Shorts — #1 at publish, rest at channel prime time"
    area: reach
    shipped: 2026-08-06
    readout: 2026-08-13
    status: done
    criteria: >
      RESOLVED 2026-08-15 — ADOPTED. channel_views_wow_ru +29.7% in the read window with the sweep healthy and zero stranded Shorts. Honesty note: the ru-3-short-band shipped in the same window, so the WoW lift is confounded — adopted on mechanism (scheduler flips reliably, spread confirmed in publish timestamps) rather than on the confounded headline number.
    metric: channel_views_wow_ru
    target: "RU/EN Short views up WoW with unchanged supply"
    notes: >
      publishAt scheduling; EN 17/21/23 UTC, RU 15/18/20, FR 17/19/21.
      First full day 2026-08-07. Confounder note: reads channel views,
      so a big news day can mask it — check views-per-video too.

  - id: ru-3-short-band
    title: "RU 3-Short band on spacex + FF"
    area: reach
    shipped: 2026-07-30
    readout: 2026-08-13
    status: done
    criteria: >
      RESOLVED 2026-08-15 — POSITIVE. ru_short_vpd_max 60.9 -> 72.5 with 50% more supply: vpd GREW while supply expanded, so the RU audience was not saturated. Band stays; ceiling remains MAX_SHORTS_PER_EPISODE.
    metric: ru_short_vpd_max
    baseline: 60.9
    target: "short_vpd holds or grows with 50% more supply"
    notes: >
      The supply ladder's 20-vpd band. If vpd holds while supply grew,
      the audience wasn't saturated; if it drops proportionally, it was.

  - id: dub-window-headlines
    title: "Complete headlines for RU/FR window-Shorts"
    area: quality
    shipped: 2026-08-07
    readout: 2026-08-14
    status: done
    criteria: >
      RESOLVED 2026-08-15 — METRIC HIT (0.66 -> 0.10 fragment share). Residual quality defects the metric cannot see (brand garbles: 'Grog 4.6', 'Спейс-Экс', 'Cloud Fable') were fixed separately the same day via restore_brand_names in engine/translate.py. Keep the fragment metric on the dashboard as a regression watch.
    metric: dub_fragment_title_share_14d
    baseline: 0.66
    target: "fragment share of window-Short titles near zero"
    notes: >
      Was ~2/3 of @NerraRU Short titles once the 3-Short band shipped
      (mid-sentence transcript slices). Grok headline w/ clause-trim
      fallback; title-only metadata.

  - id: fr-channel-decision
    title: "@NerraFR — keep or pause the dub channel"
    area: reach
    shipped: 2026-07-21
    readout: 2026-08-25
    status: done
    metric: fr_short_vpd_max
    baseline: 0.26
    criteria: >
      Keep if any FR show reaches short_vpd >= 1.0 or organic subscriber
      traction appears by 2026-08-25; else pause FR dubs
      (youtube.dub_languages: [] on the four shows — one line,
      reversible). FR PODCAST tracks are decided separately via the
      per-language OP3 instrument.
    notes: >
      53 of 72 launch-window uploads had zero recorded views. Clean
      analytics only since 2026-07-29 (the glob fix), hence the late
      decision date rather than a same-day verdict.
    outcome: >
      READ 2026-09-02: KEEP @NerraFR. fr_short_vpd_max 0.26 -> 16.0; FF-FR 4.3k views/28d (median 65/Short, +24 subs), spacex-FR 3.7k. MIT-FR is dead (140 views/25 Shorts) — see mit-dubs-cull.

  - id: dub-long-form-probe
    title: "RU+FR long-form dubs on tesla / spacex / FF (operator override)"
    area: reach
    shipped: 2026-08-07
    readout: 2026-08-21
    status: done
    criteria: >
      RESOLVED 2026-08-15 — NEGATIVE, ended 6 days early per the experiment's own exit rule: ru_long_vpd 1.52 vs the 2.0 floor, 52 forced RU/FR longs earned 461 total views (~9/video) and a net -2 subscribers — the July demotion numbers repeated almost exactly. dub_force_long_channels removed from tesla/spacex/FF; the adaptive policy's shorts-only tier decides again. Do not re-probe without a materially different format (e.g. native-RU presentation rather than a dubbed EN render).
    metric: ru_long_vpd_max
    baseline: 2.0
    target: >
      RU/FR long_vpd clearing the RU floor (2.0) with retention above the
      ~9% the July demotion measured; watch fr_long_vpd_max too.
    notes: >
      Operator-directed: dub_force_long_channels pins long-form ON for
      the big three on both dub channels regardless of tier; the Shorts
      supply ladder is untouched. Overrides the July RU long demotion
      (~9% retention, ~11 views/video) to give dubbed long-form a
      second, deliberate read. If the readout repeats the July numbers,
      remove the YAML flags and let the policy decide again.

  - id: shorts-progress-bar
    title: "Shorts progress bar (all channels)"
    area: retention
    shipped: 2026-08-07
    readout: 2026-08-21
    status: done
    target: >
      Short median AVP up vs the ~44% pre-bar level (compare EN+RU short
      retention before/after 08-07 in youtube_stats).
    notes: >
      Thin animated cyan bar at the top of every Short — visible time
      remaining, the standard short-video retention device. Render-only;
      per-show opt-out youtube.shorts_progress_bar: false. NOTE: overlaps
      the staggered-shorts readout window — judge by retention (AVP),
      which stagger does not touch, not by views.
    outcome: >
      No isolated instrument (shipped alongside other render changes); Shorts AVP rose through the window. Kept.

  - id: earnings-special-format
    title: "Event specials — earnings/flight reaction episodes"
    area: reach
    shipped: 2026-08-05
    readout: 2026-08-12
    status: done
    criteria: >
      RESOLVED 2026-08-15 — NEGATIVE. The Q2 special (spacex Ep59) was the WORST RU performer in its neighbourhood by three orders of magnitude (3 RU views vs 2,469 for Ep60) and second-lowest EN long AVP (12.0%), partly because it landed on a 9-upload day (3/day norm). Verdict: specials do not outperform dailies as published; keep them rare, give each its OWN publish day, and enforce the deep-dive brief floor (Ep59 shipped 1775w vs its 2400 floor). Applies to the queued flight-14-catch-reaction special.
    target: "special out-performs the daily episodes around it"
    notes: >
      Ep059 (Q2 earnings) is the pilot: compare its 7d views/downloads
      against Ep057/058/060. flight-14-catch-reaction and
      q3-2026-earnings are pre-staged in the spacex deep-dive queue.

  - id: site-outro-cards
    title: "Site-showcase endings — outro card + Shorts site strip"
    area: funnel
    shipped: 2026-08-09
    readout: 2026-08-23
    status: done
    target: >
      utm_content=outro sessions appearing in api/funnel.json (QR scans)
      and click-through from video surfaces up vs the pre-08-09 level;
      newsletter captures from src-youtube-* tags are the end goal.
    notes: >
      Long-form videos end on a card built from real nerranetwork.com
      screenshots (show page + network home) with a QR to the
      funnel-tagged show page; Shorts end cards gain the home page's
      show-grid strip. Render-only (outside landmine #17); per-show
      opt-outs youtube.outro_card_enabled /
      youtube.shorts_end_card_site_panel. Screenshots are committed
      (assets/site_screens) and refreshed by
      scripts/capture_site_screens.py.
    outcome: >
      No isolated instrument; outro sessions are attributable in funnel.json (utm_content=outro). Kept.

  - id: dp-pod-script-model-45
    title: "DP Pod script stage on grok-4.5 — SUPERSEDED by network 4.6 upgrade"
    area: quality
    shipped: 2026-08-10
    readout: 2026-08-24
    status: done
    baseline: "27/30 episodes with zero exclamation points; ~30% median digest-to-script 10-word shingle overlap (Ep22 65%)"
    target: >
      Scripts stop reading the brief verbatim (<=15% median shingle
      overlap) and the delivery spec finally lands (>=2 exclamation
      points in most episodes, voice tags actually used) with NO new
      facts beyond the digest.
    criteria: >
      SUPERSEDED 2026-08-18 — the operator-directed network-wide grok-4.6
      upgrade moved dp_pod's script stage (and every other stage) to 4.6,
      ending the 4.5 arm early. The 08-10..08-18 4.5 episodes are still a
      usable partial read: score the shingle-overlap/exclamation ledger
      predictions against them at the original 08-24 date, then judge 4.6
      under network-grok-46-upgrade.
    notes: >
      Was the first show on the July-31 script-stage model knob. Audio
      change — landmine #17 A/B applied at ship time.

  - id: youtube-chapters-live
    title: "Seek-bar chapters ship in every long-form description"
    area: reach
    shipped: 2026-08-12
    readout: 2026-08-26
    status: done
    metric: long_form_median_avp_en_14d
    baseline: 13.4
    target: >
      Long-form median AVP up vs the 13.4% (14d) baseline, and
      chapter-driven "Key moments" appearing in search — chapters had
      NEVER shipped (0 of 1,333 committed chapter files started at
      0:00, so the formatter discarded every block).
    notes: >
      Synthetic localized "0:00 Intro" line prepended when chapter 1
      starts after the music intro (clamped when it starts <10s).
      Metadata-only — outside landmine #17. Confounded with the
      caption-hold + end-card caption fixes shipping the same day;
      read AVP as the joint effect of the retention batch.
    outcome: >
      READ 2026-09-02 (lag-aware 14d windows): EN long-form median AVP 13.4 -> 13.8 — small positive, inside noise; chapters ship and seek-bar 'Key moments' exist, but retention is not the axis they move. Kept (zero cost, real navigation value).

  - id: shorts-caption-continuity
    title: "Shorts captions hold through pauses + stay visible over the end card"
    area: retention
    shipped: 2026-08-12
    readout: 2026-08-26
    status: done
    target: >
      Shorts median averageViewPercentage up vs the 50.8% EN / 43.6% RU
      (14d) baselines; specifically less drop-off in the final 3 s
      (end-card window) once retention curves are fetched.
    notes: >
      Per-word ASS cues extend to the next word's start (card no longer
      flickers off 10-20x per Short at breaths) and the subtitles stage
      runs after the end-card overlay (last ~3 s of speech kept its
      text hidden under the opaque CTA card). Render-only — outside
      landmine #17.
    outcome: >
      READ 2026-09-02: Shorts median AVP 49.4% (pre) -> 53.8% in the motion-era cohort (n=99), 51-54% since. Positive; kept.

  - id: shorts-motion-ab-stalled
    title: "Shorts motion A/B — ENDED (operator, 2026-08-14): unreadable"
    area: retention
    shipped: 2026-07-30
    readout: 2026-08-14
    status: done
    criteria: >
      RESOLVED 2026-08-14 — operator chose (b): formally ended as
      unreadable. The treatment arm froze at n=4 when
      shorts_ab_enabled: false landed via the merged spacex review PR
      #973; final data (4 vs 12) is not significant and stills remain
      the network default (no Grok Video spend on Shorts). The four
      render items the verdict was holding are now shipped on their own
      merits: progress bar (A3, shipped earlier), Shorts transition
      split (A2), punch-in easing (E1), long-form closing beat (D2) —
      all in the scene-cadence-36-slots reading window.
    notes: >
      The A/B machinery (engine/shorts_ab.py, the nightly report) stays
      dormant and re-usable; kb_extended=False keeps reproducing the
      legacy control render if a future experiment wants it.

  - id: scene-cadence-36-slots
    title: "Long-form scene cadence — input dedup + 36-slot cap + adaptive Ken Burns"
    area: retention
    shipped: 2026-08-14
    readout: 2026-08-28
    status: done
    metric: long_form_median_avp_en_14d
    target: >
      Long-form median AVP up vs the 13.4%/11.4% (14d) recent band. A
      17-min episode now changes scene every ~29 s instead of holding
      85+ s, the accelerating open survives, and Ken Burns speed no
      longer collapses on long holds (0.006/s adaptive amplitude).
      Reads jointly with youtube-chapters-live (both target the same
      metric; this is the render half).
    notes: >
      Enabled by ffmpeg input dedup (one -i per unique image + split),
      which removed the per-slot demuxer cost behind the old 24-slot
      cap. Render-only — outside landmine #17.
    outcome: >
      READ 2026-09-02: shares the AVP metric with chapters (13.8); the curves say long-form AVD is ~80 s regardless of length, so scene cadence past the first minute cannot register on AVP. Kept for visual quality; the open-cliff entry is the honest instrument.

  - id: mit-script-model-46
    title: "Modern Investing script stage on grok-4.6 — ABSORBED into network 4.6 upgrade"
    area: quality
    shipped: 2026-08-15
    readout: 2026-08-29
    status: done
    baseline: "median script 1535w against an 1800w floor; 9 of the last 10 episodes below it"
    interim: >
      2026-08-18, first 3 episodes: 2256/2366/2078 words — all clear the
      1800 floor, against 1441-1576 in the four before. Factuality audit
      traced every spelled-out number to its digest; no invented tickers.
      Tracking to a HIT; hold to the 08-29 readout for sustain.
    target: >
      Median script length reaches the 1800w floor (or at least clears
      1700w) with NO facts, tickers, prices or performance figures beyond
      what the digest carries. Length has been attacked at the digest
      substrate since July 24 and moved only 1382w -> 1535w, which points
      at the script stage as the remaining ceiling.
    criteria: >
      ABSORBED 2026-08-18 into the operator-directed network-wide 4.6
      upgrade (the explicit podcast_model pin was removed — the network
      default now equals what this A/B was trialling, and the digest
      moved too, so the script-vs-digest isolation this experiment relied
      on no longer exists). At 08-29 still score the length target
      (median script vs the 1800w floor) from the 08-15+ episodes; the
      factuality check transfers to network-grok-46-upgrade, where MIT
      remains the revert trigger.
    notes: >
      Ran 3 days as a script-stage-only A/B before the network upgrade
      landed. The MIT rule carries forward unchanged: any invented
      number/ticker/price reaching a script is an immediate revert.

  - id: network-grok-46-upgrade
    title: "Network-wide grok-4.6 — all LLM stages off 4.3 (operator-directed)"
    area: quality
    shipped: 2026-08-18
    readout: 2026-08-18
    # 'done' is the register's closed state (the dashboard coerces unknown
    # statuses to 'reading', which would render this as still active);
    # the outcome — REVERTED same day — is the notes' first line.
    status: done
    baseline: >
      grok-4.3 on digest/fetch/scripts/synth/titles/reviewer; 30d LLM
      token spend ~$25 (would be ~$47 at 4.6 rates on the same tokens);
      script under-floor share 81% network-wide (MIT 9/10 under 1800w);
      episode-reviewer FACTUAL_ERRORS flags at the pre-upgrade rate
      (establish from review_report history at readout).
    target: >
      Script length off the 4.3 plateau (MIT median clears 1700w+; the
      chronically-short shows' under-floor share drops), reviewer
      FACTUAL_ERRORS flag rate NOT above the pre-upgrade baseline, and
      refusal-fallback fires stay rare. LLM cost line lands ~$45-50/30d.
    criteria: >
      OPERATOR OVERRIDE of the July-31 digests-stay-on-4.3 decision
      (explicitly directed 2026-08-18, risks acknowledged: 4.6's
      confident-hallucination profile is unmeasured for this network).
      REVERT TRIGGERS, checked daily via the audit, decided at readout:
      (1) any invented number/ticker/price in an MIT script — immediate
      one-line revert (model: grok-4.3 in _defaults.yaml); (2) reviewer
      FACTUAL_ERRORS rate clearly above baseline across shows; (3) fetch
      tool-calls (x_search/web_search) failing on 4.6. Also A/B-listen
      the first post-merge episode on 2-3 shows per landmine #17 — every
      script's prose changes with this merge.
    notes: >
      Everything moved together: digest/fetch, scripts (dp_pod's 4.5 and
      MIT's 4.6 pins removed — superseded/absorbed), synth, titles,
      reviewer, review agent, restock; refusal fallback repointed
      grok-4.20-reasoning -> grok-4.3 (different snapshot than the new
      primary, retires the aging 4.20 dependency). Translation was
      already pinned 4.6 same-day. ~+$22-25/mo at current volume.
      REVERTED SAME DAY (2026-08-18, operator-directed): the trigger was
      OPERATIONAL, not on the registered list — 4.6 digest latency ran
      5-10x 4.3 on every show measured (planetterrian 44s -> 420s, MAB
      36s -> 242s, dp_pod 55s -> 225s, first_principles 36s -> 205s) and
      on the four largest digest prompts (tesla, spacex, omni_view,
      modern_investing) requests exceeded the 300s client timeout or were
      server-disconnected. Nested SDK+tenacity retries amplified each
      stall into a 41-45 min burn; 7 of 12 shows failed on day one
      (2026-08-18 07:37-10:33 UTC). Rolled back to grok-4.3 everywhere
      except the translation pin (already 4.6 via the alias freeze, small
      chunks, no timeouts observed). The hallucination-profile question
      is UNANSWERED — the experiment never reached an editorial readout.
      Any retry follows docs/model_upgrade_playbook.md: one show first,
      digest-latency gate before network-wide.
  - id: staged-grok-46-trial
    title: "grok-4.6 staged trial — dp_pod script, synth, reviewer (FPD+UC arms withdrawn 2026-08-27)"
    area: quality
    shipped: 2026-08-18
    readout: 2026-09-01
    # Closed at readout (2026-09-01). OUTCOME: PARTIAL ADOPTION.
    # Adopted for good: dp_pod script-stage (14/14 days incl. the 08-27
    # bad-service day, LV 7.2-8.3, 2 factual flags in 10 reviewer-days —
    # at/below every 4.3 news show), synth, and the reviewer (effort LOW
    # since 08-21). Withdrawn arms stay withdrawn (FPD/UC — 4.6 digest
    # stall/server-disconnect signature). The quality and operational
    # verdicts split cleanly: 4.6 writes better and did NOT elevate
    # factual flags cross-show; its service reliability on long
    # digest-shaped calls remains the blocker. Successor:
    # grok-46-wave2-scripts (script-stage only, the shape that survived).
    status: done
    interim: >
      2026-08-27 (operator-directed): the first_principles and
      unintended_consequences WHOLE-SHOW arms are withdrawn — on the
      GitHub-cron outage day, FPD's grok-4.6 digest calls stalled ~260s
      and the server disconnected without a response (three tenacity
      attempts, on both of its runs) and UC died on both of its runs;
      both shows missed their slots twice in one day, the same
      operational signature that forced the Aug-18 same-day network
      revert. Both shows are back on the grok-4.3 network default (the
      playbook's latency gate: an arm that costs episodes is over,
      whatever its editorial promise). The 4.3-vs-4.6 narrative-length
      question for FPD/UC is UNANSWERED, not answered negative — a
      retry needs xAI-side latency to be demonstrably fixed first. The
      dp_pod script-stage arm and the synth + reviewer stages continue
      to the 2026-09-01 readout unchanged.
    baseline: >
      All stages on grok-4.3 after the same-day revert of the network-wide
      4.6 upgrade. dp_pod on 4.3: zero exclamation points in 27/30
      episodes, Ep22 65% verbatim digest paste. FPD/UC: the accepted
      grok-4.3 narrative-length plateau (briefs 848-1116w vs 1600 ask).
      FPD/UC digest latency on 4.3: ~30-50s.
    target: >
      dp_pod scripts follow the delivery spec (exclamations/voice tags
      present, shingle overlap with the digest clearly below the 65%/42%
      worst cases). FPD/UC briefs come off the 4.3 plateau without the
      banned podcast-side retries. No latency regressions: stage durations
      in metrics_ep*.json stay under 50% of NERRA_LLM_TIMEOUT_SECONDS
      (600s) and zero 'Slow LLM completion' warnings after week one.
    criteria: >
      Operator-directed staged rollout per docs/model_upgrade_playbook.md
      (the first trial run under it). SCOPE: dp_pod llm.podcast_model
      ONLY (digest stays 4.3); first_principles + unintended_consequences
      whole-show (narrative — the brief is model-knowledge prose);
      synth_model + reviewer_model in _defaults. Every daily NEWS show's
      digest/fetch stays grok-4.3. REVERT TRIGGERS, any one suffices,
      revert = delete the named pin: (1) an invented fact/date/name in an
      FPD or UC story (these are factual histories) — judged against the
      other shows' FACTUAL_ERRORS under the SAME 4.6 reviewer, never
      against pre-08-18 history (the reviewer instrument changed this
      date too); (2) a digest/script stage exceeding ~300s repeatedly or
      any request timeout on a trial show; (3) a missed episode
      attributable to the model; (4) dp_pod A/B-listen verdict against
      the 4.6 scripts (landmine #17 — prose changes with the model).
    notes: >
      Follow-up to network-grok-46-upgrade (reverted for latency, not
      quality — the quality question was never answered). Trial shows
      were picked benefit-over-risk: dp_pod's dialogue writing is its
      product and its 4.3 failures are documented writing-model
      limitations; FPD/UC have no news fetch so a stronger model is the
      only sanctioned length/depth lever; synth + reviewer are
      analysis-stage, operator-gated or non-published. The four
      biggest-prompt news shows (tesla, spacex, omni_view, MIT) stay
      OFF 4.6 — highest hallucination stakes and the four that timed
      out on outage day. NERRA_LLM_TIMEOUT_SECONDS raised 300 -> 600 in
      run-show.yml (worst-case per stage 3 x 600s = 30 min, still under
      the 50-min watchdog). Cost: ~+$3-5/mo at trial scope.
    interim: >
      2026-08-19 first episodes: all three shipped, zero envelope
      issues (digest 53-236s). dp_pod: verbatim digest overlap 1.8%
      (best ever; 4.3-era 20-65%), exclamations/voice tags present,
      LV in band — the clearest win. UC 2354w/15:38 is ON-SPEC for the
      first time (its prompt always asked 2200-2800w; the 4.3 plateau
      under-delivered and the network tuned around it). FPD overshot
      its own 1800-2400 spec (3204w) via the "longer is better" clause
      — capped at ~2600 same day (A/B-listen applies); its
      anti-repetition retry also fired once — watch for chronic firing.
      Reviewer FACTUAL_ERRORS read pending (4.6 reviewer, cross-show).
      2026-08-20 OPERATOR LISTEN VERDICT on FPD: repetitive, not tight
      like before — the trial's first negative ear. Diagnosis: not
      verbatim looping (0 near-dup sentence pairs in Ep075/076) but
      CONCEPTUAL restatement — both FPD prompts carried
      write-past-the-floor / restate-in-plain-words scaffolding tuned
      for 4.3's chronic under-delivery, and 4.6 obeys it literally
      (briefs 1,432w -> ~2,900w; scripts ~1,400w -> 3,000+w; runtime
      doubled to ~20 min). Fix shipped same day: both prompts rewritten
      to bounded ranges + say-each-fact-once discipline (brief
      1,300-1,700w; script 1,500-1,900w, stop ~2,100), retry floors
      retuned under the bands (min_digest_words 1200, min_podcast_words
      1400). FPD STAYS on 4.6 — the verdict indicts the prompt-model
      interaction, not the model; if the next 2-3 episodes still read
      repetitive at the tightened spec, THEN revert the model pin.
      UC untouched (on-spec, no complaint); dp_pod Ep041 swung short
      (1,101w vs 1,550 target, LV 7.9) — single-day watch item.
      2026-08-21 retune VERDICT: Ep077 landed in band (brief 1,752w,
      script 1,960w, 0 near-dup pairs, 13:16 vs 20:00) — say-it-once
      worked; FPD stays on 4.6. New watch: FPD digest ran 506s under
      the tightened band (vs 190-236s before) — close to the 600s
      timeout; if it trends up, the model pin is the lever. Same day:
      the 4.6 REVIEWER at default effort timed out the whole daily
      audit (no report written); reviewer now runs reasoning_effort
      LOW + the audit's AI loop has a wall-clock budget
      (REVIEW_AI_BUDGET_SECONDS, default 1200s) so the report always
      ships. FACTUAL_ERRORS rates from 08-21 on are 4.6-LOW — compare
      cross-show within a day, never across effort eras.
  - id: grok-46-funnel-and-ops
    title: "grok-4.6 on the funnel + ops surfaces — titles, restock, specials"
    area: growth
    shipped: 2026-08-19
    readout: 2026-09-05
    # Closed early with the trial readout (2026-09-01): ADOPTED. Titles,
    # restock, and the specials pin ran the full window without an
    # operational incident (small calls — the surface shape 4.6 handles
    # reliably). Title CTR/velocity deltas stay readable in youtube_stats
    # via the model-era stamps whenever wanted.
    status: done
    baseline: >
      YouTube title bundle, topic-queue restock, and deep-dive specials
      all on grok-4.3. EN long-form CTR/velocity per the youtube_stats
      trailing window; restock briefs at the 4.3 depth the queues carry
      today; the one shipped deep-dive special (spacex q2-2026-earnings)
      generated on 4.3.
    target: >
      Title bundle: long-form vpd / Shorts velocity on 4.6-era titles at
      least matches the trailing window (titles are metadata-only —
      outside landmine 17 — so this can simply run and be read).
      Restock: next restock cycle's briefs pass validation at the same
      or better rate. Specials: next forced spacex deep-dive ships on
      4.6 with no timeout (specials have no daily-slot pressure).
    criteria: >
      Rollback is per-surface and one line each: the three model
      defaults in engine/youtube_titles.py; NERRA_RESTOCK_MODEL env or
      the pin in scripts/restock_topic_queues.py; deep_dive.model in
      shows/spacex.yaml. Revert a surface if its output quality visibly
      regresses (title truncation/garbling, restock validation-failure
      spike, special timeout) — latency canary and envelope guards from
      the 08-18 outage apply everywhere.
    notes: >
      Wave shipped alongside per-stage model-era stamps
      (llm_digest_model / llm_script_model in metrics_ep*.json and
      per-stage model ids in credit files) so every future model
      question is answerable by query — the credit file's single
      file-level model label could not represent the trial's
      split-model episodes. Wave 2 (script-stage pins for omni_view /
      MAB / MIT + dp_pod comedy-spec re-tightening) is deliberately
      HELD for the staged-grok-46-trial readout 2026-09-01 per
      docs/model_upgrade_playbook.md.
  - id: grok-46-wave2-scripts
    title: "grok-4.6 wave-2 script pins — omni_view Steel Man + MAB teaching prose"
    area: quality
    shipped: 2026-09-01
    readout: 2026-09-12
    status: reading
    baseline: >
      Both shows' scripts on grok-4.3. omni_view: 3 factual flags in 10
      reviewer-days, Steel Man both-sides framing prone to template tics
      (the "strongest case" family). MAB: 2 flags/10d; teaching prose is
      the product. dp_pod's identical script-only pin is the control
      evidence: 14/14 days, LV 7.2-8.3, 2 flags/10d.
    target: >
      Reviewer factual flags at or below each show's 4.3 baseline under
      the same 4.6-low reviewer (cross-show, within-day comparisons
      only); zero missed slots attributable to script-stage 4.6 calls;
      audibly better delivery on the operator's A/B-listen.
    criteria: >
      SCRIPT STAGE ONLY — digests stay grok-4.3 (guarded). REVERT
      TRIGGERS, one line each (delete the podcast_model pin): (1) a
      script-stage stall/server-disconnect costing an episode its slot —
      the FPD/UC withdrawal signature; (2) factual flags clearly above
      the show's 4.3 baseline; (3) the operator's listen verdict.
      MIT is deliberately EXCLUDED from this wave: its 5-flags-in-10-days
      rate is DIGEST/data-side on 4.3 (contradictory trade counts, an
      invented listing venue) — a better script model cannot fix that
      and would varnish over it; MIT's digest factual quality is its own
      work item for the review rotation.
    notes: >
      dp_pod's comedy/banter spec re-tightening was considered for this
      wave and deferred to the review-agent rotation: dp_pod scripts have
      drifted ABOVE their length target on 4.6 (2,000-2,362w vs 1,550)
      and layering more delivery spec onto an already-long script without
      a listen-informed pass is the FPD lesson in reverse.
  - id: mit-verified-window-alpha
    title: "Modern Investing states verified-window alpha only"
    area: quality
    shipped: 2026-08-15
    readout: 2026-08-29
    status: decide
    baseline: "+9.28% across 45 trades spoken on air; the 10 honestly-measured trades were at -1.95%"
    interim: >
      2026-08-18: 2 of the first 3 episodes still spoke the blend —
      _build_benchmark_block was a second, unfixed call site and Ep138
      fused the two ("+9.28% across forty-five VERIFIED-window trades").
      Both blocks now read one source. Separately, the recompute that
      would retire this split was found to backdate entries and was
      fixed; its corrected dry run says -3.35% across 42 trades.
    target: >
      Every alpha figure spoken on air comes from the 10 (and growing)
      trades with pick-date-aligned benchmark windows, with the trade
      count in the same sentence. No blended lifetime alpha reaches air.
    criteria: >
      OPERATOR DECISION at readout: run
      `scripts/recompute_mit_benchmarks.py --apply` (needs market-data
      access) to rebuild the 35 legacy windows. That collapses the split
      — every trade gains entry_bar_date and the verified figure simply
      becomes the whole record. Until it runs, the show's headline is
      honest but built on n=10.
    notes: >
      The recompute has been an open operator item in the MIT ledger
      since 2026-07-03. This change stops the unrun backfill from
      silently flattering the on-air number in the meantime.

  - id: search-terms-loop
    title: "Live search queries feed titles + tags"
    area: reach
    shipped: 2026-08-17
    readout: 2026-08-31
    status: done
    baseline: "EN search share 15% of channel views (2026-08-17); terms: starship flight 14, fsd v14.1 lite, ..."
    target: >
      EN YT_SEARCH share of views up vs 15%, and search terms appearing
      in shipped titles/tags. The Analytics search-terms report now
      flows per-show (token-matched, conservative) into
      youtube_performance.json — the title prompt gets a LIVE SEARCH
      QUERIES line and uploads prepend the matched terms as tags.
    notes: >
      Channel-level terms are attributed to a show only when they share
      a meaningful token with its recent titles/hooks; unmatched terms
      are dropped rather than sprayed across the channel. Metadata-only.
    outcome: >
      READ 2026-09-02: EN YT_SEARCH share 15% -> 16% (2 weeks; flat within noise). Terms are flowing (4 shows carry matched queries into tags + the title hint). Kept as infrastructure; not a lever by itself.

  - id: long-open-cliff
    title: "Long-form open cliff — hold at the 5% mark"
    area: retention
    shipped: 2026-08-17
    readout: 2026-09-07
    status: reading
    metric: long_open_hold_5pct_en
    baseline: 0.51
    target: >
      Mean EN audienceWatchRatio at 5% elapsed above 0.55. The curves
      show ~half the audience gone inside the first 30-45 s while decay
      flattens after 25% — the open is the dominant remaining retention
      lever. This entry TRACKS the instrument (new dashboard card +
      metric); the accelerating open, chapters, and the motion batch are
      the levers currently in flight against it. Content-side changes
      to the spoken open are audio (landmine #17) and stay operator-gated.
    notes: >
      Curves accrue nightly for each channel's top 5 recent longs.
      Outliers worth studying: tesla-RU ep554 held 72% at 5%,
      spacex ep65 68% — both had thumbnail/title promises the opening
      delivered immediately.

  - id: mit-rules-based-era
    title: "Modern Investing restarts its track record on published rules"
    area: quality
    shipped: 2026-08-18
    readout: 2026-09-15
    status: reading
    baseline: "holds ran 0-6 sessions (median 3) against a five-day thesis; alpha conflated pick quality with pick weekday"
    target: >
      Every weekly hold spans exactly 5 sessions unless stopped out, so
      per-trade alpha is attributable to the pick. The on-air record is
      the era only, with its trade count in every sentence, and a listener
      can reproduce any reported number from the pick date, the symbol and
      public price data.
    criteria: >
      At readout, score: zero holds off the horizon; zero pre-era figures
      spoken; confidence ratings actually varying; every pick carrying an
      invalidation. If holds are still drifting, the exit rule is not
      being enforced somewhere and that is a bug, not a tuning question.
    notes: >
      The headline alpha goes to zero for about a week and then to a
      sample too small to claim an edge — that is the intended cost of a
      reproducible record, and the prompts require the show to say so.
      Prompt + audio change — landmine #17 A/B.

  - id: mit-options-positions
    title: "Modern Investing trades the options it teaches"
    area: quality
    shipped: 2026-08-19
    readout: 2026-09-19
    status: reading
    baseline: "0 of 61 simulated trades used an options structure, against options taught in 32 of the last 40 episodes"
    target: >
      At least one covered call or cash-secured put per ~10 episodes when
      the setup genuinely calls for one, each with a real recorded premium
      and an expiry-settled payoff a listener can reproduce from the
      underlying's close.
    criteria: >
      Keep if the structures are chosen for the right reason (income on a
      name worth owning / willingness to own lower) rather than to look
      sophisticated, and if every position carries a real quote. ANY
      position recorded with an estimated premium is a revert — that is
      the whole design constraint. If the show never picks one, the
      prompt guidance is too weak and needs a nudge, not a mandate.
    notes: >
      Early assignment is not modelled and the prompts require saying so.
      Audio change — landmine #17 A/B.

  - id: mit-learning-loop-honesty
    title: "Rule scoreboard stops manufacturing verdicts"
    area: quality
    shipped: 2026-08-19
    readout: 2026-09-19
    status: reading
    baseline: "5 identical RETIREMENT CANDIDATE verdicts from one undivided 10-trade sample, controlled against the disowned pre-era trades"
    target: >
      Zero verdicts from collinear rule sets; production-hygiene lessons
      out of the pick prompt entirely; and a FIRST genuinely scored rule
      once the era's rule set varies across ~a dozen trades.
    criteria: >
      The loop is allowed to say nothing — that is the fix. What would
      falsify it is a retirement flag appearing while every stamped trade
      still carries the same rule set.

  - id: mit-rule-rotation
    title: "Rule rotation gives the learning loop the variation it needs"
    area: quality
    shipped: 2026-08-20
    readout: 2026-10-01
    status: reading
    baseline: "2 rule sets across 15 stamped trades, differing by one rule, arising by accident — 0 rules scoreable, ever"
    target: >
      >=4 distinct rule sets within 20 era trades, every rule carrying
      both arms, and at least one genuinely scored rule by ~25 era trades.
    criteria: >
      If 0 rules are scoreable at readout despite the rotation running,
      the pool (6 rules) is too small and needs widening — not more
      waiting. If a rule scores well it is pinned automatically and stops
      rotating, which is the intended end state, not a bug.
    notes: >
      This is an intervention on the product, not just the measurement:
      some picks are now made without some rules. Proven and hand-pinned
      rules never rotate out. Rotation is a pure function of the episode
      number, so any pick's rule set is reproducible from the ledger.

  - id: mit-strategy-families
    title: "Alpha reported by approach, not only by sector"
    area: quality
    shipped: 2026-08-20
    readout: 2026-10-01
    status: reading
    baseline: "61 trades, 61 unique free-text strategy strings, 0 groupable"
    target: >
      >=90% of new picks carry an explicit family, and at least one family
      reaches 5 verified-window trades so the show can say on air which
      approach is actually earning its keep.
    notes: >
      Derived retroactively for the 61 historic trades so the record is
      groupable now. Inherits the window discipline — indicative-only and
      not for air until computed over verified windows.

  - id: network-review-catch-up
    title: "Late-finishing episodes finally get content-reviewed"
    area: quality
    shipped: 2026-08-20
    readout: 2026-09-10
    status: reading
    baseline: "4 shows unreviewed on 2026-08-19; a 32-episode network-wide backlog"
    target: >
      Zero episodes that finish after the audit window go permanently
      unreviewed. The backlog drains at <=10/run and stays at 0.
    criteria: >
      Watch the AI-review cost line while the backlog drains — if it
      spikes, lower CATCH_UP_MAX_PER_RUN rather than dropping the pass.
    notes: >
      Network-wide, not MIT-specific. The first run surfaced real findings
      that had been invisible, including high cross-episode repetition on
      MIT Ep139/140.

  - id: mit-methodology-correction
    title: "MIT accounts on air for the record that disappeared"
    area: quality
    shipped: 2026-08-20
    readout: 2026-09-15
    status: reading
    baseline: >
      Ep141-143 all announced the new era-scoped record; 1 of 3 said why,
      0 of 3 pointed at the rulebook or ledger, and 0 of 3 mentioned the
      +9.28%-across-45-trades figure the show had been quoting.
    target: >
      Exactly 3 episodes carry the correction and then it retires. No
      listener-value regression below 7.0 on those episodes, and
      measurable traffic to modern-investing-performance.html within 14
      days of the first airing.
    criteria: >
      A 4th airing means the self-retirement is broken. Flat page traffic
      means the on-air pointer is not working and needs rewording rather
      than repeating — do NOT extend the run to compensate.
    notes: >
      Deliberately narrow. Pipeline internals are out of scope and a
      drift guard fails if they leak into the spoken portion. The
      "published for anyone to check" claim required shipping the
      performance-page verification panel in the same pass — the ledger
      was built nightly into api/ and linked from nowhere.
      First airing was Ep144 (2026-08-20), not Ep145 — the episode
      shipped after the merge. It exposed two gaps, both fixed: the
      segment invited verification without naming a destination (so the
      page-traffic reading would have been meaningless), and it fused
      with the performance numbers into one paragraph.

  - id: nerra-daily-launch
    title: "Nerra Daily — the combined daily edition"
    area: reach
    shipped: 2026-08-21
    readout: 2026-09-22
    status: reading
    target: >
      Nonzero OP3 downloads on nerra_daily_podcast.rss by the readout,
      trending toward the network per-show median. The bet is that a
      ~2 h all-network edition captures listeners who will not subscribe
      to 13 feeds.
    criteria: >
      Zero measured downloads at readout (with Apple/Spotify submission
      done — operator checklist in docs/nerra_daily.md) means the
      surface is undiscovered, not unwanted: fix distribution before
      judging the product. Cost is ~$0.08/episode, so the kill bar is
      low-urgency.
    notes: >
      Audience was UNMEASURED at launch — fetch_op3_stats only walked
      shows/*.yaml, so the registry-only edition never appeared in
      api/op3_stats.json (fixed 2026-08-25, same pass that added the
      edition's cost to the dashboard rollup). First honest read needs
      a few weeks of post-fix nightly data.

  - id: nerra-daily-edition-titles
    title: "Nerra Daily — LLM-written whole-day edition titles"
    area: reach
    shipped: 2026-09-03
    readout: 2026-09-24
    status: reading
    target: >
      Every one of the first 14 editions was titled with SpaceX Daily's
      hook (the lead show), so the all-network product read as a SpaceX
      show in every directory listing and blog title. The links call
      now writes a one-line headline for the whole day; the lead hook
      stays the fallback. Target: per-episode OP3 downloads_7d on
      editions titled by the LLM at or above the SpaceX-titled baseline
      (Eps 6-14, 2-5 per episode), and edition_title_source == llm on
      at least 8 of 10 builds.
    criteria: >
      If validate_edition_title rejects the model's line on 3+ of 10
      builds, loosen the bounds before judging the idea. Zero measured
      movement with the feed still unsubmitted to Apple/Spotify means
      distribution, not the title, is the gate — same rule as
      nerra-daily-launch.
    notes: >
      Written metadata only (title, blog title, chapters title, OG
      card) — nothing Mira speaks changed, so outside the landmine-#17
      A/B gate. Shipped with the Sep 3 2026 review alongside the show
      notes timestamps.

  - id: chapter-title-cards
    title: "Long-form chapter title cards at every chapter boundary"
    area: retention
    shipped: 2026-09-02
    readout: 2026-09-23
    status: reading
    metric: long_open_hold_5pct_en
    baseline: 0.49
    target: >
      Hold at the 5% mark up, and — the real target — the 25% hold
      (curves) up: viewers who see what is coming next stay for it.
      Long-form AVD sits at ~80 s regardless of episode length; the
      video gave no visual cue of structure. Broadcast-style cards
      (title, top-left under the brand pill, ~4 s, fade in/out) now
      fire from the same chapters JSON that powers the seek bar.
    notes: >
      Render/metadata only (outside landmine #17). Auto-derived inside
      build_long_form_video from chapters_path; chapter_cards=[] disables.

  - id: shorts-4-band
    title: "4th Short per episode at >= 60 vpd (RU/FR science + space shows)"
    area: reach
    shipped: 2026-09-02
    readout: 2026-09-16
    status: reading
    metric: ru_short_vpd_max
    baseline: 55.17
    target: >
      RU + FR channel views up with the 4th Short holding >= 60% of the
      hook Short's median views (the 3rd holds ~80%: RU 'filled' median
      172 vs hook 214, n=180). Abort if the 4th clip's median falls
      under ~100 views on RU or the 3rd's median drops.
    notes: >
      Members at ship: FF-RU (1361 vpd), spacex-RU (1195), tesla-RU
      (663), FF-FR (165), spacex-FR (142), tesla-FR (93). +3 uploads/day
      on channels running 9-17 against the 30/day ceiling. Raises need
      two consecutive nights (shorts_pending/streak).

  - id: shorts-title-headline-only
    title: "Shorts titles: headline + #Shorts, no ' | show' tail"
    area: reach
    shipped: 2026-09-02
    readout: 2026-09-23
    status: reading
    target: >
      Views/Short up on EN (median 18 -> 25+). The 45-day top- vs
      bottom-quartile cut favoured SHORTER Short titles on every channel
      (RU 66 vs 70 median chars, EN 74 vs 81); the Shorts player already
      shows the channel name and truncates ~40 chars, so the tail spent
      the visible budget on a redundancy.
    notes: >
      Metadata-only. Long-form titles keep the show tail.

  - id: mit-dubs-cull
    title: "Modern Investing RU/FR dubs — decide: cull"
    area: quality
    shipped: 2026-09-02
    readout: 2026-09-09
    status: done
    criteria: >
      28d: MIT-RU 37 Shorts -> 393 views (median 6), 3 longs -> 5 views;
      MIT-FR 25 Shorts -> 140 views (median 4), 6 longs -> 30 views.
      That is ~$0.18/ep translation + two renders/day for ~20 views/day
      across both channels, while the same channels' science/space
      Shorts earn 100-400x that per clip. Operator decision: disable
      youtube.ru_dub_enabled + dub_languages on modern_investing (the
      audio RU/FR podcast tracks can stay if the OP3 per-language read
      justifies them — separate instrument).
    notes: >
      Same shape for models_agents_beginners EN Shorts (median 4
      views/Short) — but MAB long-form works (+6 subs), so the policy's
      1-Short floor is the right answer there, not a cull.
    outcome: >
      DONE 2026-09-02 (operator): youtube.ru_dub_enabled false + dub_languages []
      on modern_investing. Audio RU/FR tracks kept pending the OP3
      per-language read.

  - id: scene-briefs-narrative-imagery
    title: "Story-driven scene briefs — the pictures follow the narration"
    area: retention
    shipped: 2026-09-02
    readout: 2026-09-23
    status: reading
    metric: long_open_hold_5pct_en
    baseline: 0.49
    target: >
      Viewer complaints about imagery not matching the subject stop;
      long-form hold at 5% and Shorts AVP up. Mechanism: one Grok text
      call per episode writes a concrete, photographable scene per
      STORY (engine/scene_briefs.py); those briefs LEAD every Grok
      Imagine prompt (the static image_queries — 'cybertruck',
      'supercharger' — used to lead, so every Tesla episode shipped the
      same four pictures); one fresh 16:9 scene per story (cap 8, was a
      fixed 4) keyed to its chapter for the scheduler; the library
      blend is back to 8 and ON-TOPIC ONLY (>=1 shared token with hook +
      chapter titles) instead of padding with random older images.
    notes: >
      Cost: +~4 images/episode at $0.02 (+$0.08/ep, ~+$30/mo network)
      + one ~$0.005 text call. Per-show opt-out scene_briefs_enabled;
      counts via scenes_per_episode / short_scenes_per_episode.
      Render/metadata only (outside landmine #17). First A/B-eyeball:
      compare a tesla + spacex + FF episode's scenes_ep*/ against the
      digest headlines.

  - id: image-source-2k-and-story-matched-shorts
    title: "2K source images + Shorts open on the scene about THEIR story"
    area: retention
    shipped: 2026-09-02
    readout: 2026-09-23
    status: reading
    target: >
      Sharper stills (the 16:9 render prescales to 3840 wide for Ken
      Burns; a 1792-wide source was a 2.1x upsample before any zoom —
      the softness viewers describe); every Short now leads with the
      fresh scene whose brief best matches its own window text instead
      of whichever scene was generated first. Reads jointly with
      scene-briefs-narrative-imagery on Shorts AVP + long-form hold.
    notes: >
      Request size ladder 2K -> legacy -> none, so a revision rejecting
      2K can never cost an image (guarded). Zero extra cost at the
      standard model. DECISION for the operator (separate, cost-bearing):
      grok_image_model: grok-imagine-image-quality ($0.05 vs $0.02) on
      the three engine shows (FF, spacex, tesla) ≈ +$35/mo — the honest
      next step if 2K + story briefs don't end the complaints.
      2026-09-03 CORRECTION: the 2K `size` request was a NO-OP — the
      endpoint ignores `size`; every one of the 7,500+ committed gallery
      sidecars (dimensions probed from the real bytes) is 1280x720 /
      720x1280 on grok-imagine-image whatever was requested. Replaced
      by the documented `aspect_ratio` + `resolution: "2k"` fields
      (video-quality-round-3). The delivered width is now recorded per
      episode as grok_image_px_max; the first honest 2K episodes are
      2026-09-03's — 1280 there means the endpoint ignored the
      documented field too and the quality-tier decision moves up.

  - id: video-quality-round-3
    title: "Real 2K sources, salient-token library blend, chapter cards as a fit gate"
    area: retention
    shipped: 2026-09-03
    readout: 2026-09-23
    status: reading
    metric: long_open_hold_5pct_en
    baseline: 0.51
    target: >
      From the first-round readout (Tesla Ep592 frame check + the
      committed gallery sidecars): (1) the endpoint ignores `size`, so
      the request now carries the documented `aspect_ratio` +
      `resolution: 2k` with the delivered width recorded as
      grok_image_px_max — target 2048 on every 09-03 episode; (2) the
      library blend counted 'tesla' / 'optimus' / 'cinematic' (95-100%
      of the library) as relevance, so min_overlap=1 matched every image
      and a Solar-Roof episode still opened its chapters on a pier of
      Model S — overlap is now on SALIENT tokens (document-frequency
      > 20% removed), target: library scenes share a story token with
      the day's chapters or are not used; (3) a chapter card is a FIT
      gate on engine.titles.CHAPTER_CARD_MAX, never a clip — clipped
      first-sentence titles and structural labels get no card (Ep592
      rendered "Several projects pair storage with solar to").
    notes: >
      Render/metadata only (outside landmine #17). Cost unchanged at the
      standard model; 2K is not priced separately in the docs (flat
      per-image) — verify on the 09-03 credit files. Guards:
      tests/test_scene_briefs.py (TestRequestSizeLadder,
      TestOnTopicLibraryBlend), tests/test_video_commands.py
      (TestChapterTitleCards::test_only_clean_headlines_earn_a_card).

  - id: dp-pod-youtube-shorts
    title: "DP Pod on YouTube — Shorts-only, first video surface"
    area: reach
    shipped: 2026-09-04
    readout: 2026-09-25
    status: reading
    baseline: "0 YouTube views; OP3 113 downloads/30d, ~27/week, 3.5/episode (below network median); Spotify 0 followers"
    target: >
      Shorts velocity earns its own tier line in api/youtube_policy.json
      (short_vpd measured, not null) and OP3 weekly downloads move off the
      ~27 plateau; the funnel report shows dp_pod Shorts -> thedppod.html
      sessions.
    criteria: >
      Decide at readout: keep Shorts-only, let the adaptive policy add
      long-form if long_vpd earns it, or revert (youtube.enabled: false,
      one line) if Shorts from a two-voice dialogue clip poorly.
    notes: >
      Held at launch and on Aug 10 pending the script-model A/B; the
      staged grok-4.6 trial closed 2026-09-01 with the dp_pod script
      stage adopted, so the audio being clipped is the audio the show
      keeps. Operator one-time: create + flag the podcast playlist in
      Studio (landmine #15). Render/metadata only — outside landmine #17.

  - id: dp-pod-lever-shape-v2
    title: "DP Pod Lever — real-world actions, not screen lookups"
    area: quality
    shipped: 2026-09-04
    readout: 2026-09-18
    status: reading
    baseline: "21/26 renewed-episode levers were screen lookups; 13/26 opened with 'Open'; 3 near-duplicate groups"
    target: >
      <=5/10 screen-lookup levers, <=3/10 opening with the same verb,
      >=9/10 fuzzy-distinct — scored by the 2026-09-04 ledger predictions.
    notes: >
      Successor tic of the Aug-10 rotation memory: exact repeats stopped
      and the model converged on a shape instead. Fix is data-side (fuzzy
      dedup, banned-verb and real-world-due lines computed from the last
      six levers) plus a shape-only de-seed of the digest Lever spec.
      Prompt change — landmine #17 A/B.

  - id: network-content-discipline-2026-09
    title: "Content-density pass — scripts written from the digest, not copied; one owner per fact"
    area: quality
    shipped: 2026-09-05
    readout: 2026-09-26
    status: reading
    baseline: "flagship scripts 44-66% median digest-verbatim 8-grams (SpaceX 66, Tesla 49, M&A 44); 33 cross-section duplicate digest items in 160 digests; filler 6-10% of sentences on SpaceX; deep dive re-told a covered story in 5/6 M&A episodes"
    target: >
      script_digest_overlap_pct <= 25% on every flagship episode;
      digest_cross_section_dupes_removed trending to 0; script_filler_pct
      <= 5%; one closing text per flagship on 10/10. Read from the
      review_snapshot "Script density audit" table and metrics_ep*.json.
    criteria: >
      Hit = all four numbers move as targeted on the three flagships by the
      readout. Partial (verbatim share does not move but duplicates and
      filler do) = ship the data-side paraphrase gate (reject + one script
      rerun above threshold). Miss on any A/B-listened prompt = git revert
      of that prompt only; the engine modules stay (removal-only, read-only).
    notes: >
      Operator brief 2026-09-05 ("narrative driven with redundant
      repetition"). Every prompt change is landmine-#17 A/B; the engine
      changes (engine/digest_overlap, engine/script_audit, show_memory
      instruction, pronunciation, duplicate tail-line guard) are not.
      Review doc: docs/reviews/network_delivery_review_2026_09_05.md.
      Same-day readout (Tesla Ep595 vs Ep596): filler 3 -> 0, verbatim
      only 63% -> 51%, so the script rewrite gate shipped the same
      evening (llm.script_rewrite_gate_overlap_pct = 40 on the nine
      English news shows). Score the verbatim target against
      script_rewrite_gate_after_pct where the gate fired.
