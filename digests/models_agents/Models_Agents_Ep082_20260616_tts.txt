Glad you're here — welcome to Models and Agents, episode eighty-two. It's June sixteenth, twenty twenty-six. Your daily briefing on the A I models and agents that are changing everything. And no, not THOSE kinds of models and agents. Let's get into it.

Export controls on Fable-5 now block the exact code-fixing workflows defenders rely on daily.

Simon Willison published a detailed breakdown showing how Fable-5's refusal to fix vulnerable open-source code triggered enforcement actions.

Researchers found the model rejected a direct security review prompt but accepted a multistep fix this code request.

That output then became working test scripts for defensive patching.

The capability sits at the center of the find-fix-test loop security teams run every day.

Regulators appear to have treated defensive bug fixing as a prohibited cyber capability.

The result limits access to models that could improve patching inside the United States.

Watch for further clarification on whether routine code review prompts stay restricted.

This development lands in the ongoing conversation around safety and policy that we have tracked since episode eighty-one.

It raises fresh questions about how export rules will separate attack generation from everyday defensive tooling.

Nemotron 3 Ultra arrived as a five hundred fifty billion total parameter mixture of experts model with fifty five billion active parameters.

The hybrid Mamba attention design was pre-trained on twenty trillion tokens and then extended to a one million token context window.

Post-training combined supervised fine tuning, reinforcement learning, and multi-teacher on-policy distillation.

The model reportedly delivers up to six times higher inference throughput than current public frontier models while matching accuracy.

Nvidia open-sourced the base, post-trained, and quantized checkpoints plus the training data on Hugging Face.

This release continues the open-weight models arc we covered yesterday and narrows the performance gap with closed frontier systems.

Builders working on long-running autonomous agents should test the one million context checkpoints for workflow memory tasks.

The Ling and Ring 2.6 family upgrades the Ling 2.0 base through architectural migration pre-training and large-scale post-training.

Ling 2.6 targets instant response with high capability per token.

Ring 2.6 targets deeper reasoning and advanced agent workflows.

A hybrid linear attention design combines Lightning Attention with MLA to improve long-context training and decoding efficiency.

Additional techniques include evolutionary chain of thought, linguistic unit policy optimization, and the KPop RL framework.

All checkpoints are open-sourced.

Teams building low-latency agentic systems should benchmark Ling 2.6 against current production stacks.

AdaMame is a two-stage training recipe that aligns reasoning language to the query language without accuracy trade-offs.

Stage one fine-tunes on naturally occurring reasoning traces across five languages.

Stage two uses a modified group relative policy optimization where a query-conditioned alignment factor grows during training.

The method improves language fidelity and token efficiency on twelve languages across two benchmarks while preserving accuracy.

Largest gains appear on lower-resource languages.

Multilingual agent developers should experiment with the GRPO adaptation on their current fine-tunes.

quicktok is a C++ BPE tokenizer that produces byte-identical output to tiktoken.

It runs four to eleven times faster than the Python version and two to three point six times faster than bpe-Open A I on Apple M1 silicon.

The project supports clone hundred thousand, otwo hundred thousand, G P T OSS, Llama-3, and Qwen2.5/3 tokenizers.

It ships with a hand-compiled pretokenizer plus dense caches for merge checks.

Any pipeline doing heavy tokenization should swap in quicktok for immediate throughput gains.

Hermes Agent now lets the delegate tool spawn background subagents via the new async delegation toolset.

Subagents run independently so the parent chat can continue while tasks execute.

Users can later steer, check status, or collect results.

The update introduces explicit subagent isolation and synchronous versus asynchronous trade-offs.

This change moves the agents and tool use conversation forward from yesterday's coverage by addressing reliability of long-horizon workflows.

Developers using Hermes should test the async path on multi-step tasks that previously stalled the main session.

Atoms lets users describe an app in plain language and then deploys A I agents to build, edit, refine, deploy, and market the application.

The system targets non-developers who want to ship functional products through iterative natural-language direction.

No specific license or pricing details were released.

Teams exploring no-code agentic app generation should try the public demo to assess current output quality on realistic product ideas.

Google Cloud introduced the open knowledge format, a vendor-neutral markdown spec for giving A I agents curated context.

OKF formalizes the L L M wiki pattern as a directory of markdown files with YAML frontmatter where each concept requires only a type field.

Google released reference tools, a working Python consumer, and an interactive bundle explorer.

The format is positioned as an alternative to retrieval augmented generation for supplying agents with structured, human-readable context.

Agent builders needing portable, version-controllable knowledge should evaluate OKF bundles against their current context-injection methods.

PhoneHarness provides a mixed-action execution harness and benchmark that lets phone agents combine GUI actions, device CLI commands, and host-side tools.

On the annotated evaluation split it reaches a seventy five percent pass rate, twelve point nine points above the strongest non-PhoneHarness baseline.

The benchmark requires observable side effects rather than just plausible final answers.

Mobile-agent developers should examine the harness for tasks that need verifiable real-world effects beyond screen taps.

A tutorial now walks through setting up Docling Parse in a stable Python environment.

It covers handling Colab dependency issues and generating custom multi-page PDFs with text, columns, tables, shapes, and images.

The pipeline extracts words, characters, and lines with page-level coordinates, renders visual overlays, and exports structured JSON or CSV.

Anyone ingesting complex PDFs for retrieval augmented generation or agent memory should run the pipeline on their current document corpus.

OK, let's pop the hood on quicktok.

Tokenization speed is really a memory-access and data-structure problem that compounds across every forward pass.

quicktok keeps the exact backtracking BPE algorithm but replaces the general regex pretokenizer with a hand-compiled one.

It adds a two-byte trie for longest-match walking plus dense caches for merge-validity checks.

These changes cut memory traffic enough to deliver four to eleven times speedups on Apple M1 while remaining byte-identical to tiktoken.

The quality of the resulting tokens stays unchanged.

The approach works best when you control the full ingestion pipeline and can afford a native dependency.

If you are already bottlenecked on Python tokenization loops or running high-volume batch encoding, the swap is low-risk and immediately measurable.

The gotcha that bites most teams is forgetting to verify byte-identity on their specific tokenizer variant after the switch.

Test Nemotron 3 Ultra one million context checkpoints on long-horizon agent workflows that previously hit context limits.

Swap quicktok into any heavy tokenization pipeline for immediate four times or greater throughput without changing token IDs.

Experiment with Hermes Agent's async delegation on multi-step tasks that currently stall the parent chat.

Load Google Cloud's OKF reference tools and compare curated markdown bundles against your existing retrieval augmented generation context strategy.

Further clarification is expected on Fable-5 export-control scope for defensive security prompts.

Additional open checkpoints from the Ling and Ring 2.6 family are planned in coming weeks.

Tomorrow, keep an eye on how other agent frameworks adopt asynchronous subagent patterns.

That's Models and Agents for today. If you found this useful, share it with someone who's trying to keep up with all these changes, and subscribe so you don't miss tomorrow's update. The A I world moves fast. We'll help you keep up. See you tomorrow. And before you go — this show is part of the Nerra Network, a family of daily podcasts covering tech, science, markets, and more.

If you enjoyed today's episode, give Modern Investing Techniques a listen: modern strategies for Canadian and U.S. investors. You can explore the whole lineup at nerranetwork dot com.

This episode used AI voice synthesis of my voice — editorial selection and analysis are my own.