Hey everyone, welcome to Models and Agents, episode forty-nine. It's May fifteenth, twenty twenty-six. Lots of news to walk through — let's start with what actually matters.

Codex just landed on your phone — Open A I’s coding agent now runs natively on iOS and Android with Windows sync coming soon.

Open A I rolled out a preview of Codex inside the ChatGPT mobile apps today.

That gives developers full agent workflows they can trigger from anywhere.

An-thropic released a new paper on U S China A I leadership plus a two hundred million dollar Gates Foundation partnership focused on global health and education.

At the same time local builders are pushing Deep Seek V4 Pro and multi-agent setups to higher performance on everyday hardware.

The Codex rollout moves the agent off the desktop and into a pocket-sized coding partner.

The mobile preview supports complete agent workflows right in the ChatGPT apps.

Phone-to-Windows desktop connection arrives in the next update.

Developers can now start complex refactors or debugging sessions while commuting or traveling.

Expanded Windows integration and potential A P I access for custom mobile tools are both on the near-term roadmap.

Supertone shipped the third generation of its on-device TTS engine.

Supertonic version three adds support for thirty one languages plus expression tags.

It cuts reading failures while keeping the same inference contract for existing integrations.

Language coverage jumps six times compared with the prior release.

Builders working on multilingual voice agents get a drop-in local upgrade with no added latency.

VectraYX-Nano is a new forty two million parameter Spanish cybersecurity model trained from scratch.

It includes native M C P tool calling and was trained on a one hundred seventy million token Spanish corpus.

The model ships as an eighty one megabyte GGUF file that runs sub-second on commodity hardware.

Early adopters now have the first Spanish-native cybersecurity L L M with end-to-end tool use.

Physics-R1 is a new visual physics reasoning model cold-started from Qwen3-VL-8B-Thinking.

It was trained on audited olympiad data and raises performance by eighteen point three percentage points on held-out olympiad problems.

The model also improves physics reasoning benchmarks by fifteen point seven percentage points over the base eight billion parameter version.

The release includes cleaned corpora and a reproducible GSPO plus DAPO training recipe.

WSPN launched a stablecoin payment skill built specifically for the A I agent economy.

The agent can execute payments autonomously while keeping compliance hooks in place.

Developers building agentic commerce flows can drop this in as a ready-made payment primitive.

MediaTek is positioning its Dimensity chips as the hardware base for smartphone A I agents.

The platform emphasizes on-device agent inference with optimized NPU support.

Mobile developers should test current agent workloads on Dimensity devices to measure power draw and latency.

Amazon deployed an A I agent that can shop end-to-end on behalf of users.

The agent is backed by Amazon’s growing satellite constellation for global connectivity.

Retail and logistics teams are watching how this shifts agent-to-commerce integration patterns.

A builder on r/LocalLLaMA demonstrated four parallel Chwen three point six thirty five billion sub-agents.

The setup is orchestrated by Deep Seek and runs on dual RTX three thousand ninety cards.

Local reviewers plus a cloud final pass keep total A P I spend under twenty dollars per month.

The full opencode.json config is shared publicly for anyone who wants to replicate the pattern.

Another user reached forty five plus tokens per second prompt processing on Deep Seek V4 Pro.

The setup uses ktransformers on Epyc plus RTX PRO six thousand hardware.

Context scaling to thirty two thousand tokens stayed stable at roughly one hundred watts of G P U draw.

The configuration runs the original model files with no conversion step required.

Community discussion is growing around forty eight gigabyte four thousand ninety class cards sourced from China.

Builders are collecting benchmarks, BIOS quirks, and long-term stability data.

Anyone considering these cards should join the shared research effort before buying.

OK, let's pop the hood on speculative decoding.

Everyone treats it as a simple speed boost you just enable.

In practice it depends on a fragile agreement between the drafter model and the target model distributions.

When that agreement slips even slightly, the average accepted length tau collapses and the promised two times speedup vanishes.

Mistletoe-style attacks exploit exactly this gap.

They nudge the drafter with null-space projected gradients that preserve output semantics while tanking acceptance rates.

The engineering tradeoff is clear.

You gain throughput only while the drafter stays close to the target.

Any defense that hardens acceptance also adds verification latency.

Most teams hit the wall once context scales past eight thousand tokens.

Drift between drafter and target grows fastest in that regime.

The practical rule is to keep your drafter within one training epoch of the target.

Monitor tau in production.

Once it drops below one point eight, fall back to standard decoding instead of forcing the speculative path.

Install the Codex preview on iOS or Android and test a multi-file refactor task while away from your desk.

Run the new Supertonic version three TTS locally on a multilingual voice agent project to compare reading stability against your current engine.

Replicate the four-sub-agent Chwen setup using the shared opencode.json if you have dual three thousand ninety class G P U's.

Download the VectraYX-Nano GGUF and test its M C P tool-calling on a Spanish cybersecurity workflow.

Benchmark your current agent stack against the MarkTechPost SWE-bench and Terminal-Bench numbers for Claude Code and G P T five point five.

Open A I is expected to expand Codex Windows phone sync and expose more mobile agent A P I's in the coming weeks.

An-thropic’s U S China paper will likely spark follow-on policy discussions at upcoming A I governance events.

More local builders are preparing sixty four thousand plus context tests for MTP models following the recent Deep Seek V4 Pro results.

Hardware vendors are watching MediaTek’s Dimensity agent push for clues on next-generation mobile NPUs.

Tomorrow, keep an eye on the first real-world benchmarks from the four-sub-agent Chwen setups now running on dual three thousand ninety cards.

That's Models and Agents for today. If you found this useful, share it with someone who's trying to keep up with all these changes, and subscribe so you don't miss tomorrow's update. The A I world moves fast. We'll help you keep up. See you tomorrow.

This episode used AI voice synthesis of my voice — editorial selection and analysis are my own.