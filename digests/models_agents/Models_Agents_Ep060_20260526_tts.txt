Pull up a chair, this is Models and Agents, episode sixty, on May twenty-sixth, twenty twenty-six. Let's see what shipped. Let's see what happened in the A I world today. And trust me, it's been busy.

Local builders can now treat markdown skill files as optimizable parameters with automated validation gates instead of manual tweaking.

A new paper formalizes SkillOpt as a method that uses frontier models to propose bounded edits to those markdown skills.

Only edits that improve a held-out validation set are accepted.

The best skills converge after one to four accepted edits, and an edit budget of four to eight works best while removing the cap collapses performance.

A skill optimized on Codex transferred directly to Claude Code and gained fifty nine point seven on SpreadsheetBench.

G P T four point one nano with an optimized skill roughly matched frontier performance on procedural tasks.

The approach needs an auto-grader with clear correct answers, which limits it to code and spreadsheets for now.

Watch for follow-up work that relaxes the auto-grader requirement.

The thirty five billion parameter Chwen variant now ships in an uncensored heretic configuration that preserves all seven hundred eighty five MTPs.

The thirty five billion and twenty seven billion versions show KL divergences of zero point zero four eight seven and zero point zero three zero eight with accuracy losses of only zero point four zero percent and zero point three five percent.

The release includes Safetensors, GGUF, NVFP4, and GPTQ-Int4 formats.

The author notes the thirty five billion model targets general-purpose use while the thirty six billion version focuses on agentic and coding tasks.

Builders wanting an uncensored general-purpose model with preserved thinking tokens should test the q four or q six GGUF variants.

Raon-Speech turns a pre-trained L L M into a SpeechLM through speech-module alignment, end-to-end pre-training with knowledge distillation, and multi-task preference optimization.

Training used one point three eight million hours of English and Korean data.

The model tops eight similarly sized audio foundation models across forty two English and Korean speech and text benchmarks while keeping strong text QA performance.

Raon-SpeechChat adds full-duplex conversation after continual training on one hundred nineteen thousand hours of dialogue data.

Teams needing bilingual speech understanding and generation should evaluate the open-sourced checkpoints.

A new pull request adds support for talkie-1930-13b-it inside the server backend.

The thirteen billion instruction-tuned model was trained only on pre-nineteen thirty one English text and further aligned with online DPO.

It enables historical role-play without modern knowledge contamination.

Reference code and the Hugging Face repo appear in the pull request.

Developers building vintage-language or time-period simulation agents now have a ready-to-run thirteen billion option.

Shard delivers a drop-in Hugging Face cache that reduces Lah-mah three point one eight billion KV memory by roughly ten times at eight thousand context and eleven times at thirty two thousand context.

It applies PCA plus int four quantization to K after undoing RoPE and Hadamard rotation plus vector quantization to V.

Attention runs directly on the compressed keys with no measurable degradation on NIAH or LongBench.

The repository sits at krish one nine zero five slash shard.

Teams running long-context local inference should benchmark Shard against their current KV setup.

A detailed thread examines how local users test prompt injection and jailbreak resistance before wiring models to tools, files, rag, shell commands, or browser automation.

Most current setups focus on quantization, context length, and tokens per second while giving less attention to isolating tool access or logging calls.

Builders moving local models into agentic workflows should adopt the read-only default and tool-call logging patterns raised in the thread.

Gartner warns that applying uniform governance across all enterprise A I agents is a death sentence.

The report recommends differentiated policies based on agent autonomy and risk.

One-size-fits-all controls will slow adoption without improving safety.

Enterprise teams evaluating agent platforms should review the governance segmentation guidance before standardizing policies.

An article outlines the primary production failure modes for agents and the first fixes teams should apply.

It focuses on concrete engineering gaps rather than high-level strategy.

Developers shipping agents should read the prioritized fix list before their next deployment review.

A user measured real-world ASR performance on an Intel Arrow Lake NPU versus CPU.

The NPU delivered two point eight to six point one times speedups and ten to twenty one times lower energy for ten to sixty second audio clips.

The setup also frees VRAM on a seven thousand nine hundred XTX and beats a previous three thousand sixty eGPU for short smart-home voice commands.

The wyoming-parakeet-on-intel-npu repository is linked.

Home Assistant or local voice developers should test the NPU path for always-on ASR workloads.

A post explains how context is divided across parallel slots and asks about consequences of exceeding model max context or running two agents in parallel when VRAM allows.

Practical questions cover performance, energy, and slot allocation.

Anyone moving from LM Studio to the server backend for multi-agent setups should review the flag interaction details.

A step-by-step guide walks through loading the TuringEnterprises slash Open-MM-RL dataset, building a reward function that checks exact matches, and exporting for GRPO.

It covers schema inspection, domain analysis, and vision-language prompting.

Developers starting multimodal reasoning projects with verifiable rewards can follow the pipeline directly.

A detailed post lists nine concrete changes that eliminated crashes and delivered forty nine to sixty five tokens per second generation on a fourteen inch M two Max.

The changes include a sixty hertz refresh rate, GGUF over MLX, wired memory limit, preserve thinking flag, OpenCode instead of Claude Code, and q eight KV cache minimum.

The author shares exact fan curves and iogpu wired limit meters settings.

Mac users struggling with long agentic sessions should apply the refresh-rate and memory-limit steps first.

OK, let's pop the hood on the NPU results.

Everyone treats NPUs as marketing gimmicks for L L M's, yet the real engineering story is about power-state transitions rather than raw TOPS.

An NPU can exit deep sleep and begin useful work in a few milliseconds while a discrete G P U must ramp clocks, allocate buffers, and warm PCIe links.

This difference only matters for short, bursty workloads.

Once the task exceeds a few seconds the G P U's higher sustained throughput wins.

In the measured ASR case the NPU delivered four point eight times lower latency and ten point seven times less energy on ten-second clips precisely because it avoided the G P U's ramp-up tax.

The practical decision rule is simple.

If average inference length is under roughly three seconds and you already have an NPU in the SoC, route the work there.

Anything longer or batch-oriented still belongs on the G P U.

The gotcha most teams miss is that the NPU's advantage disappears the moment you add a second concurrent stream, because its limited thirteen TOPS cannot hide behind parallelism the way a larger G P U can.

Test the SkillOpt edit-and-validate loop on a code or spreadsheet agent you already maintain.

The one to four edit convergence pattern is worth reproducing.

Run the thirty five billion parameter uncensored heretic GGUF on your preferred backend and compare tool-call stability with and without preserve thinking enabled.

Measure your current server setup with the parallel and context flags against the slot-division behavior described in the thread before scaling to multiple agents.

Try the wyoming-parakeet-on-intel-npu setup on any Arrow Lake machine you have for short voice commands.

The energy numbers are worth verifying locally.

Apply the sixty hertz refresh-rate and wired memory limit changes on any MacBook running long-context sessions and re-measure prompt-processing time.

More consumer-hardware diffusion L L M training runs are expected following the open-dllm and d three L L M code drops.

Additional server backend pull requests for historical and vintage models will likely land as the talkie-1930 effort gains visibility.

Follow-up work on validation-gated skill optimization is anticipated once more teams reproduce the auto-grader requirement.

Tomorrow, keep an eye on expanded NPU integration examples for non-ASR workloads as the Intel Arrow Lake results circulate.

That wraps up today's A I briefing. Share this with a developer or builder who wants to stay current. Subscribe wherever you listen. See you tomorrow.

This episode used AI voice synthesis of my voice — editorial selection and analysis are my own.