What's up — welcome to Models and Agents, episode fifty-two, for May eighteenth, twenty twenty-six. New week in A I. And if last week was anything to go by, buckle up. Let's get into it.

En-vidia's four-bit pretraining technique cuts memory needs for large hybrid models while keeping accuracy nearly identical to FP eight.

The core development is a full four-bit pretraining stack called NVFP four.

It was validated on a twelve billion parameter Mamba-Transformer hybrid trained for ten trillion tokens.

The method combines selective BF sixteen layers for critical weights, sixteen by sixteen random Hadamard transforms on gradients, two-dimensional weight scaling, and stochastic rounding.

Downstream accuracy stayed within zero point zero four points of the FP eight baseline.

That is sixty-two point five eight percent versus sixty-two point six two percent on M M L U Pro.

Teams running extended pretraining on constrained G P U fleets now have a documented path to reduce memory and interconnect pressure.

Watch for follow-up work on larger dense models and whether the same recipe transfers to pure transformer architectures.

Community forecasts point to new open-weight model drops likely landing between late May and early June.

Posters on r/LocalLLaMA note a shift in how open-weight submissions are being handled.

They are watching for signs that major labs are extending the current cadence.

Tests on Chwen three point five thirty-five B A three B and its no-refusal fine-tune show register-dependent expert routing even before refusal layers activate.

AAVE-coded safety prompts produced different first-token routing tensors and longer thinking traces than matched Academic English prompts.

This raises questions about whether refusal alone masks underlying safety gaps.

Luce DFlash plus PFlash delivered two point two four times decode and three point zero five times prefill speedups versus Lah-mah dot cpp HIP on an RX seventy-nine hundred XTX.

The optimal DDTree budget was eight rather than the twenty-two recommended for Strix Halo.

This highlights how GDDR six bandwidth changes the best speculation strategy.

A new agent built specifically for four B to fourteen B local models reaches eighty-seven percent on coding benchmarks.

It replaces chained tool calls with compound actions, automatic lint-and-fix loops, and on-demand escalation to larger models.

SmallCode runs against LM Studio or Ollama endpoints and keeps ninety-five percent of work local.

Developers are exploring dedicated local models that read a TESTING dot MD or QUALITY dot MD file.

These models continuously refactor generated code for security, readability, and maintainability without sending large codebases to cloud A P I's.

Simon Willison argues that useful A I generated tools save reviewers time while unedited L L M slop wastes it.

He recommends including detailed terminal sessions that demonstrate exactly what was tried and how success was verified.

Any L L M output containing opinions or anecdotes must be closely directed and reviewed by a human.

Willison calls out examples where models falsely claim months of personal usage as particularly harmful.

Kar-pathy noted that simply saying boot an eight times H one hundred in a tutorial immediately blocks most viewers at step one.

This highlights the gap between assumed hardware access and real-world availability.

OK, let's pop the hood on data mixing.

Everyone talks about data mixing as a one-time hyperparameter you set at the start of training.

In practice it is a recurring online decision that must be solved at every phase of the model lifecycle.

The core insight is that you can cheaply simulate candidate mixtures by interpolating between low-rank adapters already trained on the current checkpoint.

This avoids training separate proxy models from scratch.

The approach keeps the search grounded in the model's actual learning dynamics rather than an earlier snapshot.

It adds negligible overhead per decision yet finds mixtures that improve average perplexity by roughly six percent over static baselines during pretraining.

In continual learning it matches full retraining or distillation performance while using sixty-six to ninety-five percent less compute.

The practical takeaway is to treat mixing as a lightweight inner loop that runs every few hundred steps rather than a single upfront choice.

The main gotcha is forgetting to re-evaluate when the data distribution or training objective shifts.

Test the Luce DFlash plus PFlash setup on your AMD card with Chwen three point six twenty-seven B.

See whether budget equals eight gives you the reported two point two four times speedup on short generations.

Run SmallCode against a local four B to seven B model on a real codebase.

Compare failure rates to your current agent workflow.

Add a short terminal-session block to your next project README to show exactly how you verified functionality.

Follow Simon Willison's recommendation on that one.

Experiment with OP-Mix-style low-rank adapter interpolation on your next continued-pretraining run.

This keeps data mixing decisions current without extra proxy models.

Community forecasts point to new open-weight model drops likely landing between late May and early June.

Further scaling experiments on the NVFP four four-bit pretraining recipe are expected once labs move beyond the twelve B hybrid proof-of-concept.

More work on dialect-aware safety testing for MoE models is likely after the recent routing-divergence findings.

Tomorrow, keep an eye on the first public follow-ups to the NVFP four four-bit pretraining runs as labs test larger dense models.

That wraps up today's A I briefing. Share this with a developer or builder who wants to stay current. Subscribe wherever you listen. See you tomorrow.

This episode used AI voice synthesis of my voice — editorial selection and analysis are my own.