Pull up a chair, this is Models and Agents, episode fifty-nine, for May twenty-fifth, twenty twenty-six. Your daily A I briefing. Monday in the A I world — which means a weekend's worth of announcements to catch up on. Let's go.

Datasette's new slash-key jump menu now launches agent conversations directly from your databases.

Simon Willison shipped Datasette one point zero alpha thirty with a keyboard-driven jump to menu that plugins can extend.

The datasette-agent plugin adds a conversation starter form directly into that menu.

NuExtract three, a new four billion parameter vision-language model, arrived on Hugging Face for structured extraction and Markdown conversion from documents.

A custom inference engine now runs MiniCPM-V four point six on the Ascend three hundred ten B N P U inside the one hundred forty-nine dollar Orange Pi A I Pro board.

These small targeted releases lower the friction for mixing data exploration with agent workflows this week.

The top story this week is the Datasette release that turns a data exploration tool into a lightweight launchpad for agent sessions.

Datasette one point zero alpha thirty introduces a jump to menu triggered by the slash keyboard shortcut.

Users can type to reach databases, tables, or canned queries without leaving the interface.

The release includes plugin hooks so extensions can inject additional content into the menu and its empty state.

The datasette-agent plugin uses this hook to surface a form that starts a new agent conversation.

A live demo is available at agent dot datasette dot i o after GitHub sign-in.

More implementation details appear on the Datasette blog.

Teams already running Datasette can upgrade to one point zero alpha thirty and test the plugin immediately.

Now for the model updates.

NuExtract three is a four billion parameter vision-language model released on Hugging Face.

It handles both structured JSON extraction from text or images and high-quality image-to-Markdown conversion.

The model accepts text, images, or mixed inputs and supports multilingual documents plus separate reasoning and non-reasoning modes.

G G U F, N V F P four, M L X, and v L L M quantized versions are already available.

Builders working on document pipelines, O C R, or R A G preprocessing should test it on receipts, invoices, and tables this week.

Next, Chwen three point six twenty-seven B and thirty-five B B F sixteen models were benchmarked on a two times R T X PRO six thousand setup using the latest stable v L L M backend.

At sixty-four concurrency the twenty-seven B model reached one thousand six hundred tokens per second generation without M T P and one thousand eight hundred tokens per second with M T P two enabled.

The thirty-five B model hit two thousand seven hundred tokens per second at sixty-four concurrency and three thousand five hundred tokens per second at one hundred twenty-eight concurrency.

It also delivered thirty thousand tokens per second prompt processing.

These numbers come from a personal project and give concrete throughput expectations for mid-size Chwen variants on dual professional G P U's.

A custom C plus plus inference engine was written from scratch to run MiniCPM-V four point six natively on the Ascend three hundred ten B N P U inside the one hundred forty-nine dollar Orange Pi A I Pro board.

The engine bypasses torch underscore n p u entirely on the hot path.

It uses custom Ascend C kernels for cube matmul, l meters underscore head chunking, and vectorized causal-conv one d to reach five point nine zero tokens per second F P sixteen after incremental optimizations.

Python is used only for cold-path tokenization and image preprocessing.

Anyone targeting edge V L M deployment on this hardware now has an open-source starting point at the linked GitHub repo.

Mininglamp A I released Cider, a small S D K that adds W eight A eight activation quantization on top of M L X for Apple Silicon.

On an M five Pro the change reduced prefill time for a four B V L M from two point eight three nine seconds to two point five one nine seconds at four thousand five hundred sixteen token context.

Decode speed stayed essentially flat.

Per-channel I N T eight Tensor Ops require M five or newer while M four falls back to the regular path.

The repo includes accuracy numbers on Wikitext two for Chwen three eight B and Lah-mah three eight B showing modest perplexity impact.

Now turning to agent and tool developments.

A developer released A I sandbox-manager, an L X C based V M template system that gives agents full sudo access, G P U passthrough, and browser automation while isolating them from the host.

The setup supports multiple concurrent sessions, persistent dot env files, and hooks to block dangerous git operations.

It was built and tested on D G X Spark hardware where standard Docker G P U passthrough is difficult.

The repo includes c u a for computer-use support on Linux.

A new repo called M C P from Scratch walks through building a Model Context Protocol server and client in plain Node dot j s.

It then adds local G G U F inference via node-Lah-mah-cpp and a custom plan-act-observe agent loop.

The later modules demonstrate M C P sampling and tool use with a local model, plus an optional Lang Chain path.

The project is intentionally minimal and aimed at developers who want to understand the protocol mechanics rather than use a high-level S D K.

In the practical and community section, a pull request to ggml-org slash Lah-mah dot cpp improves checkpoint creation.

Agentic coding sessions no longer force full prompt re-processing after context-rewriting tools modify history.

The change targets the common pattern where an agent produces twenty k plus tokens of code and the next short user message triggers expensive reprocessing of seventy k token contexts.

Early users report noticeably more responsive agentic coding loops after adopting the patch.

OK, let's pop the hood on activation quantization.

Everyone talks about activation quantization as if it is a simple turn on I N T eight switch that always delivers free speed.

In practice it requires custom kernels that only compile on newer silicon and trades small accuracy losses for prefill gains that appear mainly at longer contexts.

The Cider implementation registers Metal primitives so the per-channel W eight A eight path runs one point eight four times faster than W eight A sixteen at M equals four thousand ninety-six.

Yet decode speed barely moves because memory bandwidth remains the limiter.

On M four hardware the same code silently falls back, showing that the technique is gated by hardware Tensor Op support rather than software availability.

Perplexity impact stays under zero point two points on eight B models when using per-channel scaling.

Per-group scaling tightens the bound further when precision matters more than the three hundred millisecond prefill saving.

The gotcha that bites most teams is assuming the speedup will appear on every workload.

Short-context chat sees almost none of the benefit while long-document R A G preprocessing sees the full win.

Use it when your pipeline is prefill-heavy and you control the deployment hardware.

Otherwise stick with weight-only quantization until the next silicon generation lands.

Here are the things to try this week.

Install the datasette-agent plugin on Datasette one point zero alpha thirty and test the slash jump menu to launch agent conversations directly from your tables.

Pull NuExtract three from Hugging Face and run it on a batch of scanned invoices to compare its structured JSON output against your current O C R plus prompt pipeline.

Clone the A I sandbox-manager repo if you need a persistent, G P U enabled sandbox for computer-use agents that survives host reboots.

Try Cider on an M five Mac with any M L X model to measure prefill improvement on your longest context workloads before deciding on wider adoption.

On the horizon, more plugin authors are expected to extend the Datasette jump menu now that the hook is public.

Additional quantized versions and fine-tunes of NuExtract three are likely to appear on Hugging Face within days.

The Lah-mah dot cpp checkpoint P R is under active review and may land in the next stable release.

Edge V L M experiments on Ascend hardware will probably produce more custom-kernel repos following the MiniCPM-V example.

Tomorrow, keep an eye on the Lah-mah dot cpp checkpoint P R status and any new Datasette plugins that extend the jump menu.

That wraps up today's A I briefing. Share this with a developer or builder who wants to stay current. Subscribe wherever you listen. See you tomorrow.

This episode used AI voice synthesis of my voice — editorial selection and analysis are my own.