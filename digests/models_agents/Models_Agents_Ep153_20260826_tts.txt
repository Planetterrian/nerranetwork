Open A I’s first custom inference chip is entering production, delivering higher throughput and lower latency in one architecture.

This is Models and Agents, episode one hundred fifty-three.

Open A I plans to deploy its Jalapeño inference chip into production clusters by year-end.

Early tests show the design improves both throughput and latency at once, raising intelligence per watt for ChatGPT and agent workloads.

A new ChatGPT Business Premium Seats plan launches at one hundred dollars per seat for small teams.

Qwen3.8-Flash-Next reaches release today with weights already posted on Hugging Face and ModelScope.

Liquid A I open-sourced the Pipette benchmarking suite for on-device model evaluation.

Open A I plans to begin deploying Jalapeño, its first custom inference chip, into production compute infrastructure by year-end.

Testing results indicate the architecture delivers both higher throughput and lower latency without efficiency tradeoffs.

This setup supports faster ChatGPT responses, more responsive Codex sessions, and agents under growing demand.

Gen two is already deep in development and Gen three is taking shape.

The company also introduced ChatGPT Business Premium Seats at one hundred dollars per seat.

The plan targets small businesses and startups that need advanced tools previously limited to large enterprises.

Remember, we covered A I Compute and Inference yesterday — today's Jalapeño deployment moves the inference cost curve forward by integrating custom silicon into existing clusters first.

Qwen3.8-Flash-Next arrives today with an estimated release at fifteen hundred U T C.

Official weights are posted on Hugging Face and ModelScope for immediate download.

Community discussion centers on quants, fine-tunes, chat templates, and inference server support.

The model targets multimodal and high-speed use cases following recent Qwen family releases.

A fully NVFP4-quantized Qwenthree point eight to twenty-sevenB checkpoint was released after two thousand four hundred forty-six steps of quantization-aware distillation.

The model runs in vLLM on Nvidia Blackwell G P U's with a two hundred sixty-two thousand one hundred forty-four token context.

It retains near-BF16 performance on G P Q A Diamond and AIME26 despite aggressive W4A4 quantization across all linear layers.

Attention and GDN layers stay at higher precision because full quantization there causes larger quality loss.

The checkpoint sits at QUASAR-QAT/Qwenthree point eight to twenty-sevenB-QUASAR-NVFP4 and supports the vLLM serve command with max model length two hundred sixty-two thousand one hundred forty-four.

IBM released Granite 4.2, a family of open reasoning models in three billion, eight billion, and thirty billion sizes under Apache two point zero.

Every model exposes a thinking, low-effort, or non-thinking switch plus native tool calling.

The eight billion and thirty billion variants receive additional agentic reinforcement learning training for code editing, terminal use, and web search inside sandboxed environments.

The thirty billion model reports fifty-seven on SWE-Bench Verified and twenty-nine point two four on Terminal-Bench two point one.

SSE-Bio maintains a structured state and uses a trainable proxy policy with group relative policy optimization.

The agent decides retrieval of knowledge triplets and prior templates, improving reasoning memory through fine-grained template editing.

On three biomedical multi-hop QA benchmarks it outperforms the strongest self-evolving baseline by six point five six absolute points on BioHopR.

A dual-layer agentic memory framework routes incoming information through a small-to-large model cascade.

The cascade labels each item non-write, write-new, or write-update, pruning up to sixty-eight percent of redundant external memory.

High-value memories later consolidate into parameters via supervised fine-tuning while retaining over ninety-eight percent of downstream QA Exact Match.

Paul Dix noted that A I wrote and iteratively refined one million lines of code into reliable software now running on millions of machines.

Verification systems and clear direction enabled the outcome even with an oracle for comparison.

Liquid A I open-sourced Pipette, a benchmarking suite developed with Artificial Analysis.

The suite measures on-device model quality, quantization, runtime, and hardware together.

It addresses the gap between server-class model cards and real phone behavior.

Try the QUASAR-QAT NVFP4 Qwenthree point eight to twenty-sevenB checkpoint in vLLM on Blackwell G P U's if you need twenty-seven billion class performance at roughly one-third the memory footprint of BF16.

Benchmark your edge models with the newly open-sourced Pipette suite to see how quantization and runtime choices behave on target hardware.

Test Qwen3.8-Flash-Next once released against current eight billion open models on agent tool-calling tasks where speed matters more than peak capability.

Run the SSE-Bio proxy policy training loop on a small biomedical multi-hop dataset to see whether structured state plus fine-grained template editing reduces instruction drift.

Compare the Granite 4.2 thirty billion model’s agentic reinforcement learning results on SWE-Bench Verified against your current open coding agents.

Gen two of Open A I’s inference chip roadmap is already in deep development.

Qwen3.8-Flash-Next community quants and fine-tunes are expected within days of today’s release.

More on-device benchmarking results using Pipette will likely surface as edge teams adopt the suite.

Everyone talks about custom inference chips as a simple efficiency win.

In practice the design centers on balancing throughput, latency, and power within a fixed thermal envelope.

Jalapeño’s reported gains come from a single architecture that raises both throughput and lowers latency rather than trading one for the other.

The first production step is integration into existing Open A I clusters, which limits initial risk while the team validates system-level behavior.

Later generations will target further efficiency and speed, but each step must still clear the same power and cooling constraints that limit G P U clusters today.

The practical decision for teams is whether the resulting token-cost curve justifies moving workloads onto the new silicon once access expands beyond Open A I’s own services.

When your workload is dominated by high-volume, latency-sensitive inference and you already sit inside Open A I’s ecosystem, the chip becomes worth testing first.

Before we go, early signals from Open A I’s Gen two inference chip should surface later this year.

That's Models and Agents for today. If you found this useful, share it with someone who's trying to keep up with all these changes, and subscribe so you don't miss tomorrow's update. The A I world moves fast. We'll help you keep up. See you tomorrow. And if you'd rather watch than listen, find us on YouTube at Nerra Network — link's in the show notes. This show comes to you from the Nerra Network.

If you want another sharp daily briefing, Offshore North has you covered — offshore ocean racing explained every Monday, with a Canadian angle. All our shows are free at nerranetwork dot com. The Do Positive Pod is the club for people who do something about it — episodes and the Dispatch Wall at nerranetwork dot com thedppod.

This episode used AI voice synthesis of my voice — editorial selection and analysis are my own.