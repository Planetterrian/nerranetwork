Hey everyone, welcome to Models and Agents, episode ninety-seven, for June thirtieth, twenty twenty-six. Your daily A I briefing. Your daily briefing on the A I models and agents that are changing everything. And no, not THOSE kinds of models and agents. Let's get into it.

Meituan just open-sourced a 1.6T MoE coding agent that ran the top OpenRouter leaderboard for two months while training entirely on Chinese ASICs.

LongCat-2.0 scored fifty nine point five on SWE-bench Pro.

It reached seventy point eight on Terminal-Bench 2.1 and seventy seven point three on SWE-bench Multilingual.

The model activates between thirty three and fifty six billion parameters per token.

It supports a native one million token context window through LongCat Sparse Attention.

Training ran on more than fifty thousand domestic Chinese ASICs and processed ten point one trillion tokens during its anonymous run.

The release carries an MIT license with free context-cache hits and a limited-time thirty cents per million input tokens and one dollar twenty cents per million output tokens A P I promo.

This development moves the open-weight models arc forward because it places a near-frontier agentic coding system under a permissive license without recurring Western A P I costs.

Builders now have a commercially usable model they can run or fine-tune locally.

The next signal to watch is whether other Chinese labs follow with similar ASIC-trained releases.

InternScience released a thirty five billion parameter MoE model called Agents-A1 on Hugging Face.

The accompanying tech report appears as arXiv two six zero six point three zero six one six.

No public benchmarks have been independently verified yet.

LocalLLaMA users are already requesting confirmation runs on standard agent harnesses.

This item continues the open-weight models conversation from yesterday because it adds another candidate that may narrow the gap with closed frontier systems.

Builders working on multi-agent systems should test the weights this week.

BERTomelo is a new monolingual Portuguese encoder built on the ModernBERT architecture.

It uses a one thousand twenty four token context window and FlashAttention optimizations.

Training covered one hundred six million Portuguese documents.

The model outperforms prior Portuguese encoders on STS and NER tasks while remaining lighter than large multilingual models.

Both base and large checkpoints are released publicly.

Teams building Portuguese-language retrieval or classification systems now have a drop-in option without multilingual overhead.

Attemory replaces online repository exploration with offline semantic search hints before feeding Claude Code.

On seven hundred twenty paired SWE-QA samples across fifteen repos the method cut total tokens by forty three point eight percent.

The official five-dimension judge score stayed statistically flat at eighty three point three nine versus eighty three point one seven.

The approach uses prefill-only retrieval instead of a second trained explorer agent.

Anyone running coding agents on large codebases can index once and reuse the same hint layer across many tasks.

This update advances the agents and tool use program we covered yesterday because it improves reliability of long-horizon coding agents without extra decode cost.

SEATauBench adapts TauBench to five Southeast Asian languages.

The benchmark tests agents under progressively localized conditions.

English-only agent scores transfer reasonably when only conversation language changes.

Quality collapses once tool specs and task domains are also localized.

The benchmark and adaptation pipeline are released on GitHub for anyone building reliable agents in linguistically diverse regions.

A detailed guide shows single and dual NVLink V100-SXM2 sixteen GB cards running Gemma four twenty six B at roughly one hundred tokens per second in TCC mode.

The setup scales to eight to sixteen concurrent agents at one hundred fifty to one hundred seventy five aggregate tokens per second under realistic twenty four K prompt loads.

The write-up covers driver window R five seventy to R five eighty, PSU transient requirements, and NVLink P2P measured at thirty three GB per second.

Builders wanting cheap offline coding agents now have concrete numbers and prebuilt binaries for a thirty two GB dual-card desktop rig.

LongCat-2.0 is now available under the same MIT license on GitHub and Hugging Face.

Developers can immediately swap it into existing Claude Code or OpenClaw workflows for agentic coding tasks.

This release keeps the open-weight models and agents threads moving because it removes both export restrictions and recurring A P I costs for production-grade agentic work.

Run LongCat-2.0 through its OpenRouter endpoint on a multi-file coding task to test the one million token context and free cache-hit pricing.

Index a private repo with Attemory, add the retrieval hint layer, and measure token savings on your own SWE-style tasks.

Set up the dual V100 rig following the linked guide if you need cheap offline agent concurrency without new hardware purchases.

Download the SEATauBench adaptation scripts and run a quick localization test on any agent you maintain for non-English markets.

More Chinese labs are expected to release ASIC-trained open models following Meituan’s playbook.

Independent verification runs of Agents-A1 should appear on LocalLLaMA within days.

Additional long-context interpretability techniques building on turn-averaged SAEs are likely in the next batch of arXiv interpretability papers.

OK, let's pop the hood on turn-averaged sparse autoencoders.

Everyone talks about sparse autoencoders as if they simply extract clean features from token activations.

In practice the standard per-token approach scales linearly with context length.

A one hundred K token transcript quickly becomes unusable for attribution graphs or manual inspection.

Turn-averaged SAEs instead reconstruct the average activation across an entire human or assistant turn.

This collapses the feature count to a fixed budget regardless of length.

The resulting features are judged more complete by L L M's for describing high-level turn characteristics.

Downstream attribution graphs become far simpler to build.

The tradeoff is loss of token-level granularity.

The method works best when the goal is turn-level interpretability rather than pinpointing exactly which token triggered a behavior.

Teams doing long-context analysis should reach for turn-averaged SAEs when context exceeds roughly eight K tokens.

They should fall back to per-token SAEs only for short, high-precision debugging.

So when should you actually reach for this versus the alternative.

Use turn-averaged SAEs for any transcript longer than eight K tokens where you need interpretable turn summaries.

Stick with per-token SAEs only when you must trace behavior to a single token.

Tomorrow, keep an eye on independent verification runs of Agents-A1 on LocalLLaMA.

That's Models and Agents for today. If you found this useful, share it with someone who's trying to keep up with all these changes, and subscribe so you don't miss tomorrow's update. The A I world moves fast. We'll help you keep up. See you tomorrow. And if you'd rather watch than listen, find us on YouTube at Nerra Network — link's in the show notes.

And before you go: this show is part of the Nerra Network — a family of daily podcasts on tech, science, and markets, each one a short, sharp briefing you can finish on the commute. If today earned its place in your feed, here's your next listen — Fascinating Frontiers, the most fascinating news from space and science. Find every show, free, at nerranetwork dot com.

This episode used AI voice synthesis of my voice — editorial selection and analysis are my own.