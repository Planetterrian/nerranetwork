Industry leaders from Anthropic, OpenAI, and DeepMind align on pacing frontier development with shared safety evaluator access.

This is Models and Agents, episode 172.

Anthropic chief executive Dario Amodei released an essay titled We Must Pace the Frontier.

The essay outlines a three-part plan for slowing AI development.

Amodei announced that Anthropic will give third-party evaluators permanent employee-level access to its systems.

Evaluators can verify safety measures, report incidents, and assess model alignment during training.

Sam Altman replied that OpenAI agrees on pacing the frontier after recent internal discussions.

OpenAI will implement the same independent evaluator access with more details coming soon.

Demis Hassabis stated that the essay points toward the right path forward even though details need working through.

Hassabis referenced DeepMind's recent proposal for an industry-wide standards body for frontier AI.

The commitments respond to rising risks and aim to establish verifiable standards across frontier labs through shared evaluator access.

Last week OpenAI revised Astra benchmarks while other models' scores fell, then rolled out faster multi-turn image editing with consistent subject preservation.

Mistral closed Europe's largest venture round at a twenty four billion dollar valuation, giving builders a new well-funded open-weight contender.

Anthropic detailed how sophisticated actors attempted cyberattacks, influence operations, and bioweapons with Claude and how every attempt was stopped.

OpenAI shared its full Defense Factory architecture for continuous agent loops that find, validate, and fix vulnerabilities.

A developer has prepared a nine point four billion parameter dense model incorporating Engram tables, Moonshot attention residual modeling, and RoPE over NoPE layering at a three to one ratio.

The model uses the Llama three tokenizer and language model head as a starting point.

Training ran on logit-level extraction from a Llama three teacher model on a single RTX four thousand ninety plus rented hardware.

The model remains stable after initial training steps.

The author plans to switch the target to the OLMo three series to avoid Llama licensing restrictions on synthetic data.

All code, data, and weights will be released publicly once pre-training completes.

Training code is optimized to run on a single RTX six thousand Pro series card.

ChatGPT Work with GPT six Astra generates five thousand and ten thousand running routes from any address using OpenStreetMap.

The system accepts an address, queries Nominatim for location, pulls local roads and trails via Overpass, calculates circular loops locally, and returns both an embedded D three visualization and downloadable GPX and GeoJSON files after running for twenty seven minutes.

The visualization skill produces a self-contained HTML file using D three from an allow-listed CDN that renders the route directly in the chat interface.

Andrej Karpathy noted that training on StackOverflow would have demonstrated earlier that large language models can function as promptable general-purpose question-answering engines.

A new platform called Lorivo hosts multiple LoRA adapters on a single base-model GPU instance by loading adapters into memory on demand.

The service exposes OpenAI-compatible endpoints directly from the web app or CLI.

Lorivo currently runs Qwen three point five four billion for free with a thirty two thousand context window and collects only token counts and timestamps.

The reverse-engineered ChatGPT visualize skill outputs a self-contained HTML fragment that uses D three to draw both the base map and the overlaid route geometry stored in a JSON script tag inside the HTML.

External resources are restricted to a short allow-list of CDNs including cdn dot jsdelivr dot net, esm dot sh, and unpkg dot com.

A new framework for multi-agent reinforcement learning addresses coordination failures in those settings.

RoastMyHarness runs DeepSWE benchmark tasks against both bare Pi and user-modified harnesses, extensions, skills, or AGENTS dot md files to measure quality, token efficiency, and cost changes.

The tool is currently tested on Linux and remains a work in progress with a wizard available to set up Pi extensions.

A developer is building a Rust and Tauri desktop application using QEMU with WHPX on Windows and is seeking advice on QMP for lifecycle management and approaches for letting an AI agent interact inside the VM.

Users reported that the Hugging Face library sends telemetry identifying the specific coding agent being used.

Test the Lorivo platform with a rank eight or rank sixteen LoRA on Qwen three point five four billion to see shared-adapter serving in action.

Run RoastMyHarness against your own Pi extensions to quantify whether they improve token efficiency on DeepSWE tasks.

Generate a running route from your own address with ChatGPT Work and GPT six Astra to explore the visualize skill output.

Further details are expected from OpenAI on its independent evaluator program.

Additional Western-lab open-weight models in the one hundred twenty billion range may appear as Chinese model restrictions tighten for some organizations.

Community training runs on the announced nine point four billion dense model could produce early checkpoints within weeks.

Before we go, OpenAI is expected to release more details on how its new evaluator access will work in practice.

You can pop the hood on LoRA adapter sharing in inference servers.

Inference servers can keep a single copy of base model weights in GPU memory while swapping lightweight adapter deltas for different users.

Adapter matrices are orders of magnitude smaller than the base weights, so memory traffic stays low even when many adapters are active.

vLLM already implements fused kernels that apply these deltas during the forward pass without materializing full weight copies.

The tradeoff appears when adapter rank grows.

Higher ranks increase both compute per token and the chance that the adapter no longer fits in the remaining GPU memory alongside the base model.

Teams therefore choose low-rank adapters when they need to serve dozens of fine-tunes on one card and accept the modest quality drop that comes with rank eight to thirty two.

When the workload instead requires many high-rank adapters or very different base models, the economics shift back toward dedicated instances.

That's Models and Agents for today. If you found this useful, share it with someone who's trying to keep up with all these changes, and subscribe so you don't miss tomorrow's update. The AI world moves fast. We'll help you keep up. See you tomorrow. And if you'd rather watch than listen, find us on YouTube at Nerra Network — link's in the show notes.

One more thing — if you liked today's episode, our sister show Models and Agents for Beginners is worth a spot in your feed: artificial intelligence explained simply, for beginners and curious teens. It's one of the Nerra Network's daily briefings, all free at nerranetwork.com. New to the network? nerranetwork.com/start-here picks the right daily briefing for you in about two minutes.

This episode used AI voice synthesis of my voice — editorial selection and analysis are my own.