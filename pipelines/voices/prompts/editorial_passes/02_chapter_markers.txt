Segment this interview into podcast chapters.

Speakers: Mira (AI host), {{cohost_name}} (co-host, when present), {{guest_name}} (guest).
Cleaned transcript ([MM:SS] SPEAKER: text):
{{cleaned_transcript}}

Output a JSON array only (no prose, no code fences):
[{"start": <seconds>, "end": <seconds>, "title": "..."}]

Rules:
- 5-9 chapters covering the whole runtime, in order, no overlaps.
- start/end in SECONDS derived from the [MM:SS] timestamps.
- Titles are listener-facing and come FROM THIS TRANSCRIPT: each names the concrete thing discussed in that span (the subject, the decision, the claim or the example the guest actually made), in the guest's own terms. Never a generic label such as an introduction or a wrap-up, and never a topic that does not appear in the transcript above.
- Name a speaker in a title only if that speaker has lines in the span.
- First chapter starts at 0. Include a final chapter for the lightning round + closing bet if present.
