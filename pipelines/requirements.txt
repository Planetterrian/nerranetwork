# Nerra Voices (The Age of AI) pipeline dependencies.
# The pipelines reuse engine/* heavily, so this pulls the root requirements
# and adds nothing new — kept as its own file because the nerra_voices_*
# workflows install exactly this path (spec §5).
-r ../requirements.txt
