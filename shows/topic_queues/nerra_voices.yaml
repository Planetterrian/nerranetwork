# Nerra Voices — DELIBERATELY EMPTY topic queue (guard rail).
#
# Production episodes are made by the Nerra Voices live-interview pipeline
# (pipelines/voices/ + .github/workflows/nerra_voices_*.yml), NOT by
# run_show.py. This queue stays empty so an accidental
# `run_show.py nerra_voices` dispatch is a clean skip (`narrative_queue_empty`)
# before any credits are spent. Do not queue entries here unless you
# intentionally want a run_show-generated contingency episode.

queue: []
